# v0.2.53.52 Dashboard Audit UI(2026-06-30)

## 1. 背景与目标

承接 [v0.2.53.51 approval_gate_audits 落档骨架](./v0.2.53.51-audit-filing-skeleton-2026-06-29.md):

- v0.2.53.51 完成了 P1:alembic 0016 迁移 + `ApprovalGateAuditStore` Protocol/Stub/InMemory + `BusinessWriterImpl` 4 动作成功/失败 audit 落档
- **v0.2.53.52** 完成**P2:Dashboard UI 展示 audit 区块**:
  - `GET /api/approval-gate/audits?limit=10` 端点(沿 v0.2.53.7-10 GET 只读范本)
  - `build_approval_gate_audits_payload(ctx, limit)` 响应(只读 + safe_list 静默降级)
  - `DashboardContext.audit_store` 注入 + `with_audit_store()` 不可变更新(沿撞坑 #64)
  - `_try_build_audit_store()` 联动注入(BUSINESS_WRITER_ENABLED=1 → InMemoryApprovalGateAuditStore)
  - HTML inspector 升级:`audit-card` system 视图 + 8 端点 + `renderAudits()` 渲染函数
  - 撞坑 #69 mypy 状态失真 顺手修复:test_oauth2.py 2 处 unused-ignore 清理

**核心不变式**:
- `audit_store` 默认 `ApprovalGateAuditStoreStub`(撞坑 #65 范本)
- 联动注入仅在 `BUSINESS_WRITER_ENABLED=1` + `DASHBOARD_REAL_DB=1` + session_factory 成功时(撞坑 #65 opt-in 4 阶段)
- 端点 GET 只读 + safe_list 静默降级(沿 v0.2.53.7-10 范本)
- HTML 渲染离线兜底 + enabled 状态 tag(Stub/InMemory)
- 撞坑 #50 第二层防御:`test_app.py` L811 + `quality_snapshot.py` 同步(2582 passed / 88.92%)

## 2. 设计决策

### 2.1 撞坑 #65 opt-in 4 阶段范本

- `DashboardContext.audit_store` 默认 `ApprovalGateAuditStoreStub`(is_enabled=False,record 永远失败)
- `with_audit_store(store)` 不可变更新(沿撞坑 #64 公共 API 范本)
- `_try_build_audit_store()` 工厂:
  - 尝试构造 `InMemoryApprovalGateAuditStore`(测试场景用,无 DB 接入)
  - 任何 Exception → 返回 None(由 caller 走默认 Stub)
  - 不接真实 SQL DB(沿 v0.2.53.51 真实 Impl 留 8/1 后)
- 联动注入条件(沿 v0.2.53.27 范本):`BUSINESS_WRITER_ENABLED=1` + session_factory 成功 + BusinessWriterImpl 注入成功 → audit_store 配套注入
- 单项失败静默降级 Stub(沿 v0.2.53.8 范本)

### 2.2 撞坑 #64 公共 API 一致性

- `with_audit_store()` 不可变更新模式与 `with_outbox_drafts/with_note_confirm/with_expense/with_business_writer` 100% 对齐
- 全部 `with_*` 方法都传 `audit_store=self.audit_store` 保持新字段在所有更新中不丢失
- `audit_store` 字段在 `DashboardContext` dataclass 中按位置放在 `business_writer` 之后(语义相关)

### 2.3 撞坑 #18 风险门控应用(沿用)

- audit_store.record() 失败不抛异常(沿 v0.2.53.51 「日志」语义)
- GET 端点 safe_list 静默降级 → 空列表(避免 store 故障阻塞 UI)
- HTML 渲染 enabled=false 状态 + 空列表显示"暂无 audit 记录"

### 2.4 撞坑 #50 第二层防御(沿用)

- `quality_snapshot.py` 同步 `pytest: 2582 passed / 1 skipped` + `coverage: 88.92%`
- `test_app.py` L811 同步 `"2582 passed"` 字符串断言
- 撞坑 #69 mypy 状态失真顺手修复(test_oauth2.py 2 处 unused-ignore 清理)

### 2.5 v0.2.53.7-10 GET 范本(沿用)

- `safe_list` 包装 list_recent(避免 store 异常阻塞)
- 端点 URL:`/api/approval-gate/audits?limit=10`
- `parse_limit` 严判 1-100(避免越界)
- 默认 Stub → `enabled: False, count: 0, items: []`
- InMemory 注入 → `enabled: True, count: N, items: [dict, ...]` 按 executed_at_ms DESC 倒序

## 3. 实施内容

### 3.1 DashboardContext.audit_store 字段注入

**更新**:`src/my_ai_employee/dashboard/context.py`

- 新增 `audit_store: ApprovalGateAuditStore` 字段,默认 `ApprovalGateAuditStoreStub.get_default_stub()`
- 新增 `with_audit_store(store)` 不可变更新方法
- 5 个 `with_*` 方法(已有 4 + 新 1)全部传 `audit_store=self.audit_store` 保持字段不丢失
- 新增 helper `_try_build_audit_store()`:
  - 尝试 `InMemoryApprovalGateAuditStore()` 构造
  - 任何 Exception → 静默降级 None
- `default()` 在 BUSINESS_WRITER_ENABLED=1 + BusinessWriterImpl 注入成功时联动注入 audit_store(沿 v0.2.53.27 范本)

### 3.2 build_approval_gate_audits_payload 响应

**更新**:`src/my_ai_employee/dashboard/responses.py`

```python
def build_approval_gate_audits_payload(
    ctx: DashboardContext, *, limit: int = 10
) -> dict[str, Any]:
    """GET /api/approval-gate/audits — 最近写操作 audit 列表(只读).

    沿 v0.2.53.7-10 GET 范本(只读 + safe_list 静默降级)+ v0.2.53.51 audit 落档:
        - 默认 ApprovalGateAuditStoreStub → enabled=False,空列表
        - DASHBOARD_REAL_DB=1 + BUSINESS_WRITER_ENABLED=1 → InMemory 注入
    """
    enabled = ctx.audit_store.is_enabled()
    raw_items = safe_list(lambda: ctx.audit_store.list_recent(limit=limit))
    return {
        "read_only": True,
        "enabled": enabled,
        "count": len(raw_items),
        "items": list(raw_items),
    }
```

**4 字段输出**:
- `read_only: True`(GET 只读契约)
- `enabled: bool`(Stub=False / InMemory=True,前端渲染用)
- `count: int`(audit 记录数)
- `items: list[dict]`(每条 8 字段:action/target_id/actor/reason/write_executed/affected_id/error/executed_at_ms)

### 3.3 GET /api/approval-gate/audits 端点

**更新**:`src/my_ai_employee/dashboard/handlers.py`

- `do_GET()` 新增路由:`path == "/api/approval-gate/audits"`
- 调用 `build_approval_gate_audits_payload(self.dashboard_context, limit=limit)`
- 复用 `parse_limit(limit_raw)` 严判 1-100(沿 v0.2.53.7-10 范本)
- `_READ_ONLY_METHODS = "GET, OPTIONS"`(默认只读)

### 3.4 HTML inspector audit section

**更新**:`docs/ui/codex-style-dashboard.html`

- 顶部 status strip 版本 pill:`v0.2.53.34` → `v0.2.53.52`
- `system` 视图新增 `audit-card`:
  - `<h3>Audit 落档 <span class="tag" id="audit-state-tag">默认 Stub</span></h3>`
  - `audit-source` 显示数据源描述
  - `audit-queue` 渲染 audit 列表(空状态 + 失败红 tag + 已执行绿 tag)
- `hydrateDashboard()` 新增 7th fetch:`/api/approval-gate/audits?limit=10`
- `renderAudits(payload, sourceLabel)` 渲染函数:
  - enabled 状态 tag(Stub="默认 Stub" / InMemory="InMemory 已注入")
  - 空状态 + reason 文案
  - 失败 audit(error 字段非 None)→ 红色 tag + tooltip 显示 error
  - 成功 audit(write_executed=True)→ 绿色 tag "已执行"
  - dry-run audit(write_executed=False)→ 灰色 tag "dry-run"
- 顶部 "API 已连接" 文案:`7 读` → `8 读`
- 离线兜底:API 断开时 `renderAudits({read_only, enabled: false, count: 0, items: []}, "API 离线兜底")`

### 3.5 撞坑 #69 mypy 状态失真修复

**更新**:`tests/core/test_oauth2.py`(本棒顺手修复 3 处 pre-existing mypy 错误)

- L201:删除 unused `# type: ignore[attr-defined]`
- L210:删除 unused `# type: ignore[arg-type]`
- L217:删除 unused `# type: ignore[attr-defined]`

**根因**:`test_oauth2.py` 在 v0.2.53.40 + v0.2.53.41 hotfix 后残留 unused-ignore(撞坑 #69 mypy 状态失真),本棒 P2 mypy strict 检查时暴露,沿 v0.2.53.41 范本清理

## 4. 测试覆盖

**`tests/dashboard/test_api.py`** — 新增 2 测试类 / **6 tests**:

### 4.1 TestApprovalGateAuditsPayload(4 tests)

- `test_default_stub_returns_empty`:默认 Stub → `enabled=False, count=0, items=[]`
- `test_inmemory_audit_returns_items`:InMemory 注入 → `enabled=True, count=2, items` 按 `executed_at_ms DESC` 倒序(2000 先于 1000)
- `test_inmemory_audit_respects_limit`:`limit=2` → 取最近 2 条(target_id=4 + 3)
- `test_failure_audit_recorded_with_error`:失败 audit(带 error 字段)→ `error="ValueError: outbox not found"` + `affected_id=None`

### 4.2 TestApprovalGateAuditsEndpoint(2 tests)

- `test_endpoint_default_stub_empty`:httptest e2e → 端点返回 200 + `enabled=False, count=0`
- `test_endpoint_inmemory_audits`:InMemory 注入 + e2e → 200 + `enabled=True, count=1, items[0].action="dismiss_anomaly"`

**`tests/core/test_oauth2.py`** — 同步 3 处 unused-ignore 清理(撞坑 #69 顺手修)

**`tests/menu_bar/test_app.py`** — L811 同步 `"2582 passed"` 字符串断言(撞坑 #50 第二层防御)

## 5. 9 质量门验收

| # | 门 | 命令 | 结果 |
|---|----|------|------|
| 1 | MD lint | `make lint` | ✅ 193 files 0 errors |
| 2 | mypy --strict | `make mypy` | ✅ 0 errors / 237 files |
| 3 | ruff check | `make ruff` | ✅ All checks passed |
| 4 | ruff format | `make format` | ✅ 251 files already formatted |
| 5 | pytest | `make test` | ✅ 2582 passed / 1 skipped |
| 6 | coverage | `make coverage` | ✅ 88.92%(80% 阈值达成) |
| 7 | alembic | `make alembic` | ✅ upgrade head --sql 干净执行 |
| 8 | build | `make build` | ✅ wheel + sdist |
| 9 | FINAL_EXIT | `make ci` | ✅ 9 质量门全绿 |

**质量快照同步**:
- `quality_snapshot.py`: `pytest: 2582 passed / 1 skipped` + `coverage: 88.92%`
- `test_app.py` L811 同步到 `2582 passed`

## 6. 撞坑累计与沿用

**本棒新增撞坑**:**0 类**(沿用撞坑 #18 / #50 / #64 / #65 / #69)

**撞坑累计 70 类沿用**,本棒重点应用:
- 撞坑 #18 风险门控:audit 落档「日志」语义 + safe_list 静默降级
- 撞坑 #50 第二层防御:`quality_snapshot.py` + `test_app.py` L811 同步
- 撞坑 #64 公共 API 一致性:`with_audit_store()` 不可变更新模式与 4 个 `with_*` 100% 对齐
- 撞坑 #65 opt-in 4 阶段范本:默认 Stub + 联动注入 + 单项失败降级 + 不动 ApprovalGate 决策矩阵
- 撞坑 #69 mypy 状态失真:test_oauth2.py 3 处 unused-ignore 顺手清理(沿 v0.2.53.40/41 范本)

**v0.2.53.51 沿用**:audit 4 不变式(写保护锁 raise / dry-run / invalid_target_id / 依赖未注入 都不落档)

**v0.2.53.27 沿用**:BUSINESS_WRITER_ENABLED env 门控 + 联动 InMemory 注入模式

**v0.2.53.7-10 沿用**:GET 只读 + safe_list 静默降级范本

## 7. 沿用边界(本棒)

- ❌ **不真发邮件 / 不写入真实凭据 / 不默认读取 Keychain 明文 / 不 kickstart launchd**
- ❌ **不移动 `v0.1.0` tag**(`2af775f` 锚定不动)
- ❌ **不打 `v0.2.x` tag**(沿 8/1 锚定策略)
- ❌ **不引入新依赖**(纯 stdlib + 自建 Protocol/Stub)
- ❌ **不接真实 `ApprovalGateAuditStoreImpl`**(留 8/1 后)
- ❌ **不接真实 BusinessWriter 真实写入**(默认 `_real_write_handler_enabled=False` 锁定)
- ❌ **不动 ApprovalGate 8 路径决策矩阵**(沿 v0.2.53.22)
- ❌ **dry-run 不落档**(撞坑 #18)
- ❌ **写保护锁 raise / invalid_target_id / 依赖未注入 都不落档**(撞坑 #18)
- ✅ **write_executed=True** 实写时(沿 v0.2.53.11 dry-run 上下文恒 False 不变式)
- ✅ **撞坑累计 70 类沿用**(本棒 0 新增)
- ✅ **撞坑 #69 顺手修复** test_oauth2.py 3 处 unused-ignore(沿 v0.2.53.41 范本)

## 8. 下一步

**P3(本次任务下棒)**:路径 4 实写 launch 前 checklist
- 4 门(DASHBOARD_WRITE_API=1 + confirm_text=CONFIRM_WRITE + BUSINESS_WRITER_ENABLED=1 + real_write_handler_enabled=True)
- 显式 launch flag(`ENABLE_PATH_4_WRITE=1` 顶级 env)
- 回滚 plan(4 门任一未达 → 立即 raise NotImplementedError)
- 文档清单:`docs/v0.2.53.53-path4-launch-checklist-2026-06-30.md`(只 docs-only)

**P4(任务最末棒)**:项目状态复盘
- SESSION-STATE.md / MODIFICATION-LOG.md / docs/v0.2-launch-plan.md 三文件状态同步
- 撞坑累计 70 类沿用审计
- 8/1 锚定策略维持(v0.1.0 tag 不动 / 不打 v0.2.x tag)

## 9. 关键文件路径

**更新**:
- `src/my_ai_employee/dashboard/context.py`(+ `audit_store` 字段 + `with_audit_store()` + `_try_build_audit_store()` + 5 个 `with_*` 同步)
- `src/my_ai_employee/dashboard/responses.py`(+ `build_approval_gate_audits_payload`)
- `src/my_ai_employee/dashboard/handlers.py`(+ `GET /api/approval-gate/audits` 路由)
- `src/my_ai_employee/quality_snapshot.py`(同步 `2582 passed / 88.92%`)
- `docs/ui/codex-style-dashboard.html`(系统视图 audit-card + 8 端点 hydrate + `renderAudits` 函数 + 版本 pill v0.2.53.52)
- `tests/dashboard/test_api.py`(+ 2 测试类 / 6 tests)
- `tests/menu_bar/test_app.py`(L811 同步 `2582 passed`)
- `tests/core/test_oauth2.py`(3 处 unused-ignore 清理,撞坑 #69 顺手修)

**新增**:
- `docs/v0.2.53.52-dashboard-audit-ui-2026-06-30.md`(本文件)

## 10. 关联文档

**前序 v0.2.53.x 文档链**:
- [v0.2.53.51 approval_gate_audits 落档骨架](./v0.2.53.51-audit-filing-skeleton-2026-06-29.md) — P1 audit 4 不变式
- [v0.2.53.50 报告搜索 UX 强化](./v0.2.53.50-reports-search-ux-2026-06-29.md) — Dashboard 报告页 P2 优化
- [v0.2.53.49 BusinessWriterImpl 写保护锁 + fake store 实写测试](./v0.2.53.49-business-writer-real-write-handler-2026-06-29.md) — 写保护锁 `_real_write_handler_enabled=False`
- [v0.2.53.27 BusinessWriterImpl opt-in 注入](./v0.2.53.27-business-writer-opt-in-2026-06-26.md) — opt-in 4 阶段范本(撞坑 #65)
- [v0.2.53.26 HTML dry-run 三门结果展示联调](./v0.2.53.26-html-three-gate-2026-06-26.md) — 三门联调 status
- [v0.2.53.20 HTML 实写设计稿](./v0.2.53.20-html-real-write-flow-design-2026-06-26.md) — §5.3 audit 落档 design

**撞坑索引**:
- 撞坑 #18 — 风险门控 + 实际写入留 8/1 后
- 撞坑 #50 — 第二层防御(quality_snapshot / test_app L811 同步)
- 撞坑 #64 — 公共 API 一致性(with_audit_store 不可变更新模式)
- 撞坑 #65 — opt-in 4 阶段范本(env 门控 + 默认 Stub + 单项失败降级 + 不动 ApprovalGate 决策矩阵)
- 撞坑 #69 — mypy 状态失真(unused-ignore 清理,本棒顺手修 3 处)
- v0.2.53.11 — write_executed 恒 False(dry-run 上下文) / True(实写上下文)不变式
- v0.2.53.22 — 8 路径决策矩阵(不动 ApprovalGate)
- v0.2.53.27 — BUSINESS_WRITER_ENABLED env 门控 + 联动 InMemory 注入模式
- v0.2.53.40 — mypy --strict tests 全清 300 errors 范本
- v0.2.53.41 — hotfix mypy 状态失真 范本
