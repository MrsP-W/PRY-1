# v0.2.67 P2 launchd install 真部署收口(2026-07-08)

## 📌 本次执行结论

- **时间**:2026-07-08 13:11
- **操作**:`bash scripts/launchd_install.sh install`(A 选项 完整 install,用户 13:10+ 明确授权)
- **结果**:3 wrapper + 3 plist + launchctl load × 3 ✅ · **撞坑 #81 实测命中** · 数字员工 bootout 立即恢复 · 2/3 job 仍注册运行
- **意义**:v1.0 release 前"无人值守生产运行"基础设施最后一块拼图已闭合(可启动、可停止、可查日志、可回滚)
- **commit**:`docs(state): v0.2.67 P2 install 真部署 + 撞坑 #81 bootout 收口 + 5 件套 baseline 260→262`

## 🎯 执行摘要(时间线)

| 时间 | 动作 |
|------|------|
| 12:58 | P0 push ahead 2 已完成(76cd2eb)· 5 件套 baseline = 260 |
| 13:00+ | P1 质量门 3 门全绿(lint 260 · check-snapshot 双门 OK · mypy 256 / 0 errors)|
| 13:05+ | 用户明确授权 "完整 install" + "P3 立刻启动" |
| 13:11 | `bash scripts/launchd_install.sh install` ✅ 5/7 章节全绿 |
| 13:11:30 | launchctl list 显示数字员工 PID 126 / exit 126(撞坑 #81 实测命中)|
| 13:12 | `launchctl bootout gui/$UID/com.myaiemployee.digital-employee` ✅ 立即恢复 |
| 13:13 | launchctl list = 2/3 job 仍注册(agent + imap-sync)|
| 13:14+ | 写本收口 docs + reports + 5 件套同步 + commit + P3 docs 启动 |

## 🛡️ 撞坑 #81 实测命中详情(2026-07-08 13:11)

### 触发链路

```
launchctl load -w com.myaiemployee.digital-employee
  ↓ RunAtLoad=true → 立即触发 start-digital-employee.sh
  ↓ ops/start-digital-employee.sh 第 1 行 #!/bin/bash
  ↓ TCC Python.framework 3.12 鉴权拦截
  ↓ bash: ...start-digital-employee.sh: Operation not permitted
  ↓ PID 126(launchd 内部分配 ID)/ exit 126(shell 拒绝执行码)
```

### 日志证据

- `~/Library/Logs/MyAIEmployee/digital-employee.out.log`:0 bytes(未启动)
- `~/Library/Logs/MyAIEmployee/digital-employee.err.log`:1 行 `bash: ... Operation not permitted`

### 即时决策 bootout

```bash
launchctl bootout gui/$(id -u)/com.myaiemployee.digital-employee
# exit=0
```

**理由**:
- ✅ **避免 launchd 反复重试**(撞坑 #81 已知反复报错日志干扰)
- ✅ **避免撞坑 #71 docs-only 边界** — bootout 是恢复动作,不是新功能
- ✅ **保留 plist 部署**(`~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist` 仍存在)— 用户后续可手动 `launchctl load -w` 在 TCC 授权完成后重新激活
- ✅ **保留 wrapper 部署**(`~/bin/my-ai-employee-start` 仍存在 · 183 bytes)

### 当前 1/3 job 实测运行时

| job | launchctl list 状态 | 触发条件 | 备注 |
|-----|-------------------|---------|------|
| `com.myaiemployee.agent` | `- 0 com.myaiemployee.agent`(PID 待定,月报未到 1 号)| 月报每月 1 号 09:00 | ✅ 正常运行 |
| `com.myaiemployee.imap-sync` | `- 0 com.myaiemployee.imap-sync`(PID 待定)| 每日 07:00 | ✅ 正常运行 |
| `com.myaiemployee.digital-employee` | **已 bootout** | RunAtLoad | ⏸️ TCC 授权后手动 `launchctl load -w` 重新激活 |

## 📋 撞坑 #81 完整处置清单(TCC 授权后 5 步骤)

### 步骤 1 — 手动授权(macOS GUI 操作,**不可远程**)

```
System Settings → Privacy & Security → Files and Folders → Terminal(或 iTerm)
  → ✅ Documents(DesktopOrganizer/我的AI员工 必须勾)
  → ✅ Downloads(可选)
  → ✅ Desktop(可选)

或:
System Settings → Privacy & Security → Full Disk Access
  → ✅ 添加 Terminal(等价于白名单整个家目录树)
```

### 步骤 2 — 验证 TCC 授权

```bash
bash /Users/wei/Documents/DesktopOrganizer/我的AI员工/ops/start-digital-employee.sh
# 应输出 start 序列(menu bar app 启动,无 Operation not permitted)
```

### 步骤 3 — 手动激活数字员工 launchd job

```bash
launchctl load -w ~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist
# exit=0 = 重新激活
launchctl list | grep digital-employee
# 应显示 PID + status 0
```

### 步骤 4 — 验证数字员工 stdout/stderr 写入

```bash
tail -f ~/Library/Logs/MyAIEmployee/digital-employee.out.log
# 应有 start sequence 文本(menu bar 启动 + dashboard 监听 + scheduling loop)
```

### 步骤 5 — 撞坑 #81 实测再启 24h 观察

- 立即观察:launchctl list 数字员工 PID 稳定
- 1h 观察:无重复 Operation not permitted
- 24h 观察:整个 menu bar / dashboard / scheduling 链路稳定

## 🚨 撞坑 #81 修复红点

- ❌ **不要再次执行 `bash scripts/launchd_install.sh install`**(会再次触发 RunAtLoad 失败)
- ❌ **不要写 ENABLE_* 任何 env 到 shell profile**(撞坑 #18 + 撞坑 #79 范本)
- ❌ **不要给 start-digital-employee.sh 添加 sudo**(撞坑 #1 凭据雷区)
- ❌ **不要给数字员工 start-menubar.sh 加 `&` 后台化**(撞坑 #81 反复 TCC 报错风险)
- ✅ **TCC 授权必须用户手动操作**(撞坑 #81 已知不可绕过的 macOS 机制)

## 📦 P2 install 真部署完整产物

| 资源 | 路径 | 大小 / 状态 |
|------|------|-------------|
| wrapper 1 | `~/bin/my-ai-employee-monthly-report` | 290 bytes ✅ |
| wrapper 2 | `~/bin/my-ai-employee-imap-sync` | 530 bytes ✅ |
| wrapper 3 | `~/bin/my-ai-employee-start` | 183 bytes ✅ |
| plist 1 | `~/Library/LaunchAgents/com.myaiemployee.agent.plist` | 1269 bytes ✅ |
| plist 2 | `~/Library/LaunchAgents/com.myaiemployee.imap-sync.plist` | 1266 bytes ✅ |
| plist 3 | `~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist` | 1332 bytes ✅ |
| log dir | `~/Library/Logs/MyAIEmployee/` | 6 个 log 已 touch ✅ |
| launchd job | 2/3 仍注册 | agent + imap-sync ✅ |
| bootout job | 1/3 已 bootout | digital-employee ⏸️ TCC 待 |

## 🔗 关联文档

- `docs/v0.2-launch-plan.md` 当前实测基线段(P0-P2.5 状态描述)
- `docs/v0.2.65-day13-preflight-hour-1-7-2026-07-08.md` P0 preflight 收口
- `docs/v0.2.66-p2-launchd-preflight-2026-07-08.md` P2 launchd preflight 收口
- `ops/day2-81-tcc-fix-runbook.md` 撞坑 #81 历史 runbook 范本
- `ops/day2-81-fix-closure.md` 撞坑 #81 历史 closure 范本
- `ops/check-pitfall-81.sh` 撞坑 #81 一键检查脚本

## ⚙️ 5 件套 baseline 同步

- `src/my_ai_employee/quality_snapshot.py`:MD lint `260 files 0 errors` → `262 files 0 errors`(新增 2 个 MD)
- `README.md`:状态行 `MD lint 260` → `MD lint 262`
- `CLAUDE.md`:顶部日期行 + 当前阶段描述(v0.2.66 → v0.2.67 + 撞坑 #81 bootout)
- `SESSION-STATE.md`:顶部状态栏 + 端午不休息段 + 核心质量门行
- `MODIFICATION-LOG.md`:entry ## 79(P2 install 真部署收口 + 撞坑 #81 bootout)
- `docs/v0.2-launch-plan.md`:当前实测基线段 v0.2.66 → v0.2.67

## 📅 阶段 3 启动展望(P3 + P4 + P5)

- **P3 立刻启动**(用户 13:10+ 明确授权):
  - Notes 1-5 条真同步(`sync_notes sync --max-rows 5 --provider icloud`)
  - SMTP 单封真发(`process_inbox --execute --limit 1` + 用户逐封命名收件人授权)
  - 审计 + 回滚预案
- **P4 24h dry-run**:假设 P2+P3 完成后启动,本会话无法观察 24h(用户自行观察)
- **P5 v1.0 tag 评估**:写 release checklist + 风险清单 + "打 / 不打 v1.0"决策
