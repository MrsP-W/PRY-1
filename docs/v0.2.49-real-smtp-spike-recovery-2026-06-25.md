# v0.2.49 真实 SMTP spike 收口包(2026-06-25 · docs-only · 凭据待激活)

> **状态**:🟡 **待凭据激活** · **承接**:v0.2.44 跳过授权码 + v0.2.47 8/1 release tag 预检包(撞坑 #58 8 项前置条件)
> **边界**:`docs-only` 收口包 · **不真发邮件** · **不写入真实凭据** · **不 kickstart launchd** · **不移动 v0.1.0 tag** · **不打 v0.2.x tag**
> **维护者**:Mr-PRY · **模型**:MiniMax-M3 · **分支**:main

---

## 1. 收口目标

承接 v0.2.44 跳过授权码 → v0.2.47 8/1 release tag 预检包(撞坑 #58 8 项前置条件 #2 唯一缺口)→ **v0.2.49 真实 SMTP spike 收口包**(本报告)。

**核心目的**:**整理真实 SMTP spike 完整执行入口**,用户输入凭据后立即可触发,无需重新设计命令模板。

---

## 2. 凭据激活 checklist(用户 yes + 凭据后执行)

### 2.1 前置条件(用户操作)

- [ ] **step 1 · 用户授权**:`yes + 凭据已就绪`(撞坑 #18 风险门控范本)
- [ ] **step 2 · Keychain 凭据准备**(用 `security` 命令,不写到任何文件):
  ```bash
  # Outlook/Microsoft OAuth2 token(沿 v0.2.2 #5 + D5.6.5 4 重防误发)
  security add-generic-password -s "my-ai-employee.smtp.outlook" -a "<email>" -w "<oauth2_token>"

  # Gmail OAuth2 token(沿 v0.2.2 #5)
  security add-generic-password -s "my-ai-employee.smtp.gmail" -a "<email>" -w "<oauth2_token>"

  # QQ 授权码(沿 v0.2.7 outlook/gmail SMTP 真实发送 spike 准备)
  security add-generic-password -s "my-ai-employee.smtp.qq" -a "<qq_number>" -w "<auth_code>"
  ```

### 2.2 5 重防误发门控核对(用户 + spike_send_100.py 双层)

- [ ] **SMTP_REAL_NETWORK=1** env 启用(默认 0 = InMemory 模拟)
- [ ] **--count 1** 单封硬上限(默认 100 = spike 范本)
- [ ] **--to <user@example.com>** 显式收件人(避免发给自己)
- [ ] **--confirm "yes-i-understand"** 二次确认(防止脚本误触)
- [ ] **--db-path** 真实 DB 路径(避免污染 spike 路径)

### 2.3 执行命令模板(只写不执行)

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 真实 spike(5 重防误发全开)
SMTP_REAL_NETWORK=1 .venv/bin/python scripts/spike_send_100.py \
    --smtp-provider outlook \
    --count 1 \
    --to "<user>@example.com" \
    --confirm "yes-i-understand" \
    --db-path "$HOME/Library/Application Support/my-ai-employee/data.db"

# 或 Gmail
SMTP_REAL_NETWORK=1 .venv/bin/python scripts/spike_send_100.py \
    --smtp-provider gmail \
    --count 1 \
    --to "<user>@example.com" \
    --confirm "yes-i-understand" \
    --db-path "$HOME/Library/Application Support/my-ai-employee/data.db"

# 或 QQ(沿 v0.2.43 解封白名单)
SMTP_REAL_NETWORK=1 .venv/bin/python scripts/spike_send_100.py \
    --smtp-provider qq \
    --count 1 \
    --to "<user>@example.com" \
    --confirm "yes-i-understand" \
    --db-path "$HOME/Library/Application Support/my-ai-employee/data.db"
```

### 2.4 检查产出(用户)

```bash
# 1. 检查 spike 产出
ls -lt output/spike/spike_send_100_*.md | head -3
cat output/spike/spike_send_100_<latest>.md | grep -E "provider|sent|latency"

# 2. 检查 OutboxEntry 状态(应为 SENT)
.venv/bin/python -c "
from my_ai_employee.core.db import Database
from my_ai_employee.db.outbox import OutboxStore
db = Database.open(db_path='$HOME/Library/Application Support/my-ai-employee/data.db')
store = OutboxStore(db.session_factory)
print('SENT count:', store.by_status('sent'))
print('Total:', sum(1 for _ in store.by_status('pending_send')))
"

# 3. 跑质量门(无回归)
make test && make mypy && make lint
```

---

## 3. 安全护栏(撞坑 #18 风险门控范本)

- ❌ **不写入真实凭据**到任何 docs / scripts / 测试 fixture
- ❌ **不读 Keychain 明文打印**(用 `security find-generic-password` 仅核对存在)
- ✅ **凭据唯一存储位置**:macOS Keychain(`security add-generic-password`)
- ✅ **spike 凭据隔离**:`scripts/spike_send_100.py` 临时注入 + 用完即清
- ✅ **5 重防误发门控**(SMTP_REAL_NETWORK=1 + --count 1 + --to + --confirm + --db-path)
- ✅ **真实 DB 路径**:不污染 spike 路径

---

## 4. 撞坑 #59(本轮新增)— 凭据激活 docs-only 收口范本

**现象**:用户说"凭据已就绪"但实际未提供 Keychain 凭据时,docs-only 收口包 = **正式化 spike 执行入口**(命令模板 + checklist)而非实际 spike。

**范本**:**docs-only 收口 = 真实执行入口的预制模板**,用户激活时无需重新设计命令。

**教训**:**真实 spike 收口 = docs-only 包(执行入口) + 真实 spike(凭据后执行)** — docs-only 部分应**完整、可执行、可审计**,不是占位符。

---

## 5. 撞坑 #18 风险门控范本沿用

- 5 重防误发门控 = **撞坑 #18 风险门控范本 + D5.6.5 4 重防误发**
- docs-only 收口不引入新功能 / 不动 v0.1.0 tag / 不打 v0.2.x tag
- 真实 spike 触发 = 用户 yes + 凭据 Keychain 就绪(用户触发后跑 spike)

---

## 6. 沿用边界与下一步

### 6.1 沿用边界 ✅

- ✅ docs-only 收口包(本包纯文本)
- ❌ **不真发邮件**(用户 yes + 凭据前不触发)
- ❌ **不写入真实凭据**
- ❌ **不 kickstart launchd**
- ❌ **不移动 `v0.1.0` tag**(`2af775f`)
- ❌ **不打 `v0.2.x` tag**(8/1 锚定策略)
- ✅ 工作树 clean · HEAD 以 `git rev-parse --short HEAD` 为准

### 6.2 下一棒候选

| 选项 | 描述 | 阻塞 |
|------|------|------|
| **真实 SMTP spike 执行** | 用户 yes + 凭据 Keychain | 凭据 |
| v0.2.50 8/1 tag 锚定评估 | 检查员强制截点重新评估 | 等 8/1 |
| v0.2.51 OutboxDispatcher SMTPProviderFactory 接入 | 沿用户 yes 启动代码改动 | 需用户授权 |

---

## 7. 关键产出文件

| 文件 | 改动 |
|------|------|
| `docs/v0.2.49-real-smtp-spike-recovery-2026-06-25.md` | 本报告(凭据激活 docs-only 收口包)|

---

## 8. 下一棒

- **用户**:凭据 Keychain 就绪后 yes 触发(本包提供完整执行入口)
- **主 Agent**:8/1 当天 12:00+ 检查员强制截点重新评估
- **检查员**:8/1 当天检查 8 项前置条件是否到位(尤其 #2 真实 SMTP)

---

> **累计**:v0.2.42 - v0.2.49 共 8 commits · **v0.2.49 真实 SMTP spike 收口包 = docs-only 部分已收口(真实 spike 待凭据激活)**
> **下次清理**:凭据 Keychain 准备就绪后,沿本包 2.3 命令模板触发真实 spike