# v0.2.39 启用 `--check-untyped-defs` 为 CI 默认(2026-06-24)

> **状态**:✅ 已收口 · **承接**:`385c9f6` + `a057ad9` v0.2.38 P1-1 mypy 严格模式 9 errors 修复
> **边界**:Makefile 配置变更 · 不真发邮件 / 不 kickstart launchd / 不移动 v0.1.0 tag / 不打 v0.2.39 tag
> **维护者**:Mr-PRY · **模型**:MiniMax-M3 · **分支**:main

---

## 1. 修复目标

**承接 v0.2.38 P1-1**:撞坑 #55 严格模式 mypy 双 0 范本落地 — 把 `mypy --check-untyped-defs` 提升为 CI 默认目标,确保后续 PR 不会回退到默认模式。

**撞坑 #50 docstring/code 漂移**:
- Makefile L214 注释写"严格模式"
- 实际命令 `mypy src tests` **未加** `--check-untyped-defs`
- v0.2.38 P1-1 修复 9 errors 后才能安全启用严格模式

---

## 2. 修复方案

### 2.1 Makefile 变更

```makefile
# 修前(撞坑 #50 docstring/code 漂移)
.PHONY: mypy
mypy: ## 9 质量门 — mypy 类型检查(严格模式,沿 v0.1 9 门范本)
    @echo "$(BLUE)🔍 mypy 类型检查$(RESET)"
    @$(PYTHON) -m mypy src tests

# 修后(v0.2.39 严格模式化)
.PHONY: mypy
mypy: ## 9 质量门 — mypy 类型检查(严格模式 `--check-untyped-defs`,沿 v0.1 9 门范本 + v0.2.39 严格模式化)
    @echo "$(BLUE)🔍 mypy 类型检查(严格模式)$(RESET)"
    @$(PYTHON) -m mypy --check-untyped-defs src tests
```

**变更说明**:
1. L214 注释明示 `--check-untyped-defs` 参数
2. echo 加 `(严格模式)` 标识
3. 实际命令加 `--check-untyped-defs` 参数

### 2.2 验证结果

```bash
$ make mypy
🔍 mypy 类型检查(严格模式)
Success: no issues found in 209 source files
```

---

## 3. 9/9 质量门全绿

| 门 | 结果 |
|----|------|
| **make mypy(严格模式)**| **0 errors / 209 files** ✨ **本轮严格模式化** |
| pytest | **2265 passed / 1 skipped / 88.77% coverage** |
| ruff check | All checks passed |
| ruff format | 246 files formatted |
| alembic --sql | exit 0 |
| uv build | OK |
| **MD lint** | **0 errors / 129 files** |

---

## 4. 撞坑沉淀

### 4.1 撞坑 #55(本轮升级)— 严格模式 mypy 双 0 范本 + CI 默认化

**v1.0(v0.2.38 沉淀)**:严格模式 mypy 双 0 = 默认模式 0 errors + 严格模式 0 errors(单点验证)
**v2.0(v0.2.39 升级)**:严格模式 mypy 双 0 范本 + CI 默认化 = 范本升级为**强制约束**(避免后续 PR 回退)

**范本应用**:
- 撞坑 #50 docstring/code 漂移范本:Makefile 注释写"严格模式"但实际未启用 = 漂移
- 撞坑 #55 严格模式 mypy 双 0 范本延伸:不只验证一次 0 errors,要让 CI 强制守住

**教训**:**"撞坑 #55 范本落地"不只是修 9 errors,而是要让 CI 强制守住** — 把 `--check-untyped-defs` 提升为 Makefile 默认目标,任何后续代码变更必须通过严格模式,避免回退。

### 4.2 撞坑 #50 docstring/code 漂移沿用

**沿用**:
- Makefile 注释说"严格模式"但实际未启用 = 漂移
- v0.2.39 修复:注释加 `--check-untyped-defs` 明示 + 实际命令加参数
- 撞坑 #50 沿用:doc 口径必须与实际状态一致

---

## 5. 沿用边界与下一步

### 5.1 沿用边界 ✅

- ✅ Makefile 配置变更,纯 CI 强化
- ❌ 不真发邮件 / ❌ 不 kickstart launchd
- ❌ 不移动 `v0.1.0` tag(`2af775f` 未动)
- ❌ 不打 v0.2.39 tag(8/1 锚定策略)
- ✅ 18 commit 链(v0.2.25-v0.2.39 + 4 docs-only 收口 + v0.2.38 P1-1 + 本轮 v0.2.39 严格模式化)
- ✅ 工作树 clean · HEAD 以 `git rev-parse --short HEAD` 为准

### 5.2 下一步候选

| 选项 | 描述 | 阻塞 |
|------|------|------|
| **pyproject.toml mypy config 同步** | `disallow_untyped_defs = true` 锁死 | 需用户授权(可选) |
| outlook/gmail SMTP 真实 spike | 沿 v0.2.2 #8 工厂模式 | Keychain 凭据 + 用户授权 |
| 7/1 月度复盘 | 12:00 → 17:00 收官 | 7 天后自动启动 |
| 8/1 v0.2.1 release tag 锚定评估 | 沿 D5.7.2 范本 | 月度复盘后 |

---

## 6. 关键产出文件

| 文件 | 改动 |
|------|------|
| `Makefile` | L213-216 mypy target 加 `--check-untyped-defs` + echo 加 `(严格模式)` 标识 + 注释明示 |
| `docs/v0.2.39-mypy-strict-ci-default-2026-06-24.md` | 本报告 |

---

## 7. 下一棒

- **用户**:等 outlook SMTP 真实凭据 + 授权 / 或选择 pyproject.toml 同步锁死
- **主 Agent**:7/1 月度复盘统一 review v0.2.25-v0.2.39 十四类报告
- **检查员**:7/1 月度复盘 review 撞坑 #55 v2.0 严格模式 mypy 双 0 + CI 默认化范本 + 撞坑 #50 docstring/code 漂移沿用

---

> **累计**:v0.2.25-v0.2.38 十七 commit 链 + **v0.2.39 严格模式 mypy 双 0 + CI 默认化 = 十八 commit 链**
> **下次清理**:2026-07-01 12:00+ 检查员归档 2026-06 旧记录(> 1 个月条目移到 archive/)