# v0.2.20 6/23 全链路重启实操前复核结果(2026-06-22 晚)

> **作者**:主 Agent · **最后更新**:2026-06-22(端午不休息第 5 天晚)
> **决策锚定**:用户决策"现在可以继续推进,但方向应从「写状态文档」切到「执行验证」"
> **性质**:**结果记录**(沿用户决策,不扩展新范本,只列实测值 + 三类判定)
> **前置 commit**:`04c97d4` v0.2.20 restart authorization state
> **下沿用范本**:[[v0.2.19-6-23-restart-execution-package-2026-06-22]] §2 阶段 1-5 5 校验命令

---

## §1 状态冻结(A0 实测)

| 维度 | 预期 | 实测 | 状态 |
|------|------|------|------|
| HEAD | `04c97d4` | `04c97d4` | ✅ |
| 起点 HEAD(v0.2.19 终点)| `dcbd6fe` | `dcbd6fe` | ✅ |
| v0.1.0 tag | `2af775f`(未动)| `2af775f` | ✅ |
| git status | clean | clean | ✅ |
| branch | main | main | ✅ |
| pwd | 我的AI员工 | 修复撞坑 #21(从 Agent Assistant 显式 cd) | ✅ |

---

## §2 阶段 1-5 复核实测(A1 + A2)

| # | 阶段 | 命令 | docs 预期 | **实测** | 撞坑命中 | 判定 |
|---|------|------|-----------|----------|----------|------|
| 1 | plist 数量 | `ls ~/Library/LaunchAgents/com.user.*.plist \| wc -l` | 1 | 1(`com.user.proxy-watch.plist`)| #24 命中(agent.plist 源存在未部署)| 🟢 **可继续** |
| 2 | 菜单栏 | `find src/my_ai_employee/menu_bar -name "*.py" \| wc -l` | 5 | **7**(+`__init__.py` + `app.py`)| #27 命中(5→7)| 🟢 **可继续** |
| 3 | Notes | `find src/my_ai_employee \| grep -i note \| grep "\.py$" \| wc -l` | 5 | **11**(跨 6 目录)| #28 命中(5→11,跨目录)| 🟢 **可继续** |
| 4 | alembic DDL | `uv run alembic upgrade 0014 --sql` | DDL 完整 | DDL 完整(0012→0013→0014, 6 行 ALTER/CREATE/UPDATE)| #30 命中(`--sql` 子命令格式)| 🟢 **可继续** |
| 5 | pytest | `uv run pytest --tb=short -q` | 2225 passed | **2225 passed, 1 skipped / 30.72s / 88.85%** | #31 baseline(13 mypy errors 不阻塞)| 🟢 **可继续** |

**A2 判定**:**5/5 通过 → GO**(进入授权候选)

---

## §3 三类判定(沿用户决策框架)

### 3.1 🟢 可继续(阶段 1-5 全部通过)

阶段 1-5 5 校验命令全部通过实测,docs 假设与实际差距已识别为撞坑 #24/#27/#28/#30/#31,均沿 v0.2.18 §3 撞坑史 6 类范本处理。**6/23 周二可执行实操**。

### 3.2 🔴 阻塞(0 项)

- 无

### 3.3 🟡 需用户授权(3 项)

| # | 项目 | 边界约束 | 解封时机 |
|---|------|----------|----------|
| 1 | **agent.plist 部署**(`launchctl load`)| ❌ 不真 kickstart launchd(沿用户 6/22 晚边界)| 用户授权 `bash scripts/launchd_install.sh install` |
| 2 | **W3 真账单 spike** | ❌ 不真导入账单 / ❌ 不造"真账单"结论 | 用户提供真实微信/支付宝 CSV 路径 |
| 3 | **outlook/gmail SMTP 真实 spike** | ❌ 不真发邮件 / ❌ 不打 v0.2.0/v0.2.1 tag / 密钥不打印 | 用户授权 + Keychain 凭据 + B 类白名单决策 |

---

## §4 docs 假设撞坑实际命中 3 例(对照 v0.2.18 §3)

| 撞坑 # | docs 假设 | 实测 | 撞坑类别 | 沿用范本 |
|--------|----------|------|----------|----------|
| **#24** | 3 plist 已部署 | 1 plist 已部署(agent.plist 源存在但未 `launchctl load`)| docs 假设错误 | v0.2.18 §3 + v0.2.19 §2 阶段 1 降级 |
| **#27** | 菜单栏 5 文件 | 7 文件(+`__init__.py` + `app.py` 漏算)| docs 假设错误 | v0.2.18 §3 + v0.2.19 §2 阶段 2 修正 |
| **#28** | Notes 5 文件跨 4 目录 | 11 文件跨 6 目录(adapters/ai/ai-prompts/connectors/migrations/db/menu_bar)| docs 假设错误 | v0.2.18 §3 + v0.2.19 §2 阶段 3 修正 |
| **#30** | `uv run alembic --sql 0014` | `uv run alembic upgrade 0014 --sql`(`--sql` 是子命令参数)| 命令格式错 | v0.2.18 §3 + 沿 [[2026-06-18-venv-sigkill-137-false-alarm]] 范本 |
| **#31** | mypy tests 0 errors | mypy tests 13 errors([no-any-return] × 13, 6 文件)| baseline 残留 | v0.2.19 §2 阶段 5 沿 baseline 不阻塞 |

---

## §5 阶段 6-7 实操启动条件(沿 v0.2.19 §3-§4)

### 5.1 阶段 6 W3 真账单 spike(等真实 CSV)

```bash
# 启动条件(阻塞):用户提供真实微信/支付宝 CSV + 文件路径
KEY=... uv run python -m my_ai_employee.connectors.wechat_bill import ~/Downloads/wechat_*.csv
KEY=... uv run python -m my_ai_employee.connectors.alipay_bill import ~/Downloads/alipay_*.csv
```

### 5.2 阶段 7 outlook/gmail SMTP 真实 spike(等授权 + 凭据 + 白名单)

```bash
# 启动条件(阻塞):用户授权 + Keychain 凭据 + B 类白名单扩展
KEY=... SMTP_REAL_NETWORK=1 uv run python -m my_ai_employee.agents.outlook_smtp_spike --to=<test_email> --subject="v0.2.19 spike" --body="test"
KEY=... SMTP_REAL_NETWORK=1 uv run python -m my_ai_employee.agents.gmail_smtp_spike --to=<test_email> --subject="v0.2.19 spike" --body="test"
```

---

## §6 终点基线(本次收口后)

| 维度 | 实测值 |
|------|--------|
| 起点 HEAD | `04c97d4` |
| 终点 HEAD | (本次 commit)|
| git status | clean |
| v0.1.0 tag | `2af775f` 未动 |
| pytest | 2225 passed, 1 skipped / 88.85% |
| alembic head | `0014_note_l2_cross_source` |
| 撞坑史类数 | 6 类(沿 v0.2.18)|
| docs/v0.2.20 | (本次新增,只记录结果)|

---

## 📌 文档元数据

- **创建时间**:2026-06-22 晚(端午不休息第 5 天)
- **决策锚定**:用户纠偏 `04c97d4` + 实操前复核决策"docs → 执行验证"
- **性质**:**结果记录**(沿用户决策,不扩展新范本)
- **下次更新**:6/23 实操启动后(沿 [[v0.2.19]] §2 阶段 1-5 复核命令或阶段 6-7 实操结果)

---

## §7 撞坑 #24 二次命中修正说明(2026-06-22 深夜补丁 · 选项 C 验证后)

> **修正者**:主 Agent · **触发**:选项 C launchctl print 验证(用户授权)
> **诚实报告**:本文 §2 A1-1 plist 数量报告**有错误**,撞坑 #24 真实根因是**主 Agent 命令匹配模式错误**,不是 docs 假设错误
> **沿用范本**:[[v0.2.18-docs-assumption-pitfall-2026-06-22]] §3 撞坑史 6 类 + [[b-class-deferral-2026-06-09]] B 类决策延后

### 7.1 撞坑 #24 二次命中根因

```bash
# v0.2.20 §2 A1-1 我用的命令(错误)
ls ~/Library/LaunchAgents/com.user.*.plist | wc -l
# 输出:1(只匹配 com.user.proxy-watch.plist)
# ↑ 错误:com.user.* 模式不匹配 com.myaiemployee.*

# 应该用(广匹配,纠正)
ls ~/Library/LaunchAgents/com.{user,myaiemployee}.*.plist 2>/dev/null | wc -l
# 输出:2(com.user.proxy-watch.plist + com.myaiemployee.agent.plist)
```

### 7.2 选项 C launchctl print 验证(只读无副作用 10 维度)

| # | 维度 | 实测值 |
|---|------|--------|
| 1 | `launchctl print gui/501/com.myaiemployee.agent` | `state = not running`(RunAtLoad=false,正常)|
| 2 | `launchctl list` 一行 | `- 0 com.myaiemployee.agent`(PID=0 = 未运行)|
| 3 | 目标 plist | 3483 bytes / Jun 16 09:57:17 2026 |
| 4 | `~/bin/my-ai-employee-monthly-report` | 197 bytes / bash / exec `uv run monthly_report "$@"` |
| 5 | 源脚本 `scripts/monthly_report.py` | 434 行 / 16533 bytes / -rwxr-xr-x |
| 6 | 日志目录 `~/Library/Logs/MyAIEmployee/` | 已创建,日志 0 bytes(未触发)|
| 7 | `plutil -lint` 目标 plist | OK |
| 8 | 目标 plist 与源 plist 一致性 | 完全一致(Label/ProgramArguments/StartCalendarInterval 全对)|
| 9 | `event triggers` | `com.myaiemployee.agent.268435470` / `calendarinterval` / `{Month=1, Day=1, Hour=9, Minute=0}` |
| 10 | `runs = 0` / `last exit code = (never exited)` | 从未运行(正常,2027/1/1 才触发)|

### 7.3 v0.2.20 §2 + §3 报告 vs 撞坑 #24 修正后真实状态

| 维度 | v0.2.20 §2 报告(错误)| 撞坑 #24 修正后(实测)| 差异 |
|------|-------------------------|-------------------------|------|
| A1-1 plist 数量 | 1 | **2** | +1 |
| A1-1 agent.plist 目标位置 | "缺失"(撞坑 #24 命中)| **已部署 5 天 3483 bytes** | 完全反转 |
| A1-1 launchctl 注册状态 | "未注册/空" | **已注册**(`- 0` PID=0) | 完全反转 |
| A3-1 launchctl install 授权 | 🟡 需用户授权 | ✅ **不需要**(已部署 5 天)| 取消授权项 |
| 撞坑 #24 真实根因 | docs 假设错误(plist 数量)| **v0.2.20 命令匹配模式错误**(`com.user.*` 不匹配 `com.myaiemployee.*`)| 主 Agent 错误 |

### 7.4 launchd 实操链 8 节点最终状态(选项 C 验证后)

| # | 节点 | 状态 | 关键实测 |
|---|------|------|----------|
| 1 | plist 部署 | ✅ | 3483 bytes / 6/16 09:57:17 |
| 2 | plist 校验 | ✅ | `plutil -lint` OK |
| 3 | plist 一致性 | ✅ | 目标 vs 源完全一致 |
| 4 | launchctl load | ✅ | `- 0 com.myaiemployee.agent` 已注册 |
| 5 | `~/bin/my-ai-employee-monthly-report` | ✅ | 197 bytes 已部署 |
| 6 | `scripts/monthly_report.py` | ✅ | 434 行 / -rwxr-xr-x |
| 7 | 日志目录 | ✅ | `~/Library/Logs/MyAIEmployee/` 已创建 |
| 8 | 日历触发器 | ✅ | 2027/1/1 9:00 待触发 |

### 7.5 撞坑 #21 pwd 漂移第四次复发

实测过程中 3 次 `cd` 后再次漂移到 Agent Assistant,导致 `scripts/monthly_report.py` 找不到。**根因**:Agent Assistant 与「我的AI员工」顶层路径相似(都 `DesktopOrganizer/` 下),Bash 跨项目必须每次显式 cd。**沿用范本**:`[[v0.2.18-docs-assumption-pitfall-2026-06-22]]` §3 撞坑 #21(7 文件夹重构后 B 类高频撞坑)。

### 7.6 三类判定修正后

- 🟢 **可继续 6 项**(原 5 + 新增 launchd install 不需要)
  - 阶段 1-5 5 校验命令全部实测通过
  - **launchd agent 8 节点完全就绪**(选项 C 验证)
- 🔴 **阻塞 0 项**:无
- 🟡 **需用户授权 2 项**(原 3,launchd install 已取消)
  - W3 真账单(等真实微信/支付宝 CSV)
  - outlook/gmail SMTP(等授权 + Keychain 凭据 + B 类白名单扩展)

### 7.7 沉淀路径(沿用用户"不再堆 docs-only"原则)

- ❌ 不开 v0.2.21+ 新 docs(沿 `04c97d4` 纠偏 + 6/22 晚"不再堆 docs-only")
- ✅ v0.2.20 docs §7 追加本补丁(只修正,不开新文件)
- ✅ SESSION-STATE timeline 追加 6/22 深夜一行
- ✅ MODIFICATION-LOG 追加 v0.2.21 段(累计 11 → 12 条)
- ✅ README 状态行 v0.2.20 → v0.2.21(撞坑 #24 修正 + 选项 C 验证 + launchd 已就绪)

### 7.8 沿用范本 + 教训

- **沿用**:`[[v0.2.18-docs-assumption-pitfall-2026-06-22]]` §3 撞坑史 6 类 + `[[b-class-deferral-2026-06-09]]` B 类决策延后 + `[[2026-06-18-venv-sigkill-137-false-alarm]]` SIGKILL 137 误报 + `[[d10.5.3-launchd-fixes]]` launchd 部署范本
- **新教训**(撞坑 #24 二次命中):
  1. **命令匹配模式需明确前缀**(避免假设 `com.user.*` 是全部用户 plist)
  2. **launchctl print 是只读真相源**(比 `ls ~/Library/LaunchAgents/` 更权威)
  3. **`- 0` PID=0 不是故障,是 launchd "已注册但未运行"正常状态**
  4. **撞坑史新增"主 Agent 命令匹配模式错误"子类型**(沿 v0.2.18 §3 docs 假设错误类下细分)
- **下次更新**:6/23 实操阶段 6 W3 spike 启动后 或 7/1 月度复盘