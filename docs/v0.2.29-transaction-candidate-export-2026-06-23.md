# v0.2.29 交易候选 Review/Export 机制收口报告(2026-06-23)

> **状态**:✅ 已收口 · **触发**:用户要求“按照建议执行” · **承接**:v0.2.28 L2 sign-lock 后剩余 `candidate_count=367` 需人工 review · **边界**:只读导出,不导入账单,不修改 DB,不自动合并候选

---

## 1. 目标

当前没有真实微信/支付宝 CSV 路径,不能启动 W3 真账单 `--max-rows 1` 小样本导入。

因此执行备选动作:把现有 `needs_confirm=1` 跨源候选变成可人工 review 的稳定导出产物,避免候选只停留在数据库或 spike 报告里。

## 2. 改动

### 2.1 Store 只读热路径

新增:

```python
TransactionStore.list_by_needs_confirm(limit=100, source_filter=None)
```

行为:

- 只查 `needs_confirm=1`
- 可选按 `source_filter` 过滤新交易来源
- 按 `imported_at_ms DESC` 排序
- 严判 `limit` 范围 `[1,10000]`
- 不修改 `status / needs_confirm / candidate_match_id`

### 2.2 导出脚本

新增:

```bash
uv run python scripts/export_transaction_candidates.py --format jsonl
uv run python scripts/export_transaction_candidates.py --format csv --output-path reports/transaction-candidates.csv
```

输出字段覆盖:

- 新交易:`tx_id/source/external_transaction_id/date/amount/counterparty/category/payment_method/status/fingerprint`
- 候选交易:`candidate_match_id/source/external_transaction_id/date/amount/counterparty/category/payment_method`
- 异常兜底:`candidate_missing`

## 3. 验证

```text
uv run pytest tests/db/test_transactions.py tests/scripts/test_export_transaction_candidates.py --no-cov
36 passed

uv run ruff check src/my_ai_employee/db/transactions.py scripts/export_transaction_candidates.py tests/db/test_transactions.py tests/scripts/test_export_transaction_candidates.py
All checks passed

uv run python scripts/export_transaction_candidates.py --help
exit 0
```

## 4. 下一步

1. 若用户提供真实 CSV:只跑 W3 真账单 `--max-rows 1` 小样本导入。
2. 若暂无 CSV:用本脚本导出现有 `needs_confirm=1` 候选,人工 review 367 类业务碰撞。
3. 仍不做:真实 SMTP、launchd kickstart、tag 移动、v0.2.x tag。
