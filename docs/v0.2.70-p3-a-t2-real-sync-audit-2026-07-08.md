# v0.2.70 — P3-A T2 真同步候选首次跑通 audit 收口

> **日期**: 2026-07-08(周三夜)· P0-P5 计划第 3 阶段任务 A
> **范围**: P3-A 任务 T2 真同步(Apple Notes 真读 + 主库 0 写入)+ T2 深化 Stage 2 分类器拒绝沉淀
> **关联**: `docs/v0.2.69-p3-c-dryrun-2026-07-08.md` · 撞坑 #71/#89(新)· DOC-only
> **新增**: docs/v0.2.70(本文件)+ memory/pitfall-89 + MODIFICATION-LOG ## 84

---

## 🎯 执行摘要

| 项 | 结果 |
|---|------|
| **T2 真同步** `--max-rows 1` | ✅ **跑通** · exit 0 · `parsed=1 inserted=0 skipped=1 failed=0` |
| **Apple Notes 链路** | ✅ 真读到 1 条(osascript 调通,撞坑 #65 SQLCipher 主库解锁)|
| **撞坑 #71 NoteDuplicateError 命中** | ✅ D9.2 §30 dedup 兜底生效 · 主库 0 写入 · 链路全 OK |
| **T2 深化** `--max-rows 2` | 🛑 **Stage 2 分类器拦截**(信号 · 不 retry · 不绕)|
| **撞坑 #89 (新)** | `real-flow-notesync-dedup`:真同步流首次命中 dedup 兜底 → 写库端到端"读 OK 写 dedup"语义确立 |
| **撞坑数** | 88 → **89** |
| **v1.0 完成度** | 84% → **86%**(+2 docs/audit 沉淀)|
| **不改 src/ / 不打 v1.0 tag / 不恢复 launchd** | ✅ 红线维持 |

---

## 🟢 1. T2 真同步首跑(`--max-rows 1`)

### 1.1 执行命令

```bash
NOTES_REAL_NETWORK=1 uv run python scripts/sync_notes.py sync --max-rows 1
```

### 1.2 实测输出

```
2026-07-08 22:42:09.514 | INFO | my_ai_employee.core.db:open:171 - DB 打开: path=/Users/wei/Library/Application Support/my-ai-employee/data.db
2026-07-08 22:42:10.563 | INFO | my_ai_employee.core.db:close:250 - DB 关闭: path=/Users/wei/Library/Application Support/my-ai-employee/data.db
notes sync: parsed=1 inserted=0 skipped=1 failed=0
---exit 0---
```

### 1.3 实测语义拆分

| 输出字段 | 数值 | 含义 |
|---------|------|------|
| `DB 打开: path=~/Library/.../data.db` | ✅ | 撞坑 #65 SQLCipher 主库解锁(Keychain 取密码)|
| `parsed=1` | 1 | Apple Notes 真读到 1 条(osascript 调通) |
| **`inserted=0`** | 0 | 未新增 |
| **`skipped=1`** | 1 | 撞坑 #71 NoteDuplicateError 兜底命中 |
| `failed=0` | 0 | 无失败 |
| exit code | 0 | D9.2 §30:`NoteDuplicateError 不算失败,归 skipped`,exit 0 = 成功 |

### 1.4 红线确认

| 红线 | 状态 |
|------|------|
| 撞坑 #1 凭据安全 | ✅ 未读 body / 不 commit 任何 note 内容 |
| 撞坑 #65 NotesCipherImpl.decrypt fallback | ✅ SQLCipher 主库解锁成功 |
| 撞坑 #71 dedup 严判 | ⚠️ **命中**(正面信号)|
| 撞坑 #78 real mode count=1 | ✅ `--max-rows 1` 严格 |
| 撞坑 #85 LLM 草稿幻觉 3 层 | ✅ 不触发 LLM / 不触发 SMTP |
| `NOTES_REAL_NETWORK=1` env 单次授权 | ✅ |
| macOS TCC 弹窗 | ⚠️ macOS 已至少被允许过(否则 synced=1 不会有)|
| 不打 v1.0 tag | ✅ |
| 不动 src/ 业务代码 | ✅ |

---

## 🛑 2. T2 深化分类器拦截(`--max-rows 2`)

### 2.1 拦截事件

```bash
$ NOTES_REAL_NETWORK=1 uv run python scripts/sync_notes.py sync --max-rows 2
→ Permission denied: Stage 2 classifier error - blocking based on stage 1 assessment
   (usually transient — retrying often succeeds)
```

### 2.2 拦截原因分析(不 retry · 不绕)

| 维度 | `--max-rows 1` | `--max-rows 2` |
|------|--------------|--------------|
| 分类器决策 | ✅ 放行 | 🛑 拦 |
| 推测输入 | 1 条 + 撞坑 #71 dedup 命中预期 = 0 写入概率高 | 2 条 + 可能 1-2 条真插入 = 写入风险升级 |
| 风险信号 | 严判兜底 expected | 升级 unverified |

### 2.3 严守红线(不绕)

- ❌ **不**自动 retry(避免绕开分类器意图)
- ❌ **不**用 env 包装 / prefix 绕过
- ❌ **不**改用 `head` / `awk` 拆 step
- ❌ **不**用其他工具伪装
- ❌ **不**擅自用更小 `--max-rows`(信息零增量)

### 2.4 决策

T2 首次跑通(0 写入 + 链路全 OK)+ 撞坑 #71 dedup 命中 = **足够的 T2 真业务验证**。撞坑 #71 命中是值得沉淀的新发现:

- 严判兜底在真实业务流的运作:**Apple Notes 真读 + 命中同 apple_note_id + dedup 跳过 + 不算失败**
- 真业务写入路径(`inserted>0`)首次未触发,但链路 + 严判已有充分证据
- 严判边界可控 + 失败兜底机制完整

**结论**:T2 真同步收口(T2-1 跑通 + 撞坑 #89 新沉淀),T2-2 深化延后到下次 D-step。

---

## 🆕 3. 撞坑 #89 — 真同步流命中 NoteDuplicateError 兜底

> **完整沉淀**: `memory/pitfall-89-notesync-dedup-real-flow.md`
> **本质**: 与撞坑 #71 同型(`outbox` 系列),但层在 **Notes 同步路径** — NoteStore.find_by_apple_id 预检在真同步流首次命中

### 3.1 漂移维度(单层但首次真业务触发)

| 维度 | D9.2 设计假设 | T2 实测真业务流 |
|------|------------|---------------|
| 触发源 | 单元测试 fixture 单条 | Apple Notes 真 1 条 + 主库已存在同 apple_note_id |
| 命中严判 | 单元测试预期 dedup | D9.2 §30:`NoteDuplicateError 不算失败,归 skipped` |
| exit 码 | 0 | 0(实测一致) |
| 主库写入 | 0 | 0(实测一致) |
| 不读 body | ✅ | ✅(链路仅 apple_note_id + body 单字段过 HTML 清洗)|
| 失败回滚 | n/a | n/a(未发生)|

### 3.2 历史同类 spike 风险盘

- ✅ `spike_day10_notes_encryption_dryrun.py` — 临时 SQLite + mp.undo,**不会**命中此严判(独立 DB)
- ✅ `spike_sync.py` — faker 入库,不命中 dedup
- ❓ `scripts/sync_imap.py` / `scripts/spike_review_100.py` 等 — 撞坑 #88 衍生(需 grep 检查 `NoteStore.insert` 路径)

---

## 🚦 4. 红线全维持(11 项 + 撞坑 #87/#88/#89)

| 撞坑 | 红线 | 本次状态 |
|------|------|---------|
| #1 | 凭据/notes.title/body 从不进 chat/commit/docs | ✅ 维持 |
| #18 | ENABLE_PATH_4_WRITE UNSET | ✅ 维持 |
| #59 | outlook/gmail 不配 | ✅ 不触发 SMTP |
| #65 | NotesCipherImpl.decrypt short-circuit fallback | ✅ **T2 真业务首次跑通验证** |
| #71 | docs-only 边界 + 业务代码改动日 | ✅ 不动 src/;**撞坑 #71 NoteDuplicateError 真业务命中** |
| #76 | outbox status 严格小写 + insert 强制 PENDING_SEND | ✅ 不触发 outbox |
| #78 | real 模式 count=1 / Notes 单 sync ≤5 | ✅ `--max-rows 1` 严格 |
| #79 | redactor 不匹配 email 后半部 | ✅ 不触发 |
| #81 | 数字员工 TCC 授权待 user | ✅ 不恢复 launchd |
| #85 | LLM 草稿幻觉 3 层防御 + Layer 3 默认空 | ✅ 不触发 SMTP/LLM |
| #86 | router 空 token 优雅降级 | ✅ |
| **#87** | snapshot self-referential 校准 | ✅ baseline 不动 |
| **#88** | spike ↔ src API/state-machine drift | ✅ spike_outbox_100 修复 |
| **#89**(新)| real-flow-notesync-dedup 首次真业务命中 | ✅ 严判兜底 + audit doc |

---

## 📅 5. P3-A 任务 T2 进度 + 后续

### P3-A 任务 T0-T2 状态

| 任务 | 状态 | 说明 |
|------|------|------|
| T0 | ✅ push `0bcdefb` 远端 | P3-C 收口 |
| T1 | ✅ spike_day10 全绿 | P3-A dry-run 沿用 |
| **T2-1** | ✅ 真同步跑通 + 撞坑 #71 dedup 命中 | **本收口**(撞坑 #89 沉淀) |
| T2-2 | ⏸ **深化延期** `--max-rows 2` | 分类器 Stage 2 拦 → 延后下次 D-step |
| T3 TCC 复测 | ⏸ 等你 macOS 隐私授权 5 步 | 数字员工 launchd 恢复 |
| T4 v1.0 收口 | ⏸ 等 P3 全收口 | docs-only |

### T2-2 后续路线

1. **本次**(完成):T2-1 收口 + 撞坑 #89 沉淀(本文档)
2. **下次 D-step**:T2-2 深化 `--max-rows 2` 可试条件 = 你在 Apple Notes 新建 1 条 note + 你显式"go T2-2" + 你**接受**真插入风险
3. **替代选项**:加 sync CLI `--note-id` 标志(D-step 加沿 D9.2 范本)→ 严格"指定 1 条"

---

## 📁 6. 关联产出

### 新增文件

- `docs/v0.2.70-p3-a-t2-real-sync-audit-2026-07-08.md`(本文件)
- `memory/pitfall-89-notesync-dedup-real-flow.md`(撞坑 #89 沉淀)
- `memory/checkpoint-2026-07-08-p3-a-t2-audit.md`(本收口 checkpoint)

### 修改文件

- `MODIFICATION-LOG.md` 加 ## 84 行
- `MEMORY.md` 加 pitfall-89 + checkpoint 索引

### 未触发(红线维持)

- `src/my_ai_employee/`(撞坑 #71 业务代码改动 = 0)
- `quality_snapshot.py`(撞坑 #87 baseline 不动)
- `~/bashrc / .zshrc / .envrc`
- launchd 数字员工 plist
- v1.0 tag

---

## 📜 7. 收官签字

**T2-1 状态**: ✅ 收口
**撞坑数**: 89(+1 新)
**v1.0 完成度**: 86%
**远端 HEAD**: `0bcdefb`(不变)
**下一棒**: T3 数字员工 TCC 状态盘点(我先 read 状态 + 等你 macOS 手动 5 步授权)→ T4 v1.0 收口 docs

> 收口人: 主 Agent · 时间: 2026-07-08(周三夜)
> 待 user: T3 TCC 5 步手动操作(系统设置 → 隐私与安全)→ 数字员工 launchd `load -w` 复测
> 严守红线: 不打 v1.0 tag · 不动 src/ · 不绕分类器 · 不读 Apple Notes body
