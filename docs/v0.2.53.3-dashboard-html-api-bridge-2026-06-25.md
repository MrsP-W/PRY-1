# v0.2.53.3 P2 — 静态 Dashboard 接只读 API(2026-06-25)

> **状态**:✅ 已落地 · **边界**:只读 GET、静态 HTML、127.0.0.1、无新依赖、不写凭据、不真发 SMTP。

## 结论

P2 下一步已从"只读 API 骨架"推进到"静态 HTML 可读取 API":`docs/ui/codex-style-dashboard.html` 会尝试读取本地 `/api/status` 和 `/api/tasks/today`,API 未启动时自动回退到静态假数据。

## 本轮变更

| 文件 | 变更 |
|------|------|
| `src/my_ai_employee/dashboard/handlers.py` | 增加本地静态页读取所需 CORS 头与 `OPTIONS` |
| `tests/dashboard/test_api.py` | 增加 CORS / OPTIONS 断言 |
| `docs/ui/codex-style-dashboard.html` | 首页指标、质量门、Keychain present/missing、SMTP 门控从只读 API hydrate |
| `docs/ui/codex-style-dashboard.md` | 说明 P2 API 连接方式 |

## 使用方式

```bash
make dashboard-api
open docs/ui/codex-style-dashboard.html
```

预期:

- API 启动时:页面顶部显示 `API 已连接`,指标来自 `127.0.0.1:8765`。
- API 未启动时:页面顶部显示 `API 离线兜底`,继续展示静态假数据。

## 安全边界

- 只允许 GET / OPTIONS。
- 服务仍绑定 `127.0.0.1`。
- CORS 仅用于静态 file 原型读取只读 API。
- 不读取或展示 Keychain 明文,只显示 present/missing。
- 不新增真实发送按钮,真实 SMTP 仍受 ApprovalGate 拦截。
