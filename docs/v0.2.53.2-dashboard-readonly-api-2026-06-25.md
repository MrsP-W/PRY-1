# v0.2.53.2 P2 — Web Dashboard 只读 API 骨架(2026-06-25)

> **状态**:✅ P2 骨架落地 · **边界**:只读 GET · 127.0.0.1 · 无新依赖 · 不写 Keychain / 不真发 SMTP

## 端点

| 路径 | 说明 |
|------|------|
| `GET /api/status` | 质量门快照、Git HEAD、Keychain present/missing、ApprovalGate 状态 |
| `GET /api/tasks/today` | 今日待办(邮件草稿 + Notes待确认 + 财务异常) |
| `GET /health` | 存活探针 |

## 启动

```bash
make dashboard-api
# 或
uv run python -m my_ai_employee.dashboard.server --port 8765
```

## 架构

- `src/my_ai_employee/dashboard/` — stdlib `ThreadingHTTPServer` + JSON
- 复用 P1 `menu_bar` 服务 Stub(注入 `DashboardContext`)
- 测试:`tests/dashboard/test_api.py`

## 暂不做

- POST/PUT/DELETE(写动作留 ApprovalGate P2+)
- 绑定非 127.0.0.1
- FastAPI/React 新依赖
