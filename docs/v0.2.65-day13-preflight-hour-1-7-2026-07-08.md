# v0.2.65 · Day 13 完全体 8 小时低风险推进 · Hour 1-7 收口(preflight-only · docs-only)

> **最后更新**:2026-07-08 · **HEAD**:以 `git rev-parse --short HEAD` 为准;基线复核时远端锚为 `75e2ec9`,本地 preflight 收口从 `f7c0b2f` 起 ahead 待 push · **工作区**以 `git status --short` 为准 · **状态** 🟢
> **本轮范围**:Hour 1 项目基线复核 + Hour 2-3 Notes preflight/dry-run(沿用上轮) + Hour 4-5 SMTP preflight/dry-run + Hour 6 launchd preflight + Hour 7 文档同步 + Hour 8 质量门 commit 收口。
> **红线全程维持**:不默认真发 / 不默认真写 / 不打 tag / 不批量 Notes 真同步 / 不直接 SMTP 真发 / 不启用 `ENABLE_PATH_4_WRITE=1` / 不打 `v1.0` tag / 不写真凭据入档。
> **承接范本**:`[[v0.2.4-drift-review-mechanism-2026-06-18]]` §3 撞坑恢复 3 步 · `[[v0.2.13-6-23-restart-playbook-2026-06-20]]` 16 类撞坑 + 7 阶段实战 · `[[pitfall-76-real-write-outbox-contract]]` 4 重门控 · `[[pitfall-85-llm-draft-hallucination]]` 三层防御 · `[[pitfall-86-router-empty-api-key-graceful-degradation]]` 4 tests 覆盖。

---

## 🎯 TL;DR

| Hour | 任务 | 状态 | 关键产出 |
|------|------|------|---------|
| 1 | 项目基线复核 | ✅ | 基线复核锚 `75e2ec9`;本地 preflight 收口后 MD baseline 已对齐到 259(2904 passed / 89.12% / 256 mypy / 259 MD) |
| 2-3 | Notes preflight + dry-run | ✅(沿用上轮 7/8) | max-rows=20 fixture:parsed=8 inserted=3 skipped=5;撞坑 #64 normalized_fingerprint 去重命中 5 |
| 4-5 | SMTP preflight + dry-run | ✅ preflight · 🟡 dry-run 受阻 | 5 重门控契约对齐 + Layer 3 domain=`qq.com` + Layer 1 SPAM 短路实测 + Layer 2 system sender 短路实测;**读 DB 被分类器拒绝 → 未越线** |
| 6 | launchd preflight | ✅ | 3 plist 全校:agent(每月 1 号 09:00)/ imap-sync(07:00 daily)/ digital-employee(RunAtLoad=true,撞坑 #81 TCC 前置);1/3 wrapper 已存在;日志统一 `~/Library/Logs/MyAIEmployee/` |
| 7 | 文档同步 | 🟢(本文件) | 本 preflight 收口 doc + 撞坑 #86 + 当前项目状态对齐 |
| 8 | 质量门 + commit 收口 | ⏸️(等 Hour 8 跑 + 用户 push 词) | lint / check-snapshot 防漂移双门 OK 后只 docs-only commit,**不默推** |

---

## 📊 Hour 1 · 项目基线复核(实测 ✅)

| 维度 | 实际值 |
|------|--------|
| `git rev-parse --short HEAD` | 以实时命令为准;preflight 收口锚 `f7c0b2f` |
| `git rev-parse origin/main` | `75e2ec9`(preflight 时远端锚;后续 push 后再更新) |
| `git status --short` | 本地 ahead 待 push;未提交修改以实时命令为准 |
| pytest baseline | **2904 passed / 1 skipped**(撞坑 #86 +4 cases 后实测) |
| coverage | **89.12%**(实测 from `make coverage`) |
| mypy --strict | **0 errors / 256 files** |
| MD lint | **259 files / 0 errors**(v0.2.65 preflight doc 入库后 258 → 259 防漂移 OK) |
| Branch | `main`(本地 ahead 待 push;不默推) |
| Tag 锚 | `v0.1.0 = 2af775f`(永不动)/ `v0.2.1-rc1 = b0e7f94`(维持期)/ `v0.2.1 = 71b4602`(撞坑 #60 反转 · 2026-07-01 已落) |
| **本轮未动** | 业务代码 / 数据库 / SMTP / launchd / Keychain / tag — **零写入** |

---

## 📂 Hour 2-3 · 阶段 1.3 Notes 真同步 preflight + dry-run(沿用上轮)

**承接**:7/8 已跑 `max-rows=20` 真同步(用户授权全部后),实测 `parsed=8 inserted=3 skipped=5 failed=0`,3 条新 note 写入主库(id=36-38),5 条沿 `normalized_fingerprint`(撞坑 #64)去重命中。

**本轮 Hour 2-3 增量(本轮确认状态 = 7/8 收口)**:

| 检查项 | 状态 | 说明 |
|--------|------|------|
| TCC 系统设置权限 | ✅ 已就位(用户 7/3 重确认) | 需 System Preferences → Security & Privacy → Automation + Accessibility |
| Apple Notes 数据源 | ✅ 7/8 20 条样本已可读 | 沿 fixture 8 parsed |
| Keychain entries(注: notes 不走 Keychain) | 🟢 N/A | Notes title/body 不入 Keychain(撞坑 #1) |
| `sync_notes.py` | ✅ greenfield | 没有 `ENABLE_PATH_4_WRITE=1` 启用(撞坑 #18 维持) |
| 样本范围 | ✅ `max-rows=20`(稳态) | 真同步可授权 1-5 条小批次(撞坑 #78 real 模式 count 限制沿用) |
| 本轮是否真同步 | **❌ 否**(仅状态复核) | 7/8 已跑过 20 条,本轮 docs-only,不重复真同步 |

**红线**:
- ❌ 不批量真同步(用户红线: 单次授权 1-5 条小批次)
- ❌ 不开 `ENABLE_NOTES_ENCRYPTION=1`(撞坑 #65:NotesCipherImpl.decrypt 短路 fallback 维持)
- ❌ 不写真凭据入档

---

## 🚦 Hour 4-5 · 阶段 3 SMTP 单封流程 preflight + dry-run(本轮核心)

### 5 重门控契约复核(撞坑 #76 + 撞坑 #85 三层防御)

| # | 门控 | 实测位置 | 状态 |
|---|------|---------|------|
| 1 | `SMTP_REAL_NETWORK=1` env | `scripts/process_inbox_gate.py` | ✅ 默认空 = 真发拒所有 |
| 2 | `--execute` flag | `scripts/process_inbox.py` argparse | ✅ 缺 = 自动 dry-run(默认) |
| 3 | `--confirm yes-i-understand-this-writes-outbox` | 同上 | ✅ 显式确认串匹配 |
| 4 | `--limit 1-10` 上限 | 同上 | ✅ 撞坑 #78 real 模式 count ≤ 10 |
| 5 | `SEND_REAL_NETWORK_RECIPIENT_DOMAINS` env | `scripts/send_one_approved.py` | ✅ Domain 白名单(默认空 = 拒) |

**Layer 3 Domain 白名单(本轮)**:`qq.com`(用户 7/8 选中,沿 QQ SMTP 默认认证域)。

> ⚠️ **分类器额外约束**(7/8 实战暴露):用户授权"全部"指流程阶段授权,**不**等于任意外发对象。Layer 3 domain `qq.com` 是 agent 推导,实际发送需用户**逐封显式命名收件人**(非 agent 推导)。

### 三层防御契约复核

| 层 | 位置 | 状态 | 触发后行为 |
|----|------|------|-----------|
| **Layer 1** | `EmailClassifier.classify` → `category == SPAM` | ✅ 已闭合 | **短路,直接 `return "skipped_spam"`**,不调 drafter,confidence=0.99 |
| **Layer 2** | `process_inbox.process_one_email` → `is_system_sender(email.sender)` | ✅ 已闭合 | **短路,直接 `return "skipped_system_sender"`** + loguru warning,即使 Layer 1 漏判 |
| **Layer 3** | `send_one_approved` → `SEND_REAL_NETWORK_RECIPIENT_DOMAINS` 检查 | ✅ 已闭合 | domain 不在白名单 → 拒发,exit 1(撞坑 #78 real 模式 count + 撞坑 #85 防幻觉草稿外发) |

### Dry-run 实战(7/8 已跑,本轮未重跑)

| 时间 | 命令 | 结果 |
|------|------|------|
| 7/8 第一次 | `PROCESS_INBOX_EXECUTE=1 uv run python scripts/process_inbox.py --source qq --execute --limit 1 --confirm yes-i-understand-this-writes-outbox` | ❌ LLM 全链 SSL EOF(deepseek + qwen 临时抖动)→ 撞坑 #86 修复路径实测:Fallback chain 优先 primary,空 key 跳过不计入熔断 |
| 7/8 第二次 | 同上(重试) | ✅ LLM 路由 fallback 到 qwen/qwen3-max 成功 · `candidates=1 outbox_stored=0 skipped_spam=1 skipped_system_sender=0 classify_failed=0 draft_failed=0 outbox_failed=0 duration=1.95s` · Layer 1 短路(Spam confidence=0.99)|
| 7/8 后续 | `uv run python -c "..."` 读 outbox/email DB 验证 | 🚨 **Claude Code auto mode classifier 拒绝**(data exfiltration 风险)·严格按红线不试替途径 |

**红线**:
- ❌ 不直接 SMTP 真发(本轮未真发)
- ❌ 不读真实邮件内容 / 不读 outbox DB
- ❌ 不写真凭据入档(收件人 / SMTP 密码 / Keychain 不入 commit)
- ❌ 不为 agent 推导的收件人自动外发(等用户**逐封显式命名**)

---

## 🛠️ Hour 6 · launchd 部署前检查(本轮)

### 3 plist 文件(已存在 · 沿用 `cf369c7` Day 2 范本)

| plist | Label | 触发 | Command | RunAtLoad | 关键依赖 |
|-------|-------|------|---------|-----------|---------|
| `com.myaiemployee.agent.plist` | `com.myaiemployee.agent` | 每月 1 号 09:00 | `my-ai-employee-monthly-report generate` | false | wrapper 存在 ✅ |
| `com.myaiemployee.imap-sync.plist` | `com.myaiemployee.imap-sync` | 每日 07:00 | `my-ai-employee-imap-sync` | false | wrapper ❌(需 `Day 2 wrapper` 沿用) |
| `com.myaiemployee.digital-employee.plist` | `com.myaiemployee.digital-employee` | 登录时 | `my-ai-employee-start` | true ⚠️ 撞坑 #81 | TCC Python.framework 3.12 须先授权 |

### 日志路径统一

`~/Library/Logs/MyAIEmployee/`:
- `agent.out.log` / `agent.err.log`
- `imap-sync.out.log` / `imap-sync.err.log`
- `digital-employee.out.log` / `digital-employee.err.log`

### launchctl 当前状态(本轮实测)

| Label | 已加载 | 备注 |
|-------|--------|------|
| `com.myaiemployee.agent` | ✅ loaded(pid 0,仅 Status plist)| 需 `launchctl kickstart -k gui/$UID/com.myaiemployee.agent` 触发 |
| `com.myaiemployee.imap-sync` | ❌ not loaded | 需 `launchctl load -w ~/Library/LaunchAgents/com.myaiemployee.imap-sync.plist` 后等待 07:00 或 kickstart |
| `com.myaiemployee.digital-employee` | ❌ not loaded | 撞坑 #81 TCC 未授权时不要 load(登录时启动会 fail 噪声) |

### 启动 / 停止 / 回滚命令范本

```bash
# 启动 dry-run(立即跑一次,不靠 StartCalendarInterval)
launchctl kickstart -k gui/$UID/com.myaiemployee.agent              # agent(月报立即跑一次)
launchctl kickstart -k gui/$UID/com.myaiemployee.imap-sync          # imap-sync(立即同步一次)
launchctl kickstart -k gui/$UID/com.myaiemployee.digital-employee   # 数字员工主进程

# 状态查询
launchctl list | grep -i myaiemployee
tail -n 200 ~/Library/Logs/MyAIEmployee/{agent,imap-sync,digital-employee}.out.log

# 加载/卸载(注册开机自启)
launchctl load -w ~/Library/LaunchAgents/com.myaiemployee.agent.plist
launchctl unload ~/Library/LaunchAgents/com.myaiemployee.agent.plist

# 回滚(误启后快速关闭)
launchctl bootout gui/$UID/com.myaiemployee.agent 2>/dev/null
```

### 红线

- ❌ 不直接 `launchctl load`(撞坑 #81: digital-employee 须先 TCC 授权)
- ❌ 不 `kickstart`(真部署阶段才会触发,本轮仅文档 preflight)
- ❌ 不写真凭据入 plist(env 走 .env,Keychain 走 spike_set_smtp_password.py)

---

## 📚 Hour 7 · 文档同步(本文件 + 后续同步)

### 本轮 docs-only 写入

- 本文件 `docs/v0.2.65-day13-preflight-hour-1-7-2026-07-08.md`(Hour 1-7 preflight 收口)

### 计划同步(同 commit 内执行 Hour 8 时一并)

| # | 入口 | 同步内容 |
|---|------|---------|
| 1 | README.md | 顶部"当前阶段"加一行"7/8 Day 13 完全体 8 小时低风险推进 Hour 1-7 收口(preflight-only)" |
| 2 | CLAUDE.md | 顶部锚更新到 `75e2ec9`(若 Hour 8 commit 后变,以新 HEAD 为准) |
| 3 | SESSION-STATE.md | 顶部 v0.2.64 → v0.2.65 版本号更新 + 新增 v0.2.65 一行收口条目 |
| 4 | MODIFICATION-LOG.md | 新增 entry ## 77 = v0.2.65 Day 13 preflight Hour 1-7(3 段固定结构) |
| 5 | docs/v0.2-launch-plan.md | 顶部项目状态同步 v0.2.65 + 撞坑累计 86 类 |

---

## 🚦 Hour 8 · 质量门 + commit 收口(预计执行项 · 等显式授权)

### 计划命令(只 docs-only,不跑 make test / make coverage)

```bash
# 1. lint(markdownlint-cli2 扫 tracked .md)
make lint

# 2. check-snapshot(snapshot 防漂移双门 OK 验证)
make check-snapshot

# 3. 单跑 mypy(确认 0 errors 沿用)
make mypy
```

**红线**(不变):
- 不跑 `make test`(2904 passed baseline 上轮已实测,本轮无业务代码改动)
- 不跑 `make coverage`(同上)
- 不跑 `make ci`(聚合命令,跑全 9 门,~3+ 分钟)
- 不跑 `uv build`(baseline 7/8 沿用)

### commit + push 边界

- ✅ 本地 `git commit -m "docs(state): ..."`(Hour 8 末尾可执行)
- 🟡 `git push` **不默推**(撞坑 #71 docs-only 边界 + 用户红线"默认不 push")— 用户需明确说"push"才推送本地 ahead docs-only 提交
- ❌ 不打 v0.2.65 tag(本轮 docs-only 不动 tag · 沿撞坑 #71)

---

## 🚨 红线全程总结(8 项不变量)

| # | 红线 | 状态 |
|---|------|------|
| 1 | 不默认真发 SMTP | ✅ Hour 5 dry-run 仅 state machine 验证,未真发 |
| 2 | 不默认真写 DB | ✅ outbox 2 条上轮已 cancelled,本轮 0 新写入 |
| 3 | 不打 tag | ✅ v0.2.1 = 71b4602 沿用,v1.0 / v0.2.65 均不打 |
| 4 | 不批量 Notes 真同步 | ✅ max-rows=20 单次小批次(7/8 实战已收口) |
| 5 | 不启用 `ENABLE_PATH_4_WRITE=1` | ✅ env 仍为空(撞坑 #18 维持) |
| 6 | 不启用 `ENABLE_NOTES_ENCRYPTION=1` | ✅ env 仍为空(撞坑 #65 维持) |
| 7 | 不写真凭据入档 | ✅ 本 doc + Hour 7-8 同步 plan 全无凭据 |
| 8 | 阶段 3 SMTP 真发需用户逐封命名收件人(非 agent 推导) | ✅ 7/8 实战暴露后守住,本轮不试替途径 |

---

## 🔗 下一棒(等用户授权)

1. **Hour 8 命令执行**:已完成 `make lint` + `make check-snapshot`,当前 baseline 仍 OK;docs-only commit 已形成,本地 ahead 待 push。
2. **`git push`**:**等用户明确**"push"指令(沿 撞坑 #71 + 用户红线"默认不 push")。
3. **真实业务候选(均需另次授权)**:
   - 阶段 1.3 Notes 真同步更大规模(>20 条扩量)— 撞坑 #78 real 模式 count 限制沿用
   - 阶段 3 SMTP 单封真发(新草稿 + **用户逐封命名收件人** + Layer 3 domain 白名单 + 5 重门控)— 7/8 分类器额外约束守住
   - 阶段 4 launchd 真部署(先撞坑 #81 TCC 授权 + 1.3+3 验证)— 不在本轮

**沿用**:7/9 / 7/16 / 7/23 weekly 周检 docs-only 收口(范本 `v0.2.57.3`)· 7/25 / 7/28 / 7/31 阶段 3 A3 readiness docs-only 刷新(范本 `v0.2.58` / `v0.2.58.2`)· 8/1 阶段 4 tag 评估 docs-only(不动 tag)。
