# v0.2.53.48 Dashboard 系统健康卡片硬编码修复(2026-06-29)

> **承接**:`v0.2.53.46 BusinessWriterImpl 4 动作实写骨架` — 沿用 9 质量门全绿 · 撞坑累计 70 类沿用
> **沿用边界**:docs-only + HTML 静态页微调 · 不前进 pytest/coverage · 不打 v0.2.x tag
> **质量门**:2546 passed / 88.81%(实测) / mypy --strict 0 / lint 189 / ruff + format 全绿

---

## 1. 背景与目标

v0.2.53.46 落地后,`docs/ui/codex-style-dashboard.html` L879 仍硬编码 `2273 passed`(v0.2.5+ 旧值),与 `quality_snapshot` 字段不同步,违反 "**Dashboard 与菜单栏共用 quality_snapshot**" 设计原则。

**v0.2.53.48 目标**:
- 移除 HTML 硬编码,统一由 `renderStatus()` → `/api/status` → `quality_gates` dict hydrate
- 撞坑 #50 第二层防御:实测 coverage 88.81% vs self-claim 88.83% 漂移 0.02pp 同步

---

## 2. 设计决策(2026-06-29 锁定)

### 2.1 HTML 硬编码 → JS hydrate 占位

| L# | 改动 | 沿用 |
|---|------|------|
| L879 | `<span class="tag green" id="system-pytest">2273 passed</span>` → `待读取` | JS L1486 `setText("system-pytest", quality.pytest ?? "unknown")` 自动 hydrate |
| L880 | `mypy --strict 0 errors` 保留(0 errors 是稳定语义,JS L1487 hydrate 仍覆盖) | 渐进式 |
| L881 | `make lint 待本轮复跑` 保留(历史信号,JS L1488 hydrate 覆盖) | 渐进式 |

### 2.2 撞坑 #50 第二层修复

- 实测 `make coverage` → 88.81%(`TOTAL 9614 1076 88.8% · 88.81%`)
- self-claim `quality_snapshot.coverage = "88.83%"` (v0.2.53.46 commit `8edb592` 记录)
- 漂移根因:v0.2.53.46 落地后未再跑 coverage,本地 `.coverage` 数据库已陈旧
- **修复**:`quality_snapshot.coverage` 字段 `88.83% → 88.81%`
- 沿 v0.2.53.32 撞坑 #50 第二层范本:实测为权威,docs-only commit 同步

---

## 3. 边界(沿 v0.2.53.46 + 撞坑 #50)

| 边界维度 | 边界规则 |
|---------|---------|
| **不前进 pytest/coverage** | docs-only + HTML 静态页微调,无新测试 |
| **不接真实业务 writer** | 沿撞坑 #18 风险门控 |
| **不发 SMTP / 不读 Keychain 明文** | 沿撞坑 #59 |
| **不打 `v0.2.x` tag** | 沿 D5.7.2 + 8/1 锚定 |
| **撞坑累计 70 类沿用** | 本棒 0 新增 |
| **9 质量门全绿** | docs-only commit 仍走完整 9 门验证 |

---

## 4. 测试覆盖(0 新增 + 0 改动)

- `tests/dashboard/test_api.py` 已有 `/api/status` 测试(沿 v0.2.53.2 P2 范本)
- HTML 静态页修改不影响 Python 端点契约
- 实测 2546 passed / 1 skipped 全绿(撞坑 #50 同步后未前进)

---

## 5. 质量门(9 门全绿 · 实测)

| 门 | 结果 |
|---|------|
| MD lint | 189 files 0 errors |
| mypy --strict | 0 errors / 235 files |
| ruff check | All checks passed |
| ruff format | 249 files already formatted |
| pytest | 2546 passed / 1 skipped |
| coverage | **88.81%**(88.83% → 88.81% · 撞坑 #50 第二层修复) |
| alembic upgrade head --sql | exit 0 |
| uv build | OK(sdist + wheel) |
| FINAL_EXIT | 0 |

---

## 6. 撞坑 #50 第二层修复应用

v0.2.53.48 暴露 0.02pp coverage 漂移(88.83% → 88.81%):

- 根因:本地 `.coverage` 数据库陈旧,实测 = 权威
- 修复:`quality_snapshot.coverage` 同步 88.81%
- 这是撞坑 #50 衍生第三版(自我应用 — 修复自己的 quality_snapshot 漂移)
- 沿 v0.2.53.46 撞坑 #50 第二层范本 + v0.2.53.32 撞坑 #50 第二层范本

---

## 7. 沿用边界 + 撞坑累计

**沿用边界**(本棒 0 新增,全部沿用):
- ❌ 不接真实业务 writer(实际写入路径留 8/1 后)
- ❌ 不写 DB 实际数据(默认 raise / dry-run 模式)
- ❌ 不发真实 SMTP
- ❌ 不读 Keychain 明文(沿 #59 撞坑规范)
- ❌ 不打 `v0.2.x` tag(沿 D5.7.2 + 8/1 锚定)
- ❌ 不移动 `v0.1.0` tag(`2af775f` 锚定不动)
- ❌ 不接 outlook/gmail SMTP(用户决策豁免)
- ✅ 默认 raise + 沿 v0.2.53.22 8 路径决策矩阵
- ✅ 撞坑 #18 风险门控(实际写入留 8/1 后)
- ✅ 撞坑 #65 opt-in 范本(env 门控 + 单项失败降级)
- ✅ 撞坑 #50 docstring/code 漂移防御(quality_snapshot 同步)
- ✅ 撞坑 #55 mypy --strict 双 0
- ✅ 9 质量门全绿
- ✅ write_executed 恒 False(沿 v0.2.53.11 不变式)

**撞坑累计 70 类沿用**(本棒 0 新增)。

---

## 8. 关联 commit

- **docs commit(本次)**:`docs(state): v0.2.53.48 Dashboard 系统健康硬编码修复 + 撞坑 #50 第二层` (docs + HTML 静态页)
- 沿用:`e76d716` (v0.2.53.46 build) + `8edb592` (v0.2.53.46 hotfix) + `a12f081` (v0.2.53.47 sync)

---

## 9. 下一棒(沿 v0.2.53.46 链)

- **P1-2 路径 4 fake store 测试** — BusinessWriterImpl 4 动作 `raise → 真实调 service` + fake store 测试 + audit 语义,**默认仍禁写**(撞坑 #18 风险门控)
- **P1-3 报告页强化** — `/api/reports/preview` + 搜索 UX 强化,沿 v0.2.53.10 范本,只读 GET
- **8/1 后独立 launch 路径 4 切换** — 实际写入留 8/1 后 + 用户明确授权

---

## 10. 跨项目关联

- **主项目** `我的AI员工`:`docs/ui/codex-style-dashboard.html` (1 处硬编码修复) + `src/my_ai_employee/quality_snapshot.py` (88.83% → 88.81% 同步) + `docs/v0.2.53.48-...md` (本次新建)
- **Agent Assistant**:仅跨项目 memory 沉淀(可选,本次未涉及)
- **范本沿用**:
  - 撞坑 #50 第二层(实测为权威 · docs-only commit 同步)
  - 撞坑 #18 风险门控(实际写入留 8/1 后)
  - 撞坑 #65 opt-in 范本(env 门控)
  - 撞坑 #55 mypy --strict 双 0
