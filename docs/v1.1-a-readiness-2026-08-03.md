# v1.1-A 启动门槛核对（2026-08-03，D6.15.1 docs-only）

> **范围声明（优先于正文）**：本文是 v1.1-A 启动候选前的 4 门槛核对文档，**不启动 v1.1-A、不修改 `src/`、`scripts/`、LaunchAgent、`ENABLE_*`**。
> **承接**：本文与 v1.0 tag 门槛预审（D6.18.3 `57480cb`）分别覆盖 v1.0 tag 与 v1.1-A 启动候选两条独立路径，门槛同源但判定窗口不同。

## 1. 4 必须门槛对照

| # | 门槛 | 当前状态 | 评估 |
| --- | --- | --- | --- |
| 1 | P3 7d unattended eligibility | elapsed ≈ 4/7 d；最近 3 份日报含 `health_unhealthy_sample` / `health_alert_opened` attention | **FAIL**：attention 非空；最早窗口 `2026-08-06T07:04:45Z` 后且连续 7d 无 attention 才可能 PASS。 |
| 2 | P3 30d P0/P1-free eligibility | elapsed ≈ 4/30 d | **FAIL**：时间未到；`2026-08-26T05:34:24Z` 后才可能 PASS。 |
| 3 | 评测样本 ≥ 30 条跨 suite | 30/30（`4edbfe2`） | **PASS**：3 suite，30 条脱敏样本。 |
| 4 | Feature Flag + SLO + Feedback 3 design docs | SLO `558f924`、Feedback `9f74797`、Feature Flag 接口 `92162a6` | **PASS**：3/3 docs-only。 |

总判定：**2 PASS / 2 FAIL（时间 + attention）**；v1.1-A 启动候选**当前不可解锁**。

## 2. 候选门槛（不阻塞）

| # | 候选 | 当前状态 |
| --- | --- | --- |
| 5 | 30+ 样本 2 轮扩 | 未推进（D6.18.1 计划项） |
| 6 | 撞坑 #104+ 全沉淀 | `pitfall-107` 已落地；D6.11.3 / D6.14.1 / D6.15.3 / D6.17.3 均确认区间无可识别新坑（`e72e034`） |
| 7 | v1.0 tag 评估 docs | D6.18.3 `57480cb` 已落地 |

## 3. 风险与未决项

- **R1**：`com.myaiemployee.agent` 非零退出 + 143/143 不健康样本的根因仍 `needs_human`；7d eligibility 重新核验前必须先授权处理。
- **R2**：30d 计时受 P3 epoch 起始时间绑定；若发生 rollover，30d 窗口随新 Day0 重置。
- **R3**：v1.1-A 解冻需 P3 4 必须 + Feature Flag 接口解冻条件（参见 [v1.1 Feature Flag 接口契约 §8](/Users/wei/Documents/DesktopOrganizer/我的AI员工/docs/v1.1-feature-flag-interface.md)）同时满足。
- **R4**：本文不发起任何 `ENABLE_*` 切换、不写 `flags` 表、不启动 SMTP / Notes 真同步。

## 4. 解锁 v1.1-A 的最小步骤（顺序）

1. 用户授权处理 `com.myaiemployee.agent` 根因；新 epoch 启动后重新核验 7d attention。
2. 等待 `2026-08-26T05:34:24Z` 后 30d 计时满足。
3. 用户对 Feature Flag 接口解冻条件（持久化方案 / ENABLE_* 兼容层 / 运行时调用点）逐项授权。
4. 在独立 worktree 起草 v1.1-A 启动候选 task-contract；Codex 验收后再合并。

## 5. 验收

- Markdown lint：0 issue。
- 任务包 `status: done`；`changed_files` 与 `git diff --name-status` 完全一致。
- `git diff --check` 通过；白名单仅 2 个 docs 文件。
- 不触碰运行时、tag、远程或 `ENABLE_*`。
