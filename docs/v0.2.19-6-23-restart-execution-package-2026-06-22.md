# v0.2.19 6/23 全链路重启执行包(紧凑)

> **作者**:主 Agent · **最后更新**:2026-06-22 · **端午不休息第 5 天(晚间)**
> **决策锚定**:候选 B 收口后,用户决策"v0.2.19 = 6/23 全链路重启执行包,不再堆 docs-only"(commit `317f7cb` 纠偏锚定)
> **结构**:5 段紧凑执行包(不沿用 v0.2.18 12 段范本)
> **沿用范本**:[[v0.2.17-6-23-readiness-final-sprint-2026-06-22]] §2 阶段 1-5 + [[v0.2.18-docs-assumption-pitfall-2026-06-22]] §7.3 docs 假设校验预演清单 + [[v0.2.13-6-23-restart-playbook-2026-06-20]] 7 阶段实战手册
> **边界承诺**:❌ 不真发邮件 · ❌ 不真导入账单 · ❌ 不真启动菜单栏 · ❌ 不真 kickstart launchd · ❌ 不移动 `v0.1.0` tag(`2af775f`)· ❌ 不打 `v0.2.0/v0.2.1` tag
> **前置 commit**:`317f7cb` docs(status): align v0.2.19 next-step snapshot

---

## §1 决策上下文

### 1.1 时间锚定

- **端午连休不休息**(2026-06-19~22,用户 6/17 确立)
- **v0.2.17**(5 阶段实测就绪)+ **v0.2.18**(撞坑史 6 类专项固化)docs-only 收口完毕
- **v0.2.19** 用户决策:**不再堆 docs-only**,直接准备并执行 6/23 全链路重启实操 7 阶段
- **当前**:6/22 周一晚,**v0.2.19 紧凑执行包 docs 收口** → 等 6/23 实操启动

### 1.2 与 v0.2.18 的差异

| 维度 | v0.2.18 | v0.2.19 |
|------|---------|---------|
| 段数 | 12 段(撞坑史专项固化)| **5 段**(紧凑执行包)|
| 目标 | 撞坑史 6 类可复用范本 | 6/23 实操启动 commands + checklist |
| 性质 | 沉淀 | 执行 |
| 触发 | docs-only 撞坑积累 | 实操准备 |

---

## §2 阶段 1-5 复核 5 校验命令(6/22 晚已实测就绪)

> **沿用范本**:[[v0.2.17]] §2 + [[v0.2.18]] §7.3 docs 假设校验预演清单 5 项
> **每项含**:命令 + 预期值 + 撞坑范本 + 降级应对

### 阶段 1:plist 数量(撞坑 #24 范本)

```bash
ls ~/Library/LaunchAgents/com.user.*.plist | wc -l
```

- **预期值**:`1`(`com.user.agent.plist`,3483 bytes)
- **撞坑 #24 范本**:docs 假设 3 plist,实际 1 个 → 沿 v0.2.18 §3
- **降级**:只跑 agent.plist 拉起,menu_bar.plist + notes.plist 6/23 不启动

### 阶段 2:菜单栏 5 子模块(撞坑 #27 范本)

```bash
find src/my_ai_employee/menu_bar -name "*.py" | wc -l
```

- **预期值**:`5`(`clipboard_capture.py` + `clipboard_listener.py` + `expense_service.py` + `note_confirm_service.py` + `tcc.py`)
- **撞坑 #27 范本**:docs 假设顶层,实际 `menu_bar/` 子目录 → 沿 v0.2.18 §3
- **降级**:5 文件源码完整(共 976 行),只 import 不拉起

### 阶段 3:Notes 5 子模块(撞坑 #28 范本)

```bash
find src/my_ai_employee | grep -i "note" | grep "\.py$" | wc -l
```

- **预期值**:`5`(跨 connectors/db/ai/adapters 4 目录 5 文件 2315 行)
- **撞坑 #28 范本**:docs 假设 `notes/` 单一目录,实际跨 4 目录 → 沿 v0.2.18 §3
- **降级**:5 文件源码完整,只 import 不拉起

### 阶段 4:alembic DDL(撞坑 #25 范本)

```bash
uv run alembic --sql 0014 2>&1 | head -50
```

- **预期值**:DDL 输出完整,无 SIGKILL 137
- **撞坑 #25 范本**:`.venv/bin/alembic` framework 签名失效 → 沿 [[2026-06-18-venv-sigkill-137-false-alarm]] 范本,`uv run` 包装器绕开
- **降级**:不重装 venv(macOS Sequoia TCC 冲突)

### 阶段 5:pytest(撞坑 #29 范本)

```bash
uv run pytest --tb=short -q 2>&1 | tail -10
```

- **预期值**:`2225 passed / 1 skipped / coverage 88.85%`(30.86s → 33.51s 正常波动)
- **撞坑 #29 范本**:docs 假设数量,实测验证 → 沿 v0.2.18 §3
- **降级**:失败行 `< 10` 直接 commit fix;`≥ 10` 暂停实操回 docs 收口

---

## §3 阶段 6 启动条件 — W3 真账单 spike(等真实 CSV)

> **沿用范本**:[[v0.2.9-W3-real-bill-spike-prep-2026-06-22]] §"6 项启动条件 checklist" + §"4 重风险门控"

### 3.1 启动条件(等用户提供)

| 条件 | 阻塞 | 解封时机 |
|------|------|----------|
| 真实微信 CSV(手动导出)| ⏸️ | 用户提供 |
| 真实支付宝 CSV(手动导出)| ⏸️ | 用户提供 |
| 文件路径(~/Downloads/wx_*.csv 等)| ⏸️ | 用户告知 |

### 3.2 启动命令(条件满足后)

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工
KEY=... uv run python -m my_ai_employee.connectors.wechat_bill import ~/Downloads/wechat_*.csv
KEY=... uv run python -m my_ai_employee.connectors.alipay_bill import ~/Downloads/alipay_*.csv
```

### 3.3 4 重风险门控

- ❌ 不造"真账单"结论(沿 [[v0.2.18]] §3 docs 假设错误类撞坑 #29 范本)
- ❌ 不修改 D6 transactions schema(沿 [[d6.2-fingerprint-dedup-launch]] 范本)
- ❌ 不扩展供应商名单(沿 B 类延后声明)
- ✅ 失败立刻回滚 + 留 SESSION-STATE 段(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3)

---

## §4 阶段 7 启动条件 — outlook/gmail SMTP 真实 spike(等授权 + 凭据)

> **沿用范本**:[[v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21]] §"6 项启动条件 checklist" + §"5 重风险门控" + [[v0.2.2-#8-SMTPProviderFactory]] 工厂模式

### 4.1 启动条件(等用户授权 + 凭据)

| 条件 | 阻塞 | 解封时机 |
|------|------|----------|
| Outlook 账号授权(MicrosoftOAuth2 已实化 `c0f83d4`)| ⏸️ | 用户授权 |
| Gmail 账号授权(GoogleOAuth2 已实化 `564b8db`)| ⏸️ | 用户授权 |
| Outlook/Gmail 凭据(写入 Keychain)| ⏸️ | 用户提供 |
| B 类白名单扩展(B1 outlook/gmail SMTP 白名单扩展)| ⏸️ | 用户决策 |

### 4.2 启动命令(条件满足后)

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工
KEY=... SMTP_REAL_NETWORK=1 uv run python -m my_ai_employee.agents.outlook_smtp_spike --to=<test_email> --subject="v0.2.19 spike" --body="test"
KEY=... SMTP_REAL_NETWORK=1 uv run python -m my_ai_employee.agents.gmail_smtp_spike --to=<test_email> --subject="v0.2.19 spike" --body="test"
```

### 4.3 5 重风险门控(沿 [[d5.6.5-real-send]] 4 重防误发 + 1 边界)

- ❌ 不真发邮件给非测试地址(D5.6.5 范本)
- ❌ 不打 v0.2.0/v0.2.1 tag(D5.7.2 范本 + 8/1 锚定策略)
- ❌ 不修改 SMTPProviderFactory 工厂模式(v0.2.2 #8 锁定)
- ❌ 不扩展 OAuth2 provider 白名单(B 类,等用户决策)
- ✅ 失败立刻回滚 + 留 SESSION-STATE 段

---

## §5 边界 + 6/23 待用户触发

### 5.1 边界承诺(本 docs-only 包)

- ❌ **不真发邮件**(阶段 7 等用户授权)
- ❌ **不真导入账单**(阶段 6 等用户 CSV)
- ❌ **不真启动菜单栏**(阶段 2-3 只 import 不拉起)
- ❌ **不真 kickstart launchd**(阶段 1 只 plist 数量验证,`launchctl load` 等用户授权)
- ❌ **不移动 `v0.1.0` tag**(`2af775f` 不动,沿 D5.7.2 范本)
- ❌ **不打 `v0.2.0/v0.2.1` tag**(留作 8/1 锚定策略)
- ❌ **不跑真实 OAuth flow**(等用户授权 + 凭据)
- ❌ **不造"真账单"结论**(等真实 CSV)
- 密钥不打印到聊天(KEY env var 方式)
- B 类决策延后到「我的AI员工」项目完成后

### 5.2 6/23 周二待用户触发清单

| 阶段 | 用户决策项 | 主 Agent 动作 |
|------|------------|---------------|
| 1 | 授权 `launchctl load agent.plist` | 阶段 1 命令 + 验证 launchd |
| 2-3 | (无需决策)| 阶段 2-3 import 验证 |
| 4 | (无需决策)| 阶段 4 alembic --sql |
| 5 | (无需决策)| 阶段 5 pytest |
| 6 | 提供真实 CSV(可选)| 阶段 6 命令 + 验证导入 |
| 7 | Outlook/Gmail 授权 + 凭据 + B 类决策(可选)| 阶段 7 命令 + 验证 SMTP |

### 5.3 终点基线锚定(本 docs-only 收口后)

| 维度 | 期望值 |
|------|--------|
| 起点 HEAD | `317f7cb` |
| 终点 HEAD | (本次 commit)|
| git status | clean |
| v0.1.0 tag | `2af775f` 未动 |
| make lint | 0 errors / 116 files(从 v0.2.18 115 + 1)|
| 撞坑史类数 | 6 类(沿 v0.2.18)|
| 范本累计 | 11(沿 v0.2.18)|
| docs/v0.2.19 | (本次新增 5 段紧凑)|

---

## 📌 文档元数据

- **创建时间**:2026-06-22(端午不休息第 5 天晚)
- **决策锚定**:用户结论"v0.2.19 = 6/23 全链路重启执行包,不再堆 docs-only"(`317f7cb` 锚定)
- **前置 commit**:`317f7cb` v0.2.19 next-step snapshot
- **沿用范本**:[[v0.2.17-6-23-readiness-final-sprint-2026-06-22]] + [[v0.2.18-docs-assumption-pitfall-2026-06-22]] + [[v0.2.13-6-23-restart-playbook-2026-06-20]] + [[d5.6.5-real-send]]
- **结构**:5 段紧凑(不沿用 v0.2.18 12 段范本)
- **下次更新**:6/23 实操启动后(沿 [[v0.2.10]] checklist 范本)
