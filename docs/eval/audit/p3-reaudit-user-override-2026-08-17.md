# P3 Re-audit 用户覆盖决策(2026-08-17)

## 0. TL;DR

- **触发**：用户基于运行态依据修正了 `37809f8` 的路径 A 触发判定
- **决策**：选 **B（继续监测）**，暂不执行路径 A
- **关键纠正**（原分析错误）：
  - PID 1708 caffeinate assertion 尚余 ≈4.3 天，**未过期**；可覆盖 8/20 16:42 CST 7d 时间点
  - 8/17 仅统计约 2.7 小时（health=12、news=2），与完整日不可比；折算后 ≈106/天、18/天
  - 唯一完整新日 8/16 = 72/12 → ⚠️ 部分恢复，**非"必须 A"档**
  - 睡眠日志显示真因：电池合盖睡眠（macOS battery lid-close）；caffeinate `-i` 和 `pmset -c` 均不覆盖电池场景
- **不做项**：`sudo pmset`、无限期 caffeinate、第二个 caffeinate、二次 rollover
- **窗口**：维持原 8/19 23:59 CST 监测；8/21 18:34 CST 前最晚审批 A1（30 天续期）
- **本任务**：docs-only 决策记录 + 纠正；**不动任何系统、仓库、P3 状态**

## 1. 用户反馈原始结论

```yaml
DECISION:
  path: B
  rationale: |
    1. 保持现状，观察至原定 8/19 23:59 CST，按完整 UTC 日或实际 gap 判定
    2. 若需覆盖 30d，最晚在 8/21 18:34 CST 前单独审批 A1：有限期 30 天续期
    3. sudo pmset、无限期 caffeinate、第二个 caffeinate、二次 rollover 均暂不做

KEY_EVIDENCE:
  - PID 1708 assertion 尚余约 4.3 天，并未过期，可覆盖 8/20 16:42 CST 7d 时间点
  - 8/17 仅统计约 2.7 小时；health=12、news=2；折算约 106/天、18/天
  - 与完整 8/16 相加除以 2 得出 42/7，没有完整日统计意义
  - 唯一完整的新日 8/16 为 72/12，属于"部分恢复"，不是"必须 A"档
  - 睡眠日志显示实际问题是电池状态下的合盖睡眠；延长 caffeinate -i 不能解决，pmset -c 也不覆盖电池场景
  - 当前 epoch 已产生历史 attention，续期不会反向清除；后续若要重新取得资格，还需要另行决策新的干净 epoch

READ_ONLY: true（未修改系统、仓库或 P3 状态）
```

## 2. 纠正原分析错误（`37809f8` 路径 A 触发依据不成立）

### 2.1 原分析（`37809f8`）

| 假设 | 原结论 | 真实状态（用户修正） |
|------|--------|---------------------|
| caffeinate `-i -t` 已过期 | 是路径 A 触发因素 | ❌ 实际剩余 ≈4.3 天；可覆盖 7d 时间点 |
| 8/17 数据含义"必须 A" | 是（按 <50 <12 阈值） | ❌ 8/17 仅 2.7 小时；折算后 ≈106/天、18/天（≥阈值） |
| caffeinate 续期能解决问题 | 是 | ❌ 真因是电池合盖睡眠；caffeinate `-i` 和 `pmset -c` 均不覆盖 |
| 当前 epoch 续期能清 attention | 否（历史阻断） | 否（仍阻断；但不影响决策） |

### 2.2 完整日 vs 部分日数据对比

| 日期 | 健康 | 新闻 | 完整日? | 判定 |
|------|------|------|---------|------|
| 2026-08-13 (rollover) | 137 | 23 | ✅ | 正常基线 |
| 2026-08-14 | 101 | 17 | ✅ | 略低 |
| 2026-08-15 | 9 | 1 | ✅ | 严重停摆（撞坑 #107 历史） |
| **2026-08-16** | **72** | **12** | ✅ | **⚠️ 部分恢复** |
| 2026-08-17 | 11 | 2 | ❌（仅 2.7h） | 不计入；折算 ≈106/天、18/天 |

**结论**：唯一完整新日 8/16 = 72/12 → ⚠️ 部分恢复，**非"必须 A"档**。

### 2.3 真因分析（基于睡眠日志）

- **撞坑 #107 复现**：8/15 严重停摆 + 8/17 11/2 → 高置信度机器睡眠
- **触发场景**：电池状态 + 合盖睡眠（macOS 默认行为）
- **caffeinate `-i` 限制**：仅阻止 idle sleep；不阻止 lid close / battery critical
- **`pmset -c disablesleep 1` 限制**：仅 AC 电源；电池场景不覆盖
- **唯一覆盖电池合盖**：`pmset -b disablesleep 1` 或 `pmset -a disablesleep 1`；但需 sudo

### 2.4 epoch 续期与 attention 关系

- 当前 epoch Day0=2026-08-13T08:42:06Z；已产生 attention（health_sample_gap、news_run_gap）
- caffeinate 续期不会反向清除历史 attention
- **若要重新取得资格**：需要新的干净 epoch（reread-only 路径 A1 触发）
- 但用户当前明确**不接受** caffeinate 续期 + 二次 rollover；仅观察

## 3. 决策与时间窗

### 3.1 当前决策

**B（继续监测）**：维持原 8/19 23:59 CST 监测计划；按完整 UTC 日或实际 gap 判定；不动系统/仓库/P3 状态。

### 3.2 时间窗

| 时点 | CST | UTC | 性质 |
|------|-----|-----|------|
| 当前 | 2026-08-17 13:30 CST | 13:30 UTC | 决策时点 |
| PID 1708 剩余 ≈4.3 天 | 约 2026-08-21 21:42 CST | 13:42 UTC | caffeinate 到期 |
| 8/19 23:59 CST 监测重评 | 2026-08-19 23:59 CST | 15:59 UTC | 原定触发点 |
| **8/21 18:34 CST A1 最晚审批** | 2026-08-21 18:34 CST | 10:34 UTC | 30d 续期截止 |
| 7d 时间门 | 2026-08-20 16:42 CST | 08:42 UTC | PID 1708 可覆盖 |
| 30d 时间门 | 2026-09-12 16:42 CST | 08:42 UTC | 需 30d 续期 |

### 3.3 路径 A1（30d 续期）边界

- **触发条件**：8/19 23:59 CST 重评判定"必须 A"
- **实施细节**（待触发）：
  - 单一 launchd 实例替换：caffeinate `-t 2592000`（30 天）覆盖 8/19 → 9/18
  - 受控操作：保留当前 PID 1708 进程；新进程接管时同步 plist；不引入第二个 caffeinate
- **触发关键词**：用户单独说「授权 A1 路径」或「30d 续期」

## 4. 不做项（硬边界）

| 操作 | 状态 |
|------|------|
| `sudo pmset` | ❌ 不做 |
| 无限期 caffeinate（移除 `-t`） | ❌ 不做 |
| 第二个 caffeinate 进程 | ❌ 不做 |
| 二次 rollover | ❌ 不做 |
| caffeinate 续期（30d / 无限期） | ❌ 暂不做（待 8/19 重评） |
| 系统电源策略修改 | ❌ 不做 |
| plist 修改 | ❌ 不做（除非路径 A1 触发） |
| load / unload plist | ❌ 不做 |

## 5. 现有状态维持

### 5.1 不变项

- `main` = `37809f8` = `origin/main`
- ahead/behind = 0 / 0
- 全量 lint = 342 文件 0 issues
- 未跟踪 WIP = 2 项（C 类）
- worktree = 1 个（main）
- worktree/.archive/ = 1 个（d6102-20260817）
- 任务包 `TASK-20260819-001-p3-gap-reaudit` 状态 = `queued`（维持，不改）

### 5.2 实时监控（持续）

- 8/17 健康/新闻 计数 → 8/18 早晨核验
- 8/18 健康/新闻 计数 → 8/19 早晨核验
- 8/19 健康/新闻 计数 → 8/19 23:59 CST 触发完整 UTC 日判定

## 6. 已验证 / 未验证

### 6.1 已验证（只读）

- ✅ PID 1708 进程存在（launchctl list）
- ✅ p3-awake plist caffeinate `-i -t` 参数确认
- ✅ 8/16-8/17 健康/新闻 部分数据（不完整）
- ✅ 8/16 完整日数据（72/12）→ ⚠️ 部分恢复
- ✅ 8/17 仅 2.7h 数据（11/2）→ 不计入完整日判定
- ✅ 当前 epoch 已产生 attention（无法反向清除）
- ✅ 现有状态不动（用户已确认）

### 6.2 未验证

- ❌ caffeinate 实际到期精确时间（plist 内 `-t` 参数；用户报告 ≈4.3 天剩余）
- ❌ macOS 睡眠日志（用户已说明"电池合盖睡眠"，未直接读取）
- ❌ `pmset -b disablesleep 1` 是否用户计划中的备选方案（用户暂不做）
- ❌ 8/18-8/19 实际健康/新闻 数据（待时间窗结束）

### 6.3 边界

- 不动 P3 状态目录
- 不 load / unload plist
- 不跑 `ops/run-claude-p3-watch.sh`
- 不跑 `scripts/p3_rollover_epoch.py`
- 不修改任何文件除本任务审计 + MODIFICATION-LOG
- 不 push ahead（待 ff-only + push 本任务）

## 7. 推荐下一步动作

1. **本任务**：commit + ff-only + push（docs-only 决策记录）
2. **本任务后**：维持现状；不动任何系统；等 8/19 23:59 CST 完整 UTC 日判定
3. **可选提前触发**（用户单独决定）：8/18 或 8/19 早手动重新审计
4. **路径 A1 触发**（若 8/19 判定"必须 A"）：用户单独以「授权 A1 路径」关键词启动

## 8. 决策签名

- 模型：M3（MiniMax-M3）主执行；TERRA/LUNA 未唤醒。
- 工作树：`/tmp/wt-p3-reaudit-user-override-20260817`，分支 `codex/p3-reaudit-user-override-20260817`。
- 基线：main=`37809f8`=origin/main；本地 ahead=0；提交后应为 ahead=1。
- 时点：`2026-08-17T14:00:00Z`（写入时）。
- 决策方：用户「现在选 B（继续监测）」(2026-08-17 下午)
- 任务包：`TASK-20260819-001-p3-gap-reaudit` status=`queued`（维持）
