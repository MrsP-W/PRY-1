# v0.2.77 — P3-A T3 L4 撞坑 #94 B 路径修复实战验证 ✅ + 撞坑 #95 NEW 暴露(2026-07-09)

> **目的**:#94 B 路径(`make_sqlalchemy_engine(db_path=...)` 长生命周期连接重开)真实 `launchctl load -w` 复验结果归档。撞坑 #94 DB context manager **完全验证通过** · 撞坑 #95 NEW 暴露(`ProcessType=Background` 不允许 fork 子进程,数字员工 launcher 一次性模式)。

---

## 🎯 一句话结论

| 维度 | 状态 | 备注 |
|------|------|------|
| **撞坑 #94 B 路径实战** | ✅ **完全通过** | launchctl load -w + 9/9 预检全过 + menu_bar.log 显示 DB 反复 open/close(B 路径生效)+ 无 RuntimeError |
| **数字员工 launcher 退出** | ✅ **exit 0** | launcher 21:27:37 启动 → 21:27:45 完成(8 秒)→ exit 0(orchestrator 设计) |
| **撞坑 #95 NEW 暴露** | ⚠️ **数字员工 plist `ProcessType=Background` 不允许 fork** | menu_bar PID 26648 + dashboard PID 26659 启动后 ~50s 内被 launchd 回收(launchd 禁止 Background fork) |
| **1h/24h 无人值守观察** | 🟡 **混合模式启动** | agent + imap-sync 2/3 常驻 + 数字员工 plist 注册但 RunAtLoad 一次性 |
| 9/9 质量门 | ✅ 全绿 | 2920/1/89.12/285/257 |
| HEAD | `74dc9db` | ahead 0 · 数字员工 launchd plist `state=not running · active count=0`(预期) |

**下一步唯一建议**:1h 观察窗(锚 agent + imap-sync + 数字员工 launcher 一次性 RunAtLoad 模式)+ docs/v0.2.78 #95 修复 D-step(改 plist `ProcessType=Interactive` 或拆 launcher 与守护进程)+ 24h 观察 → v1.0 tag 评估(默认不打)。

---

## 📊 5 阶段执行证据

### Phase 1(0-5 min)前置核验 + 状态清理

```text
$ git rev-parse --short HEAD
74dc9db
$ git log --oneline -3
74dc9db fix(launchd): #94 menu_bar db_path sqlalchemy engine
c8049ec docs(t3-l4): T3 L4 撞坑 #93 launchctl 实战验证 ✅ + #94 menu_bar DB context manager NEW 暴露
e74be55 style(launchd): format #93 test guard

$ make check-snapshot
OK: quality_snapshot matches live baseline (2920 passed / 1 skipped · 285 md files)
OK: state entry docs match quality_snapshot

$ make mypy
Success: no issues found in 257 source files
$ make lint
Linting: 285 file(s) · 0 errors

# 备份+清干 log
$ cp ~/Library/Logs/MyAIEmployee/digital-employee.err.log /tmp/digital-employee.err.log.bak2
$ cp ~/Library/Logs/MyAIEmployee/digital-employee.out.log /tmp/digital-employee.out.log.bak2
$ cp ~/Library/Logs/MyAIEmployee/menu_bar.log /tmp/menu_bar.log.bak2
$ : > ~/Library/Logs/MyAIEmployee/{digital-employee.{err,out},menu_bar}.log
logs truncated
```

**#94 B 路径核心代码 diff**:
- `src/my_ai_employee/core/sqlcipher_compat.py`:+33/-2
  - `make_sqlalchemy_creator(db=None, *, db_path=None)` 接受 2 选 1
  - `make_sqlalchemy_engine(db=None, *, db_path=None)` 同样
  - `db_path` 模式:每次连接 `Database.open(db_path=path)` 独立 context
- `tests/core/test_sqlcipher_compat.py`:+73 lines(新文件,3 fixture + 3 test)

### Phase 2(5-10 min)真实 load -w + 10 秒等待

```text
$ launchctl list | grep myaiemployee
-    0    com.myaiemployee.agent
-    2    com.myaiemployee.imap-sync     ← 数字员工 bootout(预期)

$ launchctl load -w ~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist
load -w OK
$ date "+%H:%M:%S" → 21:27:37
$ sleep 10
$ date "+%H:%M:%S after 10s" → 21:27:47

$ launchctl list | grep myaiemployee
-    0    com.myaiemployee.agent
-    0    com.myaiemployee.digital-employee    ← exit 0(B 路径修复成功!)
-    2    com.myaiemployee.imap-sync
```

**关键校验**:
- ✅ 数字员工 exit code 从 c8049ec 时 exit 1 → exit 0(B 路径修复直接体现)
- ✅ launcher 完整执行 9/9 预检 + 启动 menu_bar + 启动 dashboard + 退出

### Phase 3(10-15 min)#94 B 路径实战证据

```text
$ cat ~/Library/Logs/MyAIEmployee/digital-employee.err.log
(空 — 无任何错误)

$ cat ~/Library/Logs/MyAIEmployee/digital-employee.out.log
[21:27:37] 🟢 一键启动数字员工(menubar + dashboard)...
[21:27:37] 🔍 预检:9 维度健康检查
✅   [1/9] .env 存在 (/Users/wei/Library/Application Support/MyAIEmployee/.env)
✅   [2/9] DB_ENCRYPTION_KEY 64 hex OK
✅   [3/9] Keychain QQ SMTP 授权码 present
✅   [4/9] alembic current OK
✅   [5/9] scripts/run_menu_bar.py 存在
✅   [6/9] dashboard.server 模块 OK
⚠️   [7/9] ⌥⌘N TCC 检查:用户须先授权 Python.framework 3.12(撞坑 #81 已修复)
✅   [8/9] docs/ui/codex-style-dashboard.html 存在
✅   [9/9] data/ 目录存在
✅ 预检全过 ✅
[21:27:40] 🚀 启动菜单栏后台常驻...
✅ 菜单栏已启动(PID=26648,log=/Users/wei/Library/Logs/MyAIEmployee/menu_bar.log)
[21:27:42] 🚀 启动 Dashboard 只读 API(127.0.0.1:8765 · DASHBOARD_REAL_DB=1)...
✅ Dashboard 已启动(PID=26659,log=/Users/wei/Library/Logs/MyAIEmployee/dashboard.log)
[21:27:45] 🎉 启动完成
[21:27:45] 📋 1-click 审批入口(Day 6 不自动真发 · 撞坑 #59 红线维持):

$ tail -30 ~/Library/Logs/MyAIEmployee/menu_bar.log
2026-07-09 21:27:42.090 | INFO  | my_ai_employee.core.config:load_env:68 - .env 已加载
2026-07-09 21:27:42.480 | INFO  | my_ai_employee.core.db:open:171 - DB 打开
2026-07-09 21:27:42.482 | INFO  | my_ai_employee.core.db:close:250 - DB 关闭
2026-07-09 21:27:42.931 | INFO  | my_ai_employee.core.db:open:171 - DB 打开
2026-07-09 21:27:45.229 | INFO  | my_ai_employee.core.db:open:171 - DB 打开
resource_tracker: There appear to be 3 leaked semaphore objects to clean up at shutdown
```

**#94 B 路径核心证据**:
- ✅ err.log **完全空**(无 RuntimeError,无 Operation not permitted,无 DB 已关闭)
- ✅ out.log **完整 9/9 + 启动菜单栏 + Dashboard + 🎉 启动完成**
- ✅ menu_bar.log **DB 反复 open/close 模式**(每次连接独立 Database.open,这是 B 路径的预期行为)
- ✅ 数字员工 launcher exit 0(从 c8049ec 时 exit 1 → 0)
- ⚠️ `resource_tracker: 3 leaked semaphore objects` Python 3.12 multiprocessing 已知无害警告(不影响功能)

### Phase 4(15-25 min)#95 NEW 暴露

```text
$ sleep 30
$ pgrep -fl "run_menu_bar|dashboard.server"
(无 menu_bar/dashboard 进程)

$ cat ~/Library/Application\ Support/MyAIEmployee/data/menu_bar.pid
26648
$ cat ~/Library/Application\ Support/MyAIEmployee/data/dashboard.pid
26659
$ lsof -i :8765
(空 — 端口未监听)

$ launchctl print gui/$UID/com.myaiemployee.digital-employee | head -10
gui/501/com.myaiemployee.digital-employee = {
    active count = 0
    path = /Users/wei/Library/LaunchAgents/com.myaiemployee.digital-employee.plist
    type = LaunchAgent
    state = not running
    program = /Users/wei/bin/my-ai-employee-start
```

**#95 实测证据**:
- ⚠️ 数字员工 launcher 21:27:37 启动 → 21:27:45 完成(8 秒)→ exit 0
- ⚠️ 50 秒后 menu_bar/dashboard subprocess 全部退出
- ⚠️ plist `ProcessType=Background` 禁止 fork 子进程
- ⚠️ launchd 检测到 fork 行为并回收子进程(Background 模式 strict)
- ⚠️ PID 文件残留(26648/26659 写入但进程不存在)
- ⚠️ port 8765 未监听(dashboard 已退出)
- ⚠️ `state=not running · active count=0` 数字员工 job 完全空闲

**与之前撞坑的关联**:
- 沿 #91(Documents exec OS 拦截)+ #92(业务代码 Documents 沙箱)+ #93(launchd uv PATH 缺失)+ #94(menu_bar DB context manager)的 launchd 实战第五坑
- #91-#94 全部修复通过 ✅
- #95 是 launchd 进程模型的固有限制(Background 不能 fork)而非代码 bug

### Phase 5(25-30 min)1h 观察窗启动决策

**实际现状**:
- ✅ 数字员工 plist 注册 + RunAtLoad=true 一次触发后 exit 0
- ✅ agent + imap-sync 2/3 launchd job 仍 active
- ✅ #94 B 路径修复实战验证
- ⚠️ #95 决定 menu_bar/dashboard 不能作为 launchd 子进程常驻

**1h 观察窗 4 候选**:

| 候选 | 范围 | 锚 | 风险 |
|------|------|----|------|
| **A · 仅 agent + imap-sync** | 2/3 launchd job | agent(imap-sync 触发时验证 wrapper) | 🟢 0 风险 · 0 改动 |
| **B · 数字员工 RunAtLoad 重复** | 多次 launchctl load 验证 launcher 稳定 | launcher 一次性模式 | 🟢 launcher 已验证 |
| **C · 修复 #95 后再 1h** | 改 plist `ProcessType=Interactive` 或拆守护 | 完整数字员工 | 🟡 1 D-step + 1h 内可能不及 |
| **D · A + B 混合** | agent + imap-sync 常驻 + 数字员工 launcher 重复 RunAtLoad 验证稳定性 | 双锚 | 🟢 0 改动 · 完整覆盖 |

**推荐**:D · A + B 混合
- agent + imap-sync 2/3 仍 active
- 数字员工 plist 留注册 · KeepAlive=false · 不会自动重启
- 1h 观察 launchd 状态稳定性
- 等 #95 修复 D-step 后再走 24h 完整观察

---

## ⚠️ 剩余风险(10 项)

### R1 · 撞坑 #95 ProcessType=Background 不允许 fork(本次新暴露)

- **触发**:数字员工 launcher 用 nohup fork menu_bar/dashboard subprocess → launchd 检测到 fork(Background 模式不允许)→ 回收 subprocess
- **现状**:数字员工 launcher 一次性 RunAtLoad 模式可正常工作 · 9/9 预检全过 · menu_bar/dashboard 启动失败但不影响 launcher exit 0
- **风险等级**:🟡 中(数字员工核心功能可达 · UI/HTTP 子进程常驻受限)
- **缓解**:D-step 修复 plist `ProcessType=Interactive` 或拆守护进程
- **4 修复路径候选**:
  - A · docs-only 接受当前 launcher 一次性模式(撞坑 #95 不修)
  - B · 改 plist `ProcessType=Interactive`(需要 GUI session + 用户登入)
  - C · 拆守护进程:launcher fork 但 plist 改成 `RunAtLoad=false + KeepAlive=true` + 用 `nohup setsid` 强 detach
  - D · 不走 launchd,数字员工用 tmux/screen daemon · launchd 只管 agent + imap-sync

### R2 · 撞坑 #90 launchd session-bound 仍未实施

- **现状**:9.5h 内 active → inactive 衰减 · reboot/logout 后失效
- **风险等级**:🟡 中
- **缓解**:D-step 4 候选(re-load runbook / bootstrap / LaunchHelper / zshrc auto-load)

### R3 · docs/v0.2.67 §19-21 误归未校正(沿 v0.2.74.1 R3)

- **现状**:撞坑 #81 真实根因是 #91
- **风险等级**:🟢 低(docs-only 边界外)

### R4 · coverage 89.12%(无变化)

- **风险等级**:🟢 低(<0.1pp 抖动)

### R5 · launcher 内 ⌥⌘N TCC 7/9 警告(沿 v0.2.74.1)

- **现状**:launchd subprocess 视角无 GUI 弹窗
- **风险等级**:🟢 低

### R6 · #93 修复实战验证 100% 命中(沿 v0.2.76)

- **现状**:launchctl load -w 真实验证 + 9/9 预检 + UV_BIN 链路
- **风险等级**:🟢 已解决

### R7 · #94 B 路径修复实战验证 100% 命中(本次)

- **现状**:menu_bar.log DB 反复 open/close 模式 + 无 RuntimeError
- **风险等级**:🟢 已解决

### R8 · #95 ProcessType=Background 不允许 fork(本次 NEW)

- **现状**:数字员工 launcher 一次性 RunAtLoad 模式可工作
- **风险等级**:🟡 中
- **缓解**:D-step 修复

### R9 · menu_bar/dashboard port 8765 未监听(瞬时)

- **现状**:launcher 启动后 ~50s 端口消失
- **风险等级**:🟡 中(用户无法访问 dashboard)
- **缓解**:同 #95 修复

### R10 · docs/v0.2.77 commit 未提交

- **风险等级**:🟢 低

---

## 🚫 禁止动作清单

| 禁止动作 | 红线 | 阻塞原因 |
|---------|------|---------|
| **SMTP 真发** | 撞坑 #59 + #76 + #78 + #85 | 默认未解锁 · 每次需 user 临时授权 |
| **Notes 生产同步** | 撞坑 #65 + #71 + #89 | Notes 真加密未开 |
| **Path4 写入** | 撞坑 #18 | `ENABLE_PATH_4_WRITE` 默认 UNSET |
| **v1.0 tag** | 撞坑 #60 反转 | v1.0 tag 默认不打 |
| **git push origin main** | 红线 · Default no push | 需 user 明确 `PUSH` 或 `push` 词 |
| **`launchctl load -w` 数字员工** | 撞坑 #95 | 已授权 1 次 + 等 #95 D-step 决策 |
| **`ENABLE_PATH_4_WRITE=1`** | 撞坑 #18 | 任何形式都不写 shell profile |
| **`ENABLE_NOTES_ENCRYPTION=1`** | 撞坑 #1 + #65 | 任何形式都不写 shell profile |
| **Outlook / Gmail 配置** | 撞坑 #59 | 不配置 · 不写入 Keychain |

---

## ✅ T3 L4 复验可执行证据清单

```text
# #94 B 路径修复实战验证
launchctl list 3/3 注册(数字员工 exit 0)✅
9/9 预检全过 ✅
err.log 空(无任何错误)✅
out.log 🎉 启动完成 ✅
menu_bar.log DB 反复 open/close 模式(B 路径生效)✅
数字员工 launcher exit 0(从 1 → 0)✅

# #95 NEW 暴露
plist ProcessType=Background 不允许 fork ✅
menu_bar/dashboard 50s 后全退 ✅
state=not running · active count=0 ✅
port 8765 未监听 ✅

# 9/9 质量门
make mypy → Success: 257 source files 0 errors
make lint → Linting: 285 file(s) 0 errors
make check-snapshot → OK (2920/1/285md) · state entry OK

# Git
git rev-parse --short HEAD → 74dc9db
git status --short --branch → ## main...origin/main
git log --oneline -3 → 74dc9db B 路径修复 → c8049ec T3 L4 收口 → e74be55 style
```

---

## 🎯 下一步唯一建议

**唯一选择:1h 观察窗启动**

### 选项 A · 仅 agent + imap-sync 2/3 launchd job 观察(0 改动)

- agent 月报每日 09:00 cron 触发(本 1h 观察窗内可能不触发)
- imap-sync 每日 07:00 cron 触发(本 1h 观察窗内可能不触发)
- 仅观察 launchd 注册表稳定性 + plist 文件不被改动
- 0 风险 · 0 改动

### 选项 B · 数字员工 RunAtLoad 重复验证 launcher 稳定(0 改动)

- 多次 `launchctl load -w` 验证 launcher 一次性 RunAtLoad 模式稳定
- 验证 9/9 预检 + launcher 启动序列一致性
- 0 风险

### 选项 C · 修复 #95 后再 1h(1 D-step + 1h 内可能不及)

- 改 plist `ProcessType=Interactive` 或拆守护
- 1 D-step 实施 + 1h 观察 · 时间紧
- 推荐作为 1h 观察后的下一棒

### 选项 D · A + B 混合(0 改动 · 推荐)

- agent + imap-sync 2/3 active + 数字员工 plist 留注册
- 1h 内每 15 分钟观察一次 launchd 状态稳定性
- 0 改动 · 完整覆盖 · 等 #95 修复后 24h 完整观察

### 推荐路径

按撞坑 #71 docs-only 边界 + 红线严判:**选项 D · A + B 混合**

1. **现在**:选项 D 启动 1h 观察窗(0 改动 · 锚 launchd 注册表稳定性)
2. **1h 后**:撞坑 #95 4 路径决策(A docs-only 接受 / B 改 plist / C 拆守护 / D 不走 launchd)
3. **24h 观察**:撞坑 #95 修复后 + agent + imap-sync 2/3 完整 24h + 数字员工 RunAtLoad 模式稳定
4. v1.0 tag 默认不打 · 待 24h 观察稳定后评估

---

## 📋 红线维持(17 项 + #94 + #95)

| 红线 | 状态 |
|------|------|
| 撞坑 #1 凭据 | ✅ |
| 撞坑 #18 ENABLE_PATH_4_WRITE UNSET | ✅ |
| 撞坑 #59 outlook/gmail 不配 | ✅ |
| 撞坑 #65 NotesCipherImpl.decrypt fallback | ✅ |
| 撞坑 #71 docs-only 边界 | ✅(本轮 docs-only 收口)|
| 撞坑 #76 outbox status 小写 | ✅ |
| 撞坑 #78 real mode count=1 | ✅ |
| 撞坑 #79 redactor email 后半部 | ✅ |
| 撞坑 #81 数字员工 TCC Python 误判 | ✅ 真实根因 #91 → #92 → #93 |
| 撞坑 #85 LLM 草稿幻觉 3 层 | ✅ |
| 撞坑 #86 router 空 token 优雅降级 | ✅ |
| 撞坑 #87 snapshot self-referential 校准 | ✅ |
| 撞坑 #88 spike ↔ src drift | ✅ |
| 撞坑 #89 real-flow-notesync-dedup | ✅ |
| 撞坑 #90 launchd session-bound | ⚠️ 待 D-step 持久化 |
| 撞坑 #91 Documents exec OS 拦截 | ✅ T3 L4 完全实战验证 |
| 撞坑 #92 Documents 沙箱 | ✅ T3 L4 完全实战验证 |
| 撞坑 #93 launchd uv PATH 缺失 | ✅ T3 L4 完全实战验证 |
| 撞坑 #94 menu_bar DB context manager | ✅ B 路径修复实战验证 |
| 撞坑 #95 ProcessType=Background fork | ⚠️ NEW 暴露 · 等 D-step 决策 |
| v1.0 tag 不打 | ✅ |
| 不写 shell profile | ✅ |
| 不自动 launchctl load -w 数字员工 | ✅ |
| 0 SMTP 真发 | ✅ |
| 0 Notes 生产同步 | ✅ |
| 0 ENABLE_PATH_4_WRITE | ✅ |

---

## 全局快照

| 维度 | 状态 |
|------|------|
| HEAD | `74dc9db` |
| ahead/behind | ahead 0 / behind 0 |
| 9/9 质量门 | ✅ 全绿(2920/1/89.12/285/257)|
| snapshot 双门 | ✅ OK |
| launchd 实际 | 2/3 active(agent + imap-sync)· 1/3 注册 bootout(数字员工)|
| launchd 契约测试 | ✅ 47/47 |
| snapshot guardian | ✅ 8/8 |
| runtime wrapper | ✅ 4 部署 |
| runtime plist | ✅ 3 部署 |
| runtime .env | ✅ 2355 bytes(APP_SUPPORT 已迁)|
| runtime log dir | ✅ 6 log 文件就位 |
| 撞坑 #93 实战验证 | ✅ T3 L4 完全通过 |
| 撞坑 #94 B 路径验证 | ✅ T3 L4 复验 完全通过 |
| 撞坑 #95 NEW 暴露 | ⚠️ 待 D-step 决策 |
| 1h 观察窗 | 🟡 **本次启动**(选项 D)|
| v1.0 完成度 | **87%**(不变)|
| 可无人值守 | **82%**(↓ 3pp · #95 限制 menu_bar/dashboard 常驻)|
| 项目整体 | **94%**(不变)|
