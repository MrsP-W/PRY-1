# D5.6.3 spike 严判放宽 @审计员 review(2026-06-30)

> **D-step 命名**:D5.6.3(v0.2.56)
> **承接**:`docs/v0.2.56-d5.6.3-relax-design.md`(设计文档)
> **@审计员**:L4 专属角色(撞坑 #18 风险门控 + 撞坑 #78 设计意图保护)
> **状态**:🟢 **@审计员 review PASS · v0.2.56.1 已实施(2026-06-30)**

---

## 1. review 范围

- 设计文档:`docs/v0.2.56-d5.6.3-relax-design.md`(8 节 + 11 节)
- 代码改动目标:`scripts/spike_send_100.py:382-384`(原严判)
- 新增常量:`_MULTI_CONFIRM_PHRASE = "yes-i-understand-batch-send"`
- 新增 CLI 参数:`--multi-confirm`
- 新增测试:`tests/scripts/test_spike_send_100.py`(10 个 case)

---

## 2. 撞坑 #18 风险门控 review(必须满足)

| 维度 | 严判 | 设计是否满足 | 备注 |
|------|------|------------|------|
| 多重防御 ≥ 9 重 | 撞坑 #18 核心约束 | ✅ 9 重(原 5 + 新 4) | 详见设计 §5 |
| 真实邮件发出去 = 公开 = 不可撤回 | 撞坑 #18 顶层原则 | ✅ 多重防御 + 显式 confirm | `--multi-confirm` 二次确认不可绕过 |
| `--max-recipients 1` 不动 | 撞坑 #18 防 spam | ✅ 不动 | 仍严判 max_recipients != 1 |
| 收件人 spam 风险评估 | 撞坑 #18 R3 | ⚠️ 10 封一次性发 QQ 反垃圾可能拒收 | A3 readiness docs 必须包含 10 封一次性发风险 |
| Keychain round-trip 16 chars | 撞坑 #16/#17 | ✅ 不动 | 沿用 spike_set_smtp_password.py --check |
| `--recipient` 白名单 | 撞坑 #18 防 spam | ✅ 不动 | 自发自收 477753009@qq.com |
| placeholder 严判 | D5.6.2 P0 凭证 | ✅ 不动 | `--smtp-host` / `--smtp-username` 仍拒绝占位 |

**撞坑 #18 风险门控 review**:✅ **PASS**(9 重防御 + 显式 confirm + 严判不放松)

---

## 3. 撞坑 #78 设计意图 review

### 3.1 原设计意图(L382 注释)

```python
# 4. D5.6.2 检查员反馈:--real 模式强制 count == 1(防止"我以为是 1 封但实际 10")
```

### 3.2 D5.6.3 是否保留设计意图

| 原意图 | D5.6.3 处理 | 是否保留 |
|-------|-----------|---------|
| 防止"我以为是 1 封但实际 10" | `--count > 1` 必传 `--multi-confirm "yes-i-understand-batch-send"` | ✅ **保留**(更显式) |
| 强制 count 必须 == 1 | `count 1-10` + 二次 confirm | ⚠️ **放宽到 1-10**,但保留"必须显式确认"原则 |
| 严判无歧义 | ValueError 严判 + 明确 message | ✅ **保留** |

**撞坑 #78 设计意图 review**:✅ **PASS**(从"count 必传 1"升级到"count 1-10 + 二次 confirm",核心原则"必须显式确认"保留)

---

## 4. 严判边界 review

### 4.1 上界严判

```python
if count < 1 or count > _REAL_MODE_MAX_COUNT:  # _REAL_MODE_MAX_COUNT=10
    raise ValueError(...)
```

- ✅ `_REAL_MODE_MAX_COUNT=10` 是 hard upper bound(L78 既有常量)
- ✅ 任何代码改动不能放宽到 11+(撞坑 #78 #B3 防放宽)
- ✅ 上界严判精确,不允许 count=11

### 4.2 下界严判

```python
if count < 1 or count > _REAL_MODE_MAX_COUNT:
    raise ValueError(...)
```

- ✅ count=0 拒绝(下界)
- ✅ count=-1 拒绝(下界负数)
- ✅ count=1 通过(下界通过)

### 4.3 多重 confirm 严判

```python
if count > 1 and multi_confirm != _MULTI_CONFIRM_PHRASE:
    raise ValueError(...)
```

- ✅ count=1 + 无 multi_confirm → 通过(向后兼容)
- ✅ count=5 + 无 multi_confirm → ValueError
- ✅ count=5 + multi_confirm="wrong" → ValueError
- ✅ count=5 + multi_confirm="yes-i-understand-batch-send" → 通过

**严判边界 review**:✅ **PASS**(上下界精确 + 多重 confirm 严判)

---

## 5. 沿用边界 review

| 维度 | 是否沿用 | 备注 |
|------|---------|------|
| 5 重防误发门控不动 | ✅ | SMTP_REAL_NETWORK=1 / --max-recipients 1 / --confirm / Keychain / 占位 |
| `_REAL_MODE_MAX_COUNT=10` 沿用 | ✅ | L78 既有常量 |
| Keychain 流程不动 | ✅ | spike_set_smtp_password.py --check |
| placeholder 严判不动 | ✅ | smtp_host / smtp_username |
| InMemory 模式 --count 规则不动 | ✅ | _INMEMORY_MAX_COUNT |
| `--max-recipients 1` 不动 | ✅ | 防 spam |
| `--confirm` 严判不动 | ✅ | 向后兼容 count=1 |
| 不打 tag / 不动 v0.1.0 | ✅ | 不动 |

**沿用边界 review**:✅ **PASS**(所有不动的边界都明确)

---

## 6. pytest 测试覆盖 review

| # | 测试 | 覆盖场景 | 期望 |
|---|------|---------|------|
| 1 | test_real_mode_count_1_no_multi_confirm_passes | 向后兼容 | ✅ 通过 |
| 2 | test_real_mode_count_5_no_multi_confirm_raises | 二次 confirm 缺失 | ✅ ValueError |
| 3 | test_real_mode_count_5_with_multi_confirm_passes | 显式 confirm | ✅ 通过 |
| 4 | test_real_mode_count_10_with_multi_confirm_passes | 上界 | ✅ 通过 |
| 5 | test_real_mode_count_11_raises | 超上界 | ✅ ValueError |
| 6 | test_real_mode_count_0_raises | 下界 0 | ✅ ValueError |
| 7 | test_real_mode_count_negative_raises | 下界负数 | ✅ ValueError |
| 8 | test_real_mode_multi_confirm_invalid_raises | confirm 错值 | ✅ ValueError |
| 9 | test_inmemory_mode_count_unaffected | InMemory 模式不动 | ✅ 通过 |
| 10 | test_real_mode_existing_gates_still_pass | 5 重门控不动 | ✅ 通过 |

**测试覆盖 review**:✅ **PASS**(10 个 case 覆盖上下界 + 多重 confirm + 沿用边界)

---

## 7. @审计员 review checklist(全部勾选)

- [x] `--multi-confirm` 二次确认是不可绕过的
- [x] `_REAL_MODE_MAX_COUNT=10` 是 hard upper bound,任何代码改动不能放宽到 11+
- [x] InMemory 模式 `--count` 严判规则不变(沿 _INMEMORY_MAX_COUNT)
- [x] 5 重防误发门控都不动
- [x] Keychain round-trip 16 chars 命中沿用
- [x] `--recipient` 白名单沿用
- [x] placeholder 严判沿用
- [x] pytest 测试覆盖 ≥ 10 个 case
- [x] 撞坑 #18 多重防御 ≥ 9 重
- [x] 撞坑 #78 设计意图(必须显式确认)保留

---

## 8. @审计员 review 决策

🟢 **PASS — 设计满足所有约束,可进入实施阶段**

### 8.1 决策要点

1. ✅ 撞坑 #18 多重防御 = 9 重(原 5 + 新 4),核心约束满足
2. ✅ 撞坑 #78 设计意图 = "必须显式确认"原则保留(升级到二次 confirm)
3. ✅ 严判边界 = 上界 10 / 下界 1 / 多重 confirm
4. ✅ 沿用边界 = 5 重门控 / Keychain / placeholder / --max-recipients / InMemory 都不动
5. ✅ pytest 测试 = 10 个 case 覆盖关键路径

### 8.2 A3 readiness docs 必须包含

- ⚠️ `--multi-confirm "yes-i-understand-batch-send"` 必传(若 --count > 1)
- ⚠️ `--count` 上界 10,下界 1
- ⚠️ 一次性发 10 封 = QQ 反垃圾可能拒收,需用户决策收件人策略
- ⚠️ 撞坑 #78 #B3 防放宽:任何代码改动不能放宽 _REAL_MODE_MAX_COUNT

### 8.3 实施阶段必须

- 修改 `scripts/spike_send_100.py:382-394`
- 新增 `_MULTI_CONFIRM_PHRASE = "yes-i-understand-batch-send"`(L77 附近)
- 新增 `--multi-confirm` CLI 参数(L34 附近)
- 新增 `tests/scripts/test_spike_send_100.py`(10 个 case)
- 跑 9/9 质量门

---

## 9. 下一棒

- **本期**:@审计员 review PASS · 等用户最终授权
- **下一步**:用户授权后 → 实施 `spike_send_100.py` 修改 + 加 pytest 测试 → 9/9 质量门 → 收口报告 → commit

---

**@审计员**:MiniMax-M3 扮演 · **最后更新**:2026-06-30