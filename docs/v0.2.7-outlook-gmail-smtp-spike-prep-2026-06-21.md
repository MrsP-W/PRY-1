# v0.2.7 outlook/gmail SMTP 真实发送 spike 准备(端午不休息第 4 天 · 6/21 周日)

> **项目**:我的AI员工
> **文档时间**:2026-06-21 端午连休第 4 天(周日)
> **承接**:[[v0.2.6-d4.7.4-v1.0.3-deferred-2026-06-20]] §"下一棒" 候选 2 + [[v0.2.5-smtp-real-send-preflight-2026-06-18]] §"启动条件 checklist 6 项" + [[v0.2.4-drift-review-mechanism-2026-06-18]] 撞坑恢复 3 步实战演练 2 + [[v0.2.2-p8-smtp-provider-factory-2026-06-18]] SMTPProviderFactory + [[v0.2.2-p5-oauth-xoauth2-2026-06-18]] XOAUTH2
> **范围**:**outlook/gmail SMTP 真实发送 spike 准备 docs-only** — 1 commit · docs/v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21.md 新建 · SESSION-STATE/MODIFICATION-LOG/README 同步 · **不真发邮件**(只做启动条件 checklist + 风险门控 + 启动命令范本)
> **撞坑恢复 3 步实战演练**:本报告作为 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本的**实战演练 2**(演练 1 = v0.2.5 SMTP preflight docs-only)
> **维护者**:Mr-PRY · **模型**:MiniMax-M3

---

## 🎯 文档目标

沿 [[v0.2.5-smtp-real-send-preflight-2026-06-18]] §"启动条件 checklist 6 项" + §"阻塞点 3 项",把 outlook/gmail SMTP 真实发送 spike **启动前 6 项 checklist 完整化 + 风险门控文档化 + 启动命令范本沉淀**,作为 6/23+ 全链路重启时 outlook/gmail 真实 SMTP 发送 spike 的**只读启动手册**。

**关键约束**(沿用户 2026-06-21 决策):
- ❌ **不真发邮件**(本报告纯 docs-only,只做预演 + 准备)
- ❌ **不移动 `v0.1.0` tag**(继续锚定 `2af775f`,沿 D5.7.2 范本)
- ❌ **不真实 OAuth flow**(等用户授权 + 凭据)
- ⏸️ **outlook/gmail 凭证写入脚本**(B 类延后,等用户单独决策白名单扩展)

## 📋 启动条件 checklist(6 项)

### Checklist 1:用户明确授权

| 触发条件 | 当前状态 | 解封条件 |
|---------|---------|---------|
| 用户明确说"今天跑 1 封真实 outlook/gmail SMTP spike" | ⏸️ 待用户授权 | 用户口头授权 + 明确 provider(outlook or gmail) |

**范本**(沿 [[d5.6.5-real-send]] 用户授权):
> "今天我授权跑 1 封真实 outlook SMTP 发送 spike,收件人=我自己邮箱,只测链路"

### Checklist 2:凭据就绪(按 provider 二选一)

#### Outlook SMTP 凭据(2 选 1)

**方案 A:App Password(2FA 关闭时)**
```
SMTP host: smtp.office365.com
SMTP port: 587
用户名:   <outlook邮箱>  e.g. xxx@outlook.com
密码:     <App Password>  从 Microsoft Account → Security → App passwords 生成
```

**方案 B:OAuth 2.0(v0.2.2 #5 commit 2 + commit 4 链路)**
```
client_id:      <Microsoft Azure App client_id>
client_secret:  <Microsoft Azure App client_secret>
tenant_id:      <common> 或具体 tenant_id
flow:           跑 OAuth authorization code flow 拿 access_token + refresh_token
refresh_token:  持久化到 Keychain(SERVICE_MICROSOFT_OAUTH)
```

#### Gmail SMTP 凭据(必须 OAuth 2.0)

```
SMTP host:    smtp.gmail.com
SMTP port:    587
用户名:      <gmail邮箱>  e.g. xxx@gmail.com
client_id:    <Google Cloud Console OAuth client_id>
client_secret:<Google Cloud Console OAuth client_secret>
flow:         跑 OAuth authorization code flow 拿 access_token + refresh_token
refresh_token:持久化到 Keychain(SERVICE_GOOGLE_OAUTH)
```

**OAuth 实现就绪状态**(沿 [[v0.2.2-p5-oauth-xoauth2-2026-06-18]]):
- ✅ `MicrosoftOAuth2Provider` 实化(v0.2.2 #5 commit 2 · `c0f83d4` · 12 tests)
- ✅ `GoogleOAuth2Provider` 实化(v0.2.2 #5 commit 3 · `564b8db` · 11 tests)
- ✅ `XOAUTH2 SMTP 鉴权集成` 实化(v0.2.2 #5 commit 4 · `9966ad0` · 12 tests)
- ✅ 依赖加锁(v0.2.2 #5 commit 5 · `6a0549e` · msal + google-auth + google-auth-oauthlib)
- ⏸️ **真实 OAuth flow 未跑**(需要用户手动跑 authorization code flow 拿 token)

### Checklist 3:凭据写入 Keychain(provider-aware CLI 已就绪)

**CLI 命令**(沿 [[v0.2.2-p8-smtp-provider-factory-2026-06-18]] §"provider-aware Keychain"):

```bash
# Outlook App Password 方式
uv run python scripts/spike_set_smtp_password.py \
  --provider outlook \
  --email <outlook邮箱> \
  --set-password <App Password>

# Outlook OAuth 方式(沿 v0.2.2 #5 commit 4)
uv run python scripts/spike_set_smtp_password.py \
  --provider outlook \
  --email <outlook邮箱> \
  --set-oauth-token <access_token>  # 暂未实现,B 类延后

# Gmail OAuth 方式(沿 v0.2.2 #5 commit 4)
uv run python scripts/spike_set_smtp_password.py \
  --provider gmail \
  --email <gmail邮箱> \
  --set-oauth-token <access_token>  # 暂未实现,B 类延后
```

**Keychain 核对**:
- ✅ `set_smtp_password_for_provider(provider, email, password)` 实化(v0.2.2 #8)
- ✅ `delete_smtp_password_for_provider(provider, email)` 实化(v0.2.2 #8)
- ⏸️ `--set-oauth-token` 参数未实现(B 类延后,等用户决策白名单扩展)

### Checklist 4:provider 白名单扩展(B 类决策)

**当前状态**(沿 [[d5.6.3-p1-1-5-changes]] D5.6.3 P2-3):
```python
# scripts/spike_send_100.py:1004-1013
choices=["qq"],  # 当前 D5 阶段仅 'qq'(outlook/gmail 凭证写入脚本未实现,B 类延后)
```

**解封条件**(沿 [[b-class-deferral-2026-06-09]] §"解封条件"):
- 用户明确说"今天扩展 provider 白名单到 outlook/gmail"
- 或用户单独决策"先把 outlook/gmail SMTP 真实 spike 跑通"

**B 类决策延后声明应用**:
- ✅ 不主动提醒 B 类
- ✅ 不在检查报告再次列出
- ✅ 沿 v0.2.6 D4.7.4 v1.0.3 改进项延后实化的"用户主动启动 = 自动解封"范本

### Checklist 5:OutboxDispatcher 与 SMTPProviderFactory 集成(可选,1 commit docs-only 不涉及)

**当前状态**(沿 [[v0.2.5-smtp-real-send-preflight-2026-06-18]] §"阻塞点 3"):
- v0.2.2 #8 SMTPProviderFactory 工厂模式实化后,OutboxDispatcher **未集成**
- 当前 spike_send_100.py 沿 `smtp_provider` 参数调用,但 OutboxDispatcher.run_once 仍用 `smtp_host` / `smtp_port` 直连(无 factory 调度)

**集成方案**(沿 v0.2.2 #8 SMTPProviderFactory 设计):

```python
# src/my_ai_employee/dispatcher/outbox_dispatcher.py
async def run_once(self, *, smtp_provider: str, smtp_username: str, ...):
    connector = SMTPProviderFactory.create(
        provider=smtp_provider,
        email=smtp_username,
        transport=None,  # 用缺省的 SmtpLibTransport
    )
    await connector.connect(...)
    # ... 沿 D5.6.5 4 重防误发
```

**集成范围**(建议 1 commit docs-only 不涉及):
- 1 commit:`OutboxDispatcher.run_once` 加 `smtp_provider` 参数 + SMTPProviderFactory.create() 调度
- 测试:OutboxDispatcher.test_run_once_with_outlook_provider + test_run_once_with_gmail_provider
- 风险:OutboxDispatcher 是 D5 业务调度器核心,改动需 8/8 质量门全绿 + 真实 spike 验证

**本报告 docs-only 不实施**,留作 v0.2.7+ 候选。

### Checklist 6:沿 [[d5.6.5-real-send]] 4 重防误发

| # | 门控 | 实现位置 | 当前状态 |
|---|------|---------|---------|
| 1 | `SMTP_REAL_NETWORK=1` env 门控 | `scripts/spike_send_100.py:81-84, 353-363` | ✅ 就绪 |
| 2 | 强制 1 收件人(`--max-recipients 1`) | `scripts/spike_send_100.py --help` | ✅ 就绪 |
| 3 | 二次确认口令(`--confirm yes-i-understand-this-sends-real-email`) | `scripts/spike_send_100.py --help` | ✅ 就绪 |
| 4 | Keychain round-trip 校验 | `scripts/spike_send_100.py:401-413` | ✅ 就绪(provider-aware 已支持 outlook/gmail) |

## 🛡️ 风险门控(5 重防误发 = 4 重 + provider 白名单扩展)

### 风险门控 1:**SMTP_REAL_NETWORK=1 env 门控**

**关键代码**(`scripts/spike_send_100.py:81-84, 353-363`):
```python
_SMTP_REAL_NETWORK_ENV: str = "SMTP_REAL_NETWORK"
_SMTP_REAL_NETWORK_VALUE: str = "1"

# 默认 os.environ.get("SMTP_REAL_NETWORK") != "1" → 抛错,绝不真连
if os.environ.get(_SMTP_REAL_NETWORK_ENV) != _SMTP_REAL_NETWORK_VALUE:
    raise RuntimeError(
        f"必须显式设置环境变量 {_SMTP_REAL_NETWORK_ENV}={_SMTP_REAL_NETWORK_VALUE} 才允许真发。"
    )
```

**核对结论**:✅ **SMTP_REAL_NETWORK 门控就绪**,默认安全。

### 风险门控 2:**provider 白名单(B 类决策,需用户授权)**

**当前**:choices=["qq"]
**目标**:choices=["qq", "outlook", "gmail"] (B 类解封后)

**核对结论**:⏸️ **当前阻塞**,需用户明确授权扩展白名单。

### 风险门控 3:**Keychain round-trip(provider-aware)**

**关键代码**(`scripts/spike_send_100.py:401-413`):
```python
smtp_password_result = keychain.get_smtp_password_for_provider(smtp_provider, smtp_username)
if smtp_password_result is None:
    raise RuntimeError(f"D5.6.2 凭证链路:从 Keychain 读 {smtp_provider}/{smtp_username} 失败")
if smtp_password_result.strip() == "":
    raise RuntimeError(f"D5.6.2 凭证链路:Keychain 读出空密码")
```

**核对结论**:✅ **Keychain round-trip provider-aware 就绪**(v0.2.2 #8 实化)。

### 风险门控 4:**强制 1 收件人(--max-recipients 1)**

**核对结论**:✅ **强制 1 收件人就绪**,REAL 模式必传 1。

### 风险门控 5:**二次确认口令(--confirm)**

**核对结论**:✅ **确认口令人工输入就绪**:`yes-i-understand-this-sends-real-email`。

## 🚀 启动命令范本(3 个)

### 范本 1:Outlook SMTP 真实发送 1 封(App Password 方式)

```bash
export SMTP_REAL_NETWORK=1

# 写 Outlook App Password 到 Keychain(只需 1 次)
uv run python scripts/spike_set_smtp_password.py \
  --provider outlook \
  --email <outlook邮箱> \
  --set-password <App Password>

# 真实 Outlook SMTP 发送 1 封
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.office365.com \
  --smtp-port 587 \
  --smtp-username <outlook邮箱> \
  --smtp-provider outlook \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1
```

**前置条件**:
- ✅ Checklist 1 用户授权
- ✅ Checklist 2 凭据就绪(App Password)
- ✅ Checklist 3 凭据写入 Keychain
- ✅ Checklist 4 provider 白名单扩展(B 类决策)
- ✅ Checklist 6 4 重防误发门控

### 范本 2:Outlook SMTP 真实发送 1 封(OAuth 方式)

```bash
export SMTP_REAL_NETWORK=1

# 1. 跑 Microsoft OAuth authorization code flow 拿 token(沿 v0.2.2 #5 commit 2)
uv run python scripts/oauth_flow_microsoft.py \
  --client-id <Azure client_id> \
  --client-secret <Azure client_secret> \
  --email <outlook邮箱> \
  --scopes "offline_access https://outlook.office.com/SMTP.Send"

# 2. 真实 Outlook SMTP 发送 1 封(OAuth 自动刷新 token)
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.office365.com \
  --smtp-port 587 \
  --smtp-username <outlook邮箱> \
  --smtp-provider outlook \
  --auth-method oauth \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1
```

**前置条件**(同范本 1,但 Checklist 2 用 OAuth)+ `scripts/oauth_flow_microsoft.py` 实化(当前未实化,B 类延后)

### 范本 3:Gmail SMTP 真实发送 1 封(OAuth 方式,必 OAuth)

```bash
export SMTP_REAL_NETWORK=1

# 1. 跑 Google OAuth authorization code flow 拿 token(沿 v0.2.2 #5 commit 3)
uv run python scripts/oauth_flow_google.py \
  --client-id <Google client_id> \
  --client-secret <Google client_secret> \
  --email <gmail邮箱> \
  --scopes "https://mail.google.com/"

# 2. 真实 Gmail SMTP 发送 1 封(OAuth 自动刷新 token)
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.gmail.com \
  --smtp-port 587 \
  --smtp-username <gmail邮箱> \
  --smtp-provider gmail \
  --auth-method oauth \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1
```

**前置条件**(同范本 2,但 provider=gmail)+ `scripts/oauth_flow_google.py` 实化(当前未实化,B 类延后)

## 🔄 撞坑恢复 3 步实战演练 2(沿 v0.2.4 机制 3)

本报告作为 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本的**实战演练 2**(演练 1 = [[v0.2.5-smtp-real-send-preflight-2026-06-18]] SMTP preflight docs-only)。

### 演练 2:本任务的撞坑恢复 3 步

#### 步骤 1:撤回自己基于旧状态写的 docs-only 报告

- **本任务**:**不需要撤回**(因为本任务是首次执行 outlook/gmail SMTP spike 准备,无旧状态报告)
- **触发条件**:未来撞同款问题(并发进程冲突 outlook/gmail SMTP spike 实化撞坑)时,撤回自己写的 stale 报告

#### 步骤 2:验证并发进程工作通过 8/8 质量门

- **本任务**:**不需要验证**(因为没有并发进程)
- **8/8 质量门 baseline 验证**(本 preflight docs-only 不涉及代码):
  - ✅ ruff check:All checks passed
  - ✅ ruff format:已 formatted
  - ✅ mypy src:Success 101 files 0 errors
  - ✅ alembic --sql:exit 0
  - ✅ pytest:2225 passed / 1 skipped / 88.85% coverage(沿 v0.2.6 收口锚定)
  - ✅ uv build:success
  - ✅ MD lint:0 errors / 103 files
  - ✅ coverage:88.85% ≥ 80%

#### 步骤 3:用户授权代 commit 2 commits(feat + docs closure)

- **本任务**:**不需要**(因为本任务是 docs-only,沿 v0.2 launch plan 整体收口范本)
- **commit 范本**:`docs(sync): v0.2.7 outlook/gmail SMTP 真实发送 spike 准备 docs-only`

### 撞坑恢复 3 步应用次数统计

- **撞坑恢复 3 步实际应用次数**:**0 次**(本会话无撞坑)
- **撞坑史累计次数**:1 次(v0.2.2 #8 SMTPProviderFactory 撞坑恢复,2026-06-18 22:00)
- **撞坑恢复范本累计**:2 个(演练 1 = v0.2.5 preflight + 演练 2 = v0.2.7 outlook/gmail spike 准备)
- **7/1 月度复盘**:撞坑恢复范本应用情况统计(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §4 复盘 5 项)

## 🚦 outlook/gmail SMTP 真实 spike 启动流程(6/23+)

### 阶段 1:用户授权 + 凭据就绪

```
用户: "今天跑 1 封真实 outlook/gmail SMTP 发送 spike"
       ↓
    + 提供凭据(App Password 或 OAuth client_id/secret)
       ↓
    + 明确 provider(outlook or gmail)
```

### 阶段 2:provider 白名单扩展(B 类决策,1 commit docs-only)

```
用户授权扩展白名单
       ↓
[1 commit]:choices=["qq"] → choices=["qq", "outlook", "gmail"]
       ↓
8/8 质量门验证
       ↓
docs closure commit
```

### 阶段 3:OAuth flow 实化(可选,1-2 commits docs-only)

```
用户决策 OAuth flow 方案
       ↓
[1-2 commits]:scripts/oauth_flow_microsoft.py + scripts/oauth_flow_google.py
       ↓
8/8 质量门验证
       ↓
docs closure commit
```

### 阶段 4:OutboxDispatcher 集成(可选,1 commit docs-only)

```
用户决策 OutboxDispatcher 集成范围
       ↓
[1 commit]:OutboxDispatcher.run_once 加 smtp_provider 参数
       ↓
8/8 质量门验证
       ↓
docs closure commit
```

### 阶段 5:真实 spike 跑通(实战)

```
SMTP_REAL_NETWORK=1 + 5 重防误发门控
       ↓
跑范本 1/2/3 命令
       ↓
1 封真实发送
       ↓
状态机 4 步全过(PENDING_SEND → APPROVED → SENDING → SENT)
       ↓
docs 收口报告(spike 跑通报告)
```

## 🔗 关键引用(10 项)

- **本报告**: `我的AI员工/docs/v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21.md`(outlook/gmail SMTP 真实发送 spike 准备)
- **v0.2.6 D4.7.4 v1.0.3 改进项延后**: `我的AI员工/docs/v0.2.6-d4.7.4-v1.0.3-deferred-2026-06-20.md`(本报告承接 §"下一棒" 候选 2)
- **v0.2.5 SMTP 真实发送 spike preflight**: `我的AI员工/docs/v0.2.5-smtp-real-send-preflight-2026-06-18.md`(本报告承接 §"启动条件 checklist 6 项")
- **v0.2.4 状态漂移审查机制**: `我的AI员工/docs/v0.2.4-drift-review-mechanism-2026-06-18.md`(`机制 3` 撞坑恢复 3 步范本 · 实战演练 2 锚定)
- **v0.2.2 #8 SMTPProviderFactory 收口**: `我的AI员工/reports/v0.2.2-p8-smtp-provider-factory-2026-06-18.md`(工厂模式 + provider-aware Keychain)
- **v0.2.2 #5 XOAUTH2**: `我的AI员工/reports/v0.2.2-p5-oauth-xoauth2-2026-06-18.md`(RFC 7628 + 4 重防误发 + 12 tests)
- **d5.6.5 真实 1 封 SMTP 范本**: `Agent Assistant/memory/d5.6.5-real-send.md`(smtp.qq.com:465 SSL 真实 1 封 · sent=1/1.27s)
- **d5.6.5.1-fixes**: `Agent Assistant/memory/d5.6.5.1-fixes.md`(检查员驳回 5 缺陷全部修复)
- **d5.6.3 p1-1-5 changes**: `Agent Assistant/memory/d5.6.3-p1-1-5-changes.md`(P2-3 修复:provider 白名单限制)
- **B 类决策延后**: `Agent Assistant/memory/b-class-deferral-2026-06-09.md`(provider 白名单扩展需用户单独决策)

## 📌 下一棒

### 端午连休(6/22 周一 · 本端午最后一天)

- **6/22 周一**:v0.2 release notes 收口 + v0.2.1 release tag 锚定策略同步
  - 整理 v0.2.1-v0.2.7 的真实落地链(57+ 主项目 commits)→ 发布说明
  - 复审 v0.2.1 release tag 锚定策略(沿 D5.7.2 范本 + 8/1 锚定策略)
  - docs-only 收口(1 commit,沿 v0.2.5/v0.2.6 范本)

### 全链路重启(6/23+)

- **6/23 周二**:W3 真账单 spike 启动(等用户提供真实微信/支付宝 CSV)→ 沿 D6.6 4 重防误发范本
- **6/23+**:**outlook/gmail SMTP 真实发送 spike 启动**(沿本报告 §"启动流程 5 阶段")
  - 阶段 1:用户授权 + 凭据就绪
  - 阶段 2:provider 白名单扩展(B 类决策)
  - 阶段 3:OAuth flow 实化(可选)
  - 阶段 4:OutboxDispatcher 集成(可选)
  - 阶段 5:真实 spike 跑通(实战)
- **7/1 月度复盘**(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §4 复盘 5 项):
  - 跑状态漂移审查(机制 1 + 2)
  - 跑并发进程冲突审查(机制 4)
  - 评估 B 类延后清单(outlook/gmail provider 白名单 + W3 真账单 + NoteStore OAuth 集成等)
  - 复审 v0.2.1 release tag 锚定策略
  - 撞坑恢复范本应用情况统计(本报告作"实战演练 2")
- **8/1**:v0.2.1 release tag 锚定(W3 真账单 spike + outlook/gmail 真实 SMTP 发送 spike 跑通后)

## 📌 完成定义(本报告 Done)

- [x] 6 项启动条件 checklist(用户授权 + 凭据就绪 + Keychain 写入 + provider 白名单扩展 + OutboxDispatcher 集成 + 4 重防误发)
- [x] 5 重防误发风险门控核对(SMTP_REAL_NETWORK + provider 白名单 + Keychain round-trip + 强制 1 收件人 + 确认短语)
- [x] 3 个启动命令范本(Outlook App Password + Outlook OAuth + Gmail OAuth)
- [x] outlook/gmail SMTP 真实 spike 启动流程 5 阶段(用户授权 + 白名单扩展 + OAuth flow + OutboxDispatcher + 真实 spike)
- [x] 撞坑恢复 3 步实战演练 2 沉淀(本任务无撞坑,沿 [[v0.2.4]] §3 机制 3 范本)
- [x] 撞坑恢复范本累计 2 个(演练 1 = v0.2.5 preflight + 演练 2 = v0.2.7 outlook/gmail spike 准备)
- [x] 10 项关键引用(v0.2.6 D4.7.4 改进项 + v0.2.5 preflight + v0.2.4 drift review + v0.2.2 #5/#8 收口 + d5.6.5 范本 + B 类延后声明)
- [x] 端午最后一天路径 + 全链路重启路径 + 7/1 月度复盘路径 + 8/1 release tag 锚定策略
- [x] **不真发邮件**(只做启动条件 checklist + 风险门控 + 启动命令范本)
- [x] **不移动 `v0.1.0` tag**(继续锚定 `2af775f`,沿 D5.7.2 范本)
- [x] **不真实 OAuth flow**(等用户授权 + 凭据)

## 维护者

**Mr-PRY** · 2026-06-21 v0.2.7 outlook/gmail SMTP 真实发送 spike 准备 docs-only(撞坑恢复 3 步实战演练 2)
**模型**:MiniMax-M3
**项目**:我的AI员工(v0.2.7 outlook/gmail SMTP 真实发送 spike 准备 docs-only · 端午不休息第 4 天)
**关键决策**:**只做启动条件 checklist + 风险门控 + 启动命令范本**(沿用户结论 §1-3);6 项 checklist 完整化 + 5 重风险门控文档化 + 3 个启动命令范本 + 启动流程 5 阶段 + 撞坑恢复实战演练 2
**沿用范本**:[[v0.2.6-d4.7.4-v1.0.3-deferred-2026-06-20]] / [[v0.2.5-smtp-real-send-preflight-2026-06-18]] / [[v0.2.4-drift-review-mechanism-2026-06-18]] / [[v0.2.2-p8-smtp-provider-factory-2026-06-18]] / [[v0.2.2-p5-oauth-xoauth2-2026-06-18]] / [[d5.6.5-real-send]] / [[d5.6.5.1-fixes]] / [[d5.6.3-p1-1-5-changes]] / [[b-class-deferral-2026-06-09]]

**下一棒**:6/22 周一 v0.2 release notes 收口 + v0.2.1 release tag 锚定策略同步