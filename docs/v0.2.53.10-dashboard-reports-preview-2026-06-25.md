# v0.2.53.10 Dashboard 报告预览 + 搜索(2026-06-25)

> **状态**:✅ 已落地 · **边界**:只读 · 8KB 截断预览 · 路径白名单 · 无 DB

## 结论

承接 v0.2.53.9 报告清单,新增 `GET /api/reports/preview?path=...` 与 HTML 搜索/点击预览;spike 类型 filter 时提示详情模式。

## 新增端点

| 路径 | 说明 |
|------|------|
| `GET /api/reports/preview?path=docs/foo.md` | 单份报告截断预览(8KB) |

路径严判:仅 `docs/` / `reports/` / `output/` 下 `.md` / `.json`,禁止 `..` 穿越。

## HTML

- 搜索框(标题/路径)
- 点击列表项 → 预览面板
- spike filter 时显示详情提示
- API 离线时预览提示启动 `make dashboard-api`

## 下一棒

- ApprovalGate 写操作
- outlook/gmail Keychain SMTP spike
