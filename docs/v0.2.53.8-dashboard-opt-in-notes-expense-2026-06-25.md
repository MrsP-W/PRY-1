# v0.2.53.8 — Dashboard opt-in 真实 Notes + Expense(2026-06-25)

> **状态**:✅ 已落地 · **边界**:只读查询 · 同一 `DASHBOARD_REAL_DB=1` env · 默认 Stub · 不真发 SMTP · 不写 DB。

## 结论

沿 v0.2.53.7 `#65 opt-in 4 阶段范本`,`DashboardContext.default()` 在 `DASHBOARD_REAL_DB=1` 时继续注入:

- `NoteConfirmServiceImpl(NoteStore(session_factory))`
- `ExpenseServiceImpl`(NoteStore + TransactionStore + MerchantProfileStore + RuleBasedAnomalyDetector)

三项服务共享一次 `Database.open()` / session_factory;单项失败静默降级 Stub,不阻塞其余服务。

## 行为对比

| 服务 | 默认(无 env) | opt-in 成功 | opt-in 失败 |
|------|-------------|------------|------------|
| outbox | Stub | Impl | Stub |
| note_confirm | Stub | Impl | Stub |
| expense | Stub | Impl | Stub |

## 使用方式

```bash
DASHBOARD_REAL_DB=1 make dashboard-api
open docs/ui/codex-style-dashboard.html
```

## 下一棒

- `/api/reports` 只读端点 + HTML 报告页
- ApprovalGate 写操作(后续)
