# v0.2.53.4 P2 — Dashboard 只读 API 扩展(2026-06-25)

> **状态**:✅ 已落地 · **边界**:只读 GET · 127.0.0.1 · Stub 数据 · 无写操作

## 新增端点

| 路径 | 说明 | 数据源 |
|------|------|--------|
| `GET /api/outbox?limit=10` | 邮件草稿队列 | `OutboxDraftService.list_pending_drafts` |
| `GET /api/notes/pending?limit=10` | Notes 待确认 | `NoteConfirmService.list_pending_confirm` |
| `GET /api/finance/anomalies?limit=10` | 财务异常 | `ExpenseService.get_recent_anomalies` |

`limit` 默认 10,范围 1–100。

## 响应形状

```json
{ "read_only": true, "count": 2, "items": [ ... ] }
```

## 下一棒

- `OutboxDraftServiceImpl` 接 `OutboxStore.by_status`
- 写操作仍留 ApprovalGate 后续
