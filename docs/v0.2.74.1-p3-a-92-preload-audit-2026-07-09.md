# v0.2.74.1 — P3-A #92 真实 load 复验前检查报告(2026-07-09,1h 只读巡检)

> **目的**:数字员工真实 `launchctl load -w com.myaiemployee.digital-employee.plist` 复验前的状态快照。所有检查均为只读,不执行 load、不发送、不写入、不 push、不打 tag。

---

## 🎯 一句话结论

| 维度 | 状态 | 备注 |
|------|------|------|
| 9/9 质量门 | ✅ 全绿 | 2913 passed / 1 skipped / 89.10% / 278 MD / mypy 256 |
| check-snapshot 双门 | ✅ OK | snapshot 与实测对齐 |
| snapshot guardian 测试 | ✅ 8/8 PASSED | `tests/test_quality_snapshot.py` |
| launchd 契约测试 | ✅ 43/43 PASSED | `tests/scripts/test_launchd_install.py` |
| runtime 路径迁移 | ✅ 完整 | wrapper + runner + .env + log dir 全部对齐 `~/Library/Application Support/MyAIEmployee/` |
| launchd 实际状态 | 🟡 2/3 active + 数字员工 bootout | 待 T3 L3 user 授权 load 复验 |
| git push | 🟡 ahead 2(未 push) | 5c0c7be + 47cfe89 等 user 授权 |

**下一步唯一建议**:是否进入"数字员工真实 load 复验授权窗口"。若 user 授权 → 执行 `launchctl load -w com.myaiemployee.digital-employee.plist` → 预期 `err log` 无 `Operation not permitted` + 9/9 预检 OK + 菜单栏启动成功。若不授权 → 维持 bootout 状态,撞坑 #92 修复后未实战验证。

---

## 📊 6 阶段执行证据(只读)

### Phase 1(0-10 min)Git / 快照 / 质量入口核验

```text
$ git status --short --branch
## main...origin/main [ahead 2]

$ git rev-parse --short HEAD
47cfe89

$ git log --oneline -3
47cfe89 fix(snapshot): MD lint baseline 275→278(撞坑 #87 self-drift 校准)
5c0c7be fix(p3-a-t3-l2): 撞坑 #92 修复路径 B · runtime 路径迁 ~/Library/Application Support/
fcfb5a7 docs(p3-a): 收口 #89 文档一致性 + 补齐 L2/L3 memory 沉淀

$ make check-snapshot
OK: quality_snapshot matches live baseline (2913 passed / 1 skipped · 278 md files)
OK: state entry docs match quality_snapshot

$ git ls-files '*.md' | wc -l
278
```

**证据**:
- ✅ HEAD `47cfe89` 与 quality_snapshot 一致
- ✅ check-snapshot 双门 OK
- ✅ MD 文件数 278 与快照 278 对齐
- ⚠️ ahead 2(`5c0c7be` + `47cfe89`)未 push

### Phase 2(10-20 min)runtime 路径只读复核

```text
$ ls /Users/wei/bin/my-ai-employee-*
/Users/wei/bin/my-ai-employee-digital-runner
/Users/wei/bin/my-ai-employee-imap-sync
/Users/wei/bin/my-ai-employee-monthly-report
/Users/wei/bin/my-ai-employee-start

$ ls ~/Library/LaunchAgents/com.myaiemployee.*
~/Library/LaunchAgents/com.myaiemployee.agent.plist
~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist
~/Library/LaunchAgents/com.myaiemployee.imap-sync.plist

$ ls -la ~/Library/Application\ Support/MyAIEmployee/
-rw-r--r--  1 wei  staff  2355 Jul  9 14:18 .env

$ ls -la ~/Library/Logs/MyAIEmployee/
-rw-r--r--@  1 wei  staff    0 Jul  9 14:18 agent.err.log
-rw-r--r--@  1 wei  staff    0 Jul  9 14:18 agent.out.log
-rw-r--r--@  1 wei  staff  457 Jul  9 14:18 digital-employee.err.log
-rw-r--r--@  1 wei  staff  897 Jul  9 14:18 digital-employee.out.log
-rw-r--r--@  1 wei  staff  115 Jul  9 14:18 imap-sync.err.log
-rw-r--r--@  1 wei  staff    0 Jul  9 14:18 imap-sync.out.log
```

**wrapper 内 export 复核**:
```text
$ grep -E "APP_SUPPORT_DIR|ENV_FILE|DATA_DIR|LOG_DIR" /Users/wei/bin/my-ai-employee-start
# 撞坑 #92 修复(2026-07-09):runtime APP_SUPPORT_DIR / ENV_FILE 显式导出(避免 ops 默认 fallback 到 Documents 路径)
export MY_AI_EMPLOYEE_APP_SUPPORT_DIR="/Users/wei/Library/Application Support/MyAIEmployee"
export MY_AI_EMPLOYEE_ENV_FILE="/Users/wei/Library/Application Support/MyAIEmployee/.env"

$ grep "ENV_FILE" /Users/wei/bin/my-ai-employee-imap-sync
ENV_FILE="/Users/wei/Library/Application Support/MyAIEmployee/.env"

$ grep -E "APP_SUPPORT_DIR|ENV_FILE|DATA_DIR|LOG_DIR" /Users/wei/bin/my-ai-employee-digital-runner
APP_SUPPORT_DIR="${MY_AI_EMPLOYEE_APP_SUPPORT_DIR:-$HOME/Library/Application Support/MyAIEmployee}"
ENV_FILE="${MY_AI_EMPLOYEE_ENV_FILE:-$APP_SUPPORT_DIR/.env}"
DATA_DIR="$APP_SUPPORT_DIR/data"
LOG_DIR="${MY_AI_EMPLOYEE_LOG_DIR:-$HOME/Library/Logs/MyAIEmployee}"
LOG_FILE="$LOG_DIR/digital_employee.log"
MENUBAR_LOG="$LOG_DIR/menu_bar.log"
DASHBOARD_LOG="$LOG_DIR/dashboard.log"
MENUBAR_PID_FILE="$DATA_DIR/menu_bar.pid"
DASHBOARD_PID_FILE="$DATA_DIR/dashboard.pid"
```

**证据**:
- ✅ 4 wrapper + 3 plist 全部部署就位
- ✅ start wrapper 显式 export `MY_AI_EMPLOYEE_APP_SUPPORT_DIR` + `MY_AI_EMPLOYEE_ENV_FILE`
- ✅ IMAP wrapper ENV_FILE 指向 `~/Library/Application Support/MyAIEmployee/.env`
- ✅ digital-runner 全套路径变量(APP_SUPPORT_DIR + ENV_FILE + DATA_DIR + LOG_DIR + PID files)对齐
- ✅ `.env` 2355 bytes · 58 lines(撞坑 #1 红线维持:仅 stat/wc 间接验证,未读入内容)
- ✅ 日志目录 `~/Library/Logs/MyAIEmployee/` 6 个 log 文件就位

### Phase 3(20-35 min)launchd 定向测试 + snapshot guardian

```text
$ uv run pytest tests/scripts/test_launchd_install.py tests/test_quality_snapshot.py -v --no-cov
============================= 51 passed in 29.93s ==============================
```

**测试矩阵**(51 passed / 0 failed):

| 测试组 | 数量 | 关键测试 |
|--------|-----|---------|
| `tests/scripts/test_launchd_install.py` | 43 passed | F8(APP_SUPPORT setup)+ F9(wrapper export)+ F10(IMAP ENV_FILE)+ G1(start ENV_FILE)+ G2(start DATA_DIR)|
| `tests/test_quality_snapshot.py` | 8 passed | `test_tracked_md_count_matches_snapshot_lint` + `test_collected_test_count_matches_snapshot_pytest` + `test_check_quality_snapshot_script_exits_zero` |

**证据**:
- ✅ 所有 launchd 契约测试通过(撞坑 #92 修复 B 锁定)
- ✅ snapshot guardian 8 项全过(snapshot 与实测对齐严判)
- ✅ 0 真实 load 触发(`uv run pytest` 仅跑测试,不影响 launchd 状态)

### Phase 4(35-45 min)launchd 运行态 + 日志只读归档

```text
$ launchctl list | grep myaiemployee
-    0    com.myaiemployee.agent
-    2    com.myaiemployee.imap-sync

$ plutil -p ~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist
RunAtLoad => true
StandardErrorPath => "/Users/wei/Library/Logs/MyAIEmployee/digital-employee.err.log"
StandardOutPath => "/Users/wei/Library/Logs/MyAIEmployee/digital-employee.out.log"
ProgramArguments => ["/Users/wei/bin/my-ai-employee-start"]
```

**日志归档(只读,无写入)**:

| 日志 | 大小 | 内容 | 来源判定 |
|------|------|------|---------|
| `agent.out.log` | 0 | 空 | RunAtLoad=false,1 号 09:00 cron 尚未触发 |
| `agent.err.log` | 0 | 空 | 同上 |
| `imap-sync.out.log` | 0 | 空 | 下次 07:00 cron 触发 |
| `imap-sync.err.log` | 115 | `grep: .../Documents/.env: Operation not permitted` + `IMAP_USER not set in .env` | 🟡 **STALE** — 旧 wrapper 残留(部署前 run)|
| `digital-employee.out.log` | 897 | 5/9 OK + 4/9 fail + 9/9 菜单栏启动失败 | 🟡 **STALE** — T3 L3 撞坑 #92 暴露残留(bootout 后无新写)|
| `digital-employee.err.log` | 457 | `grep .env` + `data/menu_bar.log` Operation not permitted | 🟡 **STALE** — 同上 T3 L3 残留 |

**日志时间戳分析**:
- 所有日志 mtime = `Jul 9 14:18:23`(deploy-only 时 `touch` 创建)
- 内容时间非 mtime(deploy-only 只 touch 不清内容)
- imap-sync err 115 bytes 来自**前一次 wrapper run**(撞坑 #91 修复前的旧版本)
- digital-employee 两 log 来自 T3 L3 launchctl load 真实复验后 bootout(#92 暴露)

**launchctl exit code 分析**:
- `agent` exit 0 — 启动后正常退出(月报 1 号 09:00 未到,RunAtLoad=false)
- `imap-sync` exit 2 — 旧 wrapper IMAP_USER 解析失败(`IMAP_USER not set in .env` · 撞坑 #91 修复前)
- `digital-employee` 不在 list — bootout 状态(沿 docs/v0.2.67 范本)

**🟡 重要发现 — 日志均为旧 wrapper / 旧 run 残留,数字员工 bootout 后无新写。next scheduled run 才会触发新 wrapper**:
- agent 下次触发:`7 月 1 号 09:00`(周期触发,RunAtLoad=false)
- imap-sync 下次触发:`明日 07:00`(每天一次)
- digital-employee 下次触发:**仅当 user 授权 `launchctl load -w`**(RunAtLoad=true 但当前 bootout,plist enabled)

### Phase 5(45-55 min)检查报告本身(本文件)

报告产出:`docs/v0.2.74.1-p3-a-92-preload-audit-2026-07-09.md`

### Phase 6(55-60 min)下一步唯一建议

见下文。

---

## ⚠️ 剩余风险(7 项)

### R1 · 数字员工 plist 仍 bootout(撞坑 #92 修复后未真实 load 复验)

- **触发**:user 不授权 `launchctl load -w` → 数字员工永久 bootout,完全体 6/6 job 失去
- **现状**:plist enabled · RunAtLoad=true · 路径已对齐 · 0 真实运行验证
- **风险等级**:🟡 中(代码层 + 部署层均 100% 修复,但运行期未验证)
- **缓解**:撞坑 #92 修复 B 实施完成后,9 门 + check-snapshot + 51 定向测试全绿,理论预期命中;但理论 ≠ 实战

### R2 · imap-sync err log 是旧 wrapper run 残留(撞坑 #91 修复前)

- **触发**:用户读 imap-sync err log 会困惑("为什么还是 Documents/.env?")
- **现状**:log content 来自前次 launchd run(撞坑 #91 修复前),wrapper 已被 deploy-only 刷新为新版本
- **风险等级**:🟢 低(预期明日 07:00 cron 触发后会被新 wrapper 内容覆盖)
- **缓解**:等明日 07:00 触发后,新 wrapper 会读取 APP_SUPPORT/.env,正常路径不再撞 Documents

### R3 · digital-employee log 是 T3 L3 bootout 残留(#92 暴露)

- **触发**:用户读 digital-employee log 会困惑("为什么 9/9 fail?")
- **现状**:log content 来自 T3 L3 launchctl load 真实复验,bootout 后无新写
- **风险等级**:🟢 低(bootout 后无新写,user 授权 load 后新 log 会覆盖)
- **缓解**:user 授权后预期:`tail digital-employee.err.log` 无 `Operation not permitted` + `tail digital-employee.out.log` 显示 9/9 OK + 菜单栏启动成功

### R4 · `agent + imap-sync` launchctl 退出码(2/2)是旧 run 残留

- **现状**:`launchctl list` 显示 `- 0 agent`(exit 0)· `- 2 imap-sync`(exit 2 · 旧 wrapper IMAP_USER 解析失败)
- **风险等级**:🟢 低(下次 cron 触发后,新 wrapper 走 APP_SUPPORT/.env,预期 exit 0)
- **缓解**:待下次自然触发(明日 07:00)

### R5 · 撞坑 #90 launchd session-bound 未触及

- **现状**:9.5h 内 active → inactive 衰减 · reboot/logout 后失效
- **风险等级**:🟡 中(持久化方案 4 候选未实施)
- **缓解**:D-step 评估(re-load runbook / bootstrap / LaunchHelper / zshrc auto-load),待 user 决策

### R6 · docs/v0.2.67 §19-21 误归未校正

- **现状**:撞坑 #81 真实根因是 #91,文档误归"数字员工 TCC Python.framework 3.12"
- **风险等级**:🟢 低(docs-only 边界外 · 撞坑 #50 严判)
- **缓解**:可选 docs-only D-step 校正

### R7 · coverage 89.10%(-0.02pp 抖动)

- **现状**:对比上一轮 89.12%,基线 snapshot 已校准
- **风险等级**:🟢 低(<0.1pp 抖动,沿 v0.2.54-mypy-drift-sop 范本不入校准)
- **缓解**:后续若持续出现再统一校准

---

## 🚫 禁止动作清单(本巡检期间 / T3 L3 之前)

| 禁止动作 | 红线 | 阻塞原因 |
|---------|------|---------|
| **SMTP 真发** | 撞坑 #59 + #76 + #78 + #85 | 默认未解锁 · 每次需 user 临时授权 |
| **Notes 生产同步** | 撞坑 #65 + #71 + #89 | Notes 真加密未开,默认走 dry-run |
| **Path4 写入** | 撞坑 #18 | `ENABLE_PATH_4_WRITE` 默认 UNSET |
| **v1.0 tag** | 撞坑 #60 反转(7/1 v0.2.1 已落地)| v1.0 tag 默认不打 |
| **git push origin main** | 红线 · Default no push | 需 user 明确 `PUSH` 或 `P` 词 |
| **`launchctl load -w`** 数字员工 | 撞坑 #81 + #92 | 待 user 授权 T3 L3 实测复验 |
| **`ENABLE_PATH_4_WRITE=1`** | 撞坑 #18 | 任何形式都不写 shell profile |
| **`ENABLE_NOTES_ENCRYPTION=1`** | 撞坑 #1 + #65 | 任何形式都不写 shell profile |
| **Outlook / Gmail 配置** | 撞坑 #59 | 不配置 · 不写入 Keychain |
| **`cat / head / grep .env`** | 撞坑 #1 | 凭据永远不进 chat / commit / docs(本巡检已遵守)|

---

## ✅ 可执行证据清单(可粘贴回 user)

```
# Git
git rev-parse --short HEAD → 47cfe89
git status --short --branch → ## main...origin/main [ahead 2]

# Snapshot
make check-snapshot → OK (2913 passed / 1 skipped · 278 md files)
git ls-files '*.md' | wc -l → 278

# Tests(目标测试组)
uv run pytest tests/scripts/test_launchd_install.py tests/test_quality_snapshot.py --no-cov
→ 51 passed in 29.93s

# Quality gates
make test → 2913 passed, 1 skipped
make lint → Linting: 278 file(s) · 0 error(s)
make mypy → Success: no issues found in 256 source files
make coverage → 89.10% (Required test coverage of 80.0% reached)
make check-snapshot → OK · OK

# Runtime paths
ls /Users/wei/bin/my-ai-employee-* → 4 wrappers
ls ~/Library/LaunchAgents/com.myaiemployee.* → 3 plists
ls -la ~/Library/Application\ Support/MyAIEmployee/ → .env (2355 bytes, 58 lines)
ls -la ~/Library/Logs/MyAIEmployee/ → 6 log files (agent/imap-sync/digital-employee × out/err)
stat -f "%Sm %z %N" ~/Library/Logs/MyAIEmployee/*.log → all mtime Jul 9 14:18

# launchd state
launchctl list | grep myaiemployee → - 0 agent · - 2 imap-sync (digital-employee bootout)

# Wrapper content verification(heredoc 生成正确)
grep "ENV_FILE" /Users/wei/bin/my-ai-employee-imap-sync → 
    ENV_FILE="/Users/wei/Library/Application Support/MyAIEmployee/.env"
grep -E "APP_SUPPORT|ENV_FILE" /Users/wei/bin/my-ai-employee-start →
    export MY_AI_EMPLOYEE_APP_SUPPORT_DIR="/Users/wei/Library/Application Support/MyAIEmployee"
    export MY_AI_EMPLOYEE_ENV_FILE="/Users/wei/Library/Application Support/MyAIEmployee/.env"
```

---

## 🎯 下一步唯一建议(Phase 6)

**唯一选择:是否进入"数字员工真实 load 复验授权窗口"**

### 选项 A · 进入 T3 L3 真实 load 复验(user 授权后执行)

- user 单次授权 `launchctl load -w com.myaiemployee.digital-employee.plist`
- 5 秒后 tail logs 验证:
  - `tail ~/Library/Logs/MyAIEmployee/digital-employee.err.log` → 预期 无 `Operation not permitted`
  - `tail ~/Library/Logs/MyAIEmployee/digital-employee.out.log` → 预期 9/9 预检 OK + 菜单栏启动成功
- `launchctl list | grep myaiemployee` → 预期 3/3 注册(数字员工 state 非 -1)
- 通过 → 撞坑 #92 完全修复验证 ✅
- 不通过 → 立即 `launchctl bootout`,回到 bootout 状态,后续 debug

### 选项 B · 维持现状(数字员工 bootout)

- 不授权 load,数字员工永久 bootout
- 撞坑 #92 修复 B 保持"代码层已修 + 部署层已部署 + 0 真实运行验证"状态
- 风险:完全体 6/6 job 失去(v0.2.74 锁定的"全天候数字员工"功能未实战验证)

### 选项 C · 不复验,直接 push 2 commits(撞坑 #71 docs-only 边界内)

- 不授权 load,但授权 push 5c0c7be + 47cfe89
- 远端 main 同步本地撞坑 #92 修复 B 代码
- 撞坑 #92 修复持久化到 git 历史,后续 D-step 可任意时刻复验

### 推荐路径

按撞坑 #71 docs-only 边界 + 红线严判:**选项 C → 选项 A**

1. 先 push(选项 C)把撞坑 #92 修复 B 代码持久化到远端,这是 docs-only 边界 + 0 真实 load 动作
2. 再 user 授权 load(选项 A)做真实复验,这是 1 次性红线动作需 user 明确授权
3. 无论 A 或 B,撞坑 #92 代码层修复已完成,运行期只是确认/验证

---

## 📋 红线维持(17 项 + 撞坑 #90/#91/#92)

| 红线 | 状态 |
|------|------|
| 撞坑 #1 凭据安全 | ✅(本巡检 .env 仅 stat/wc 间接验证)|
| 撞坑 #18 ENABLE_PATH_4_WRITE UNSET | ✅ |
| 撞坑 #59 outlook/gmail 不配 | ✅ |
| 撞坑 #65 NotesCipherImpl.decrypt fallback | ✅ |
| 撞坑 #71 docs-only 边界 | ✅(本巡检纯只读)|
| 撞坑 #76 outbox status 小写 | ✅ |
| 撞坑 #78 real mode count=1 | ✅ |
| 撞坑 #79 redactor email 后半部 | ✅ |
| 撞坑 #81 数字员工 TCC Python 误判 | ✅ 真实根因 #91 → #92 |
| 撞坑 #85 LLM 草稿幻觉 3 层 | ✅ |
| 撞坑 #86 router 空 token 优雅降级 | ✅ |
| 撞坑 #87 snapshot self-referential 校准 | ✅ 本轮用 |
| 撞坑 #88 spike ↔ src drift | ✅ |
| 撞坑 #89 real-flow-notesync-dedup | ✅ |
| 撞坑 #90 launchd session-bound | ⚠️ 待 D-step 持久化 |
| 撞坑 #91 Documents exec OS 拦截(launchd exec 阶段)| ✅ T3 L3 完全修复验证 |
| 撞坑 #92 业务代码路径 Documents 沙箱 | ✅ 代码层修复 + 部署层就位 · 0 真实 load 验证 |
| v1.0 tag 不打 | ✅ |
| 不写 shell profile | ✅ |
| 不自动 launchctl load -w 数字员工 | ✅ 维持(等 user 授权)|
| 0 SMTP 真发 | ✅ |
| 0 Notes 生产同步 | ✅ |
| 0 ENABLE_PATH_4_WRITE | ✅ |

---

## 全局快照

| 维度 | 状态 |
|------|------|
| HEAD | `47cfe89` |
| ahead/behind | ahead 2 / behind 0 |
| 9/9 质量门 | ✅ 全绿(2913/1/89.10/278/256)|
| snapshot 双门 | ✅ OK |
| launchd 实际 | 2/3 active + 1/3 bootout |
| launchd 契约测试 | ✅ 43/43 |
| snapshot guardian | ✅ 8/8 |
| runtime wrapper | ✅ 4 部署 |
| runtime plist | ✅ 3 部署 |
| runtime .env | ✅ 2355 bytes · 58 lines(APP_SUPPORT 已迁)|
| runtime log dir | ✅ 6 log 文件就位 |
| v1.0 完成度 | **91%** |
| 可无人值守 | **90%** |
| 项目整体 | **94%** |