# v0.2.53.5 P2 — 静态 Dashboard 接扩展只读 API(2026-06-25)

> **状态**:✅ 已落地 · **边界**:只读 GET、静态 HTML、127.0.0.1、无新依赖、不写凭据、不真发 SMTP。

## 结论

承接 v0.2.53.4 三个新端点,静态 `docs/ui/codex-style-dashboard.html` 现已 hydrate 五个只读端点;API 未启动时邮件/笔记/财务页与今日队列自动回退静态假数据。

## 连接端点

| 路径 | 页面 |
|------|------|
| `GET /api/status` | 顶部状态条 + 系统页 |
| `GET /api/tasks/today` | 今日摘要 + 导航 badge |
| `GET /api/outbox?limit=10` | 邮件页 + 今日队列 |
| `GET /api/notes/pending?limit=10` | 笔记页 + 今日队列 |
| `GET /api/finance/anomalies?limit=10` | 财务页 + 今日队列 |

## 使用方式

```bash
make dashboard-api
open docs/ui/codex-style-dashboard.html
```

预期:

- API 启动时:顶部显示 `API 已连接 · 5 端点`,邮件/笔记/财务页列表来自 API。
- API 未启动时:显示 `API 离线兜底`,继续展示静态假数据。

## 下一棒

- `OutboxDraftServiceImpl` 接 `OutboxStore.by_status`(Stub → 真实草稿)
- 写操作仍留 ApprovalGate 后续
