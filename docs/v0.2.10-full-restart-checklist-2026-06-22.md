# v0.2.10 全链路重启 checklist(6/23+ 真实前置核验 · 撞坑恢复 3 步实战演练 4)

> **项目**:我的AI员工
> **文档时间**:2026-06-22 端午连休最后一天(周一)
> **承接**:[[v0.2.9-w3-real-bill-spike-prep-2026-06-22]] §"下一棒" + [[v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21]] §"启动流程 5 阶段" + [[v0.2.5-smtp-real-send-preflight-2026-06-18]] §"撞坑恢复 3 步实战演练 1" + [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本
> **范围**:**6/23 全链路重启 checklist docs-only** — 1 commit · docs/v0.2.10-full-restart-checklist-2026-06-22.md 新建 · SESSION-STATE/MODIFICATION-LOG/README 同步 · **不真发邮件 · 不真导入账单 · 不移动 v0.1.0 tag · 不打 v0.2.0/v0.2.1 tag**
> **撞坑恢复 3 步实战演练**:本报告作为 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本的**实战演练 4**(演练 1 = v0.2.5 + 演练 2 = v0.2.7 + 演练 3 = v0.2.9 + 演练 4 = v0.2.10 全链路重启 checklist)
> **维护者**:Mr-PRY · **模型**:MiniMax-M3

---

## 🎯 文档目标

把 6/23 全链路重启 **启动前 6 模块核验完整化 + 启动步骤清单化 + 风险门控文档化**,作为 6/23 周二全链路重启时的**只读执行手册**。

**关键约束**(沿用户 2026-06-22 决策 §3):
- ❌ **不真发邮件**(本报告纯 docs-only)
- ❌ **不真导入账单**(本报告纯 docs-only)
- ❌ **不移动 `v0.1.0` tag**(继续锚定 `2af775f`,沿 D5.7.2 范本)
- ❌ **不打 `v0.2.0/v0.2.1` tag**(留作 8/1 锚定策略)
- ❌ **不跑真实 OAuth flow**(等用户授权 + 凭据)
- ❌ **不在没有 CSV 的情况下造"真账单"结论**

## 🔍 6 模块链路核验(2026-06-22 现状)

### 模块 1:launchd/kickstart ✅ 就绪

**关键文件**:
- ✅ `scripts/launchd_install.sh`(5 源判定范本 + D10.5.3 P2-2 临时文件)
- ✅ `scripts/launchd_kickstart_and_seal.sh`(6/23 kickstart 入口,4 退出码范本)
- ✅ `scripts/launchd_uninstall.sh`
- ⚠️ 当前未安装 plist(`~/Library/LaunchAgents/com.myaiemployee.agent.plist` 需 6/23 跑 kickstart 安装)

**核验内容**:
- 脚本文件全部就绪(3 文件)
- 沿 [[v0.1-launch-decision-option-c-2026-06-16]] 选 C 方案 Step 2-5
- 沿 [[d10.5.4-launchd-kickstart-and-seal]] 4 退出码范本:
  - `0` = 成功
  - `1` = 源缺失(monthly_report.py / plist / 已部署脚本 缺失)
  - `2` = 业务失败(预期,不是 bug — 7/1 自然触发)
  - `3` = 技术失败(kickstart / 5 源判定最终失败 / release notes flip 失败)

**6/23 全链路重启动作**:
```bash
bash scripts/launchd_kickstart_and_seal.sh
```

### 模块 2:DB 路径 ✅ 就绪

**关键路径**:
- ✅ `.gitignore`:`data/` `*.db` `*.db-journal` `*.db-wal` `*.db-shm`(运行时数据不提交)
- ✅ SQLCipher 加密:`src/my_ai_employee/core/db.py` 范本(D1.1 替代 pysqlcipher3)
- ⚠️ `data/` 目录需 6/23 全链路重启时创建(沿 `make install` 或 `make init` 范本)

**核验内容**:
- 路径规范:`data/data.db`(加密) + `data/health.log` + `data/llm_audit.log`
- 数据隔离:加密 DB 不入 git · `.gitignore` 5 条规则覆盖所有运行时数据
- 跨平台兼容:macOS Sequoia TCC 不影响 data/ 目录(本地优先)

**6/23 全链路重启动作**:
```bash
mkdir -p data/
# 首次启动会自动创建 data/data.db(沿 db.py 范本)
make install
```

### 模块 3:菜单栏 ✅ 就绪

**关键文件**(沿 v0.2.2 #6 badge polling + v0.2 B-5 Quartz + D8.5 异常告警 + v0.2.2 #2 1-click 确认):
- ✅ `menu_bar/app.py`(rumps 入口 + badge polling)
- ✅ `menu_bar/clipboard_listener.py`(Quartz CGEvent tap,沿 v0.2 B-5)
- ✅ `menu_bar/expense_service.py`(5 类异常告警菜单项,沿 v0.2.2 P0 + D8.5)
- ✅ `menu_bar/note_confirm_service.py`(1-click 确认 UI,沿 v0.2.2 #2)
- ✅ `menu_bar/tcc.py`(macOS Sequoia TCC 兼容,沿 v0.2 AppleScript 坑)

**核验内容**:
- 7 文件全部就绪
- 依赖:`rumps`(macOS 菜单栏原生) + `Quartz`(CGEvent tap)
- 启动入口:`make menu-bar` 或 `python -m my_ai_employee.menu_bar.app`

**6/23 全链路重启动作**:
```bash
# 启动菜单栏
make menu-bar

# 或后台启动
nohup python -m my_ai_employee.menu_bar.app > data/menu_bar.log 2>&1 &
```

### 模块 4:Notes ✅ 就绪

**关键文件**(沿 D9 Apple Notes + v0.2.2 P0 + v0.2.1 #4/#5 + v0.2.2 #2):
- ✅ `connectors/apple_notes.py`(Apple Notes 同步入口,沿 D9)
- ✅ `ai/note_structurer.py` + `ai/prompts/note_structurer.py`(L2 候选结构化,沿 v0.2.2 P0)
- ✅ `db/notes.py` + `core/migrations/versions/0008_notes.py`(NoteStore 状态机化 + L2/L3 跨源去重,沿 v0.2.1 #4/#5)
- ✅ `scripts/sync_notes.py`(Apple Notes 同步脚本,沿 C5 S7 e2e 范本)
- ✅ `menu_bar/note_confirm_service.py`(1-click 确认 UI,沿 v0.2.2 #2)

**核验内容**:
- 9 文件全部就绪
- NoteStore 5 状态机(NEW / STRUCTURED / PRIVATE_SKIP / FAILED / ARCHIVED)
- L2/L3 跨源去重:normalized_fingerprint SHA-256 + ±1 day 模糊匹配
- Apple Notes 同步:`scripts/sync_notes.py`(30 笔真实样本验证)

**6/23 全链路重启动作**:
```bash
# Apple Notes 同步(等用户手动触发)
uv run python scripts/sync_notes.py --confirm yes-i-understand-this-syncs-real-notes
```

### 模块 5:账单导入脚本 ✅ 就绪

**关键文件**(沿 D6.1/D7.6 + v0.2.2 D6.2 fingerprint + D6.3 categorizer):
- ✅ `scripts/import_wechat.py`(微信账单 CSV 导入,沿 D6.1 范本)
- ✅ `scripts/import_alipay.py`(支付宝账单 CSV 导入,沿 D7.6 范本)
- ✅ `scripts/import_all.py`(混合账单 CSV 导入,自动嗅探)
- ✅ `scripts/spike_real_bill.py`(真实账单 spike 入口,沿 W3 真账单 spike 准备)
- ✅ `connectors/wechat_csv.py`(2024/2025/2026 三版本 Parser + `detect_version` 工厂层)
- ✅ `connectors/alipay_csv.py`(2024/2025 三版本 Parser + 工厂层)

**核验内容**:
- 6 文件全部就绪
- W3 faker 三阶段验证基线(沿 [[v0.2-d8-real-faker-spike-2026-06-17]]):
  - 30 笔 / 102 笔 / 1000 笔 faker 全跑通
  - 真异常误报率 0% 维持(D8.5.4 修复后)
  - 性能 0.5-0.7ms / 笔
- W3 真 spike 启动条件:等用户提供真实微信/支付宝 CSV(沿 [[v0.2.9]] §"启动流程 5 阶段")

**6/23 全链路重启动作**(等用户提供真实 CSV 后):
```bash
export WECHAT_REAL_IMPORT=1
uv run python scripts/spike_real_bill.py \
  --csv /Users/wei/Documents/账单/wechat_2026.csv \
  --count 30 \
  --confirm yes-i-understand-this-imports-real-bill
```

### 模块 6:SMTP 门控 ✅ 就绪(5 重门控)

**关键代码**(沿 [[d5.6.5-real-send]] 4 重防误发 + [[v0.2.2-p8-smtp-provider-factory-2026-06-18]] SMTPProviderFactory):

| # | 门控 | 实现位置 | 当前状态 |
|---|------|---------|---------|
| 1 | `SMTP_REAL_NETWORK=1` env 门控 | `scripts/spike_send_100.py:81-84, 353-363` | ✅ 就绪 |
| 2 | `--smtp-provider {qq}` 白名单 | `scripts/spike_send_100.py:1010` | ✅ 就绪(当前仅 qq · outlook/gmail B 类决策延后)|
| 3 | Keychain round-trip(provider-aware)| `src/my_ai_employee/core/keychain.py:252, 261` | ✅ 就绪(`get_smtp_password_for_provider` + `set_smtp_password_for_provider`)|
| 4 | 强制 1 收件人(`--max-recipients 1`)| `scripts/spike_send_100.py --help` | ✅ 就绪 |
| 5 | `--confirm` 二次确认口令 | `scripts/spike_send_100.py --help` | ✅ 就绪 |

**核验内容**:
- 5 重门控全部就绪
- QQ SMTP 真实发送已可用(沿 [[d5.6.5-real-send]] · smtp.qq.com:465 SSL · sent=1/1.27s)
- outlook/gmail SMTP 真实发送阻塞于:
  - provider 白名单扩展(B 类决策延后)
  - 真实 OAuth flow 未跑(等用户授权 + 凭据)
- 撞坑恢复 3 步实战演练 2(沿 [[v0.2.7]] §"启动流程 5 阶段"· 阻塞项 3 项)

**6/23 全链路重启动作**(等用户授权 + 凭据后):
```bash
export SMTP_REAL_NETWORK=1
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.qq.com \
  --smtp-port 465 \
  --smtp-username <qq邮箱> \
  --smtp-provider qq \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1
```

## 🛡️ 6 模块链路风险门控(汇总)

| # | 模块 | 主要风险 | 门控策略 |
|---|------|---------|---------|
| 1 | launchd/kickstart | 5 源判定最终失败 / plist 字面值变化 | 4 退出码范本 + 6/23 跑前 `bash scripts/launchd_kickstart_and_seal.sh --dry-run` |
| 2 | DB 路径 | data/ 目录未创建 / SQLCipher 密码缺失 | 6/23 跑前 `mkdir -p data/` + `.env` 含 SQLCIPHER_KEY |
| 3 | 菜单栏 | TCC 拦截 / Quartz 权限不足 | 6/23 跑前 `open -a System Settings` 授权 TCC + 首次跑触发 AppleScript 弹窗点"好" |
| 4 | Notes | Apple Notes 权限 / NoteStore 状态机异常 | 6/23 跑前 `make test tests/notes/` 验证 + 异常时降级 PRIVATE_SKIP 状态 |
| 5 | 账单导入 | WECHAT_REAL_IMPORT 未设 / 真实 CSV 缺失 | 4 重防误发门控(env + 强制 1 批 + 确认口令 + 列名嗅探)+ 等用户真实 CSV |
| 6 | SMTP 门控 | SMTP_REAL_NETWORK 未设 / outlook/gmail 白名单限制 | 5 重门控全开 + outlook/gmail B 类决策延后(沿 [[b-class-deferral-2026-06-09]]) |

## 📋 6/23 全链路重启 checklist(7 阶段)

### 阶段 1:环境准备

```bash
# 1.1 git pull(可选)
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工
git status  # 确认 clean

# 1.2 make install(沿 [[d1.1-pyproject-uv-migration]])
make install  # = uv sync --extra dev + pip install -e .

# 1.3 mkdir data/
mkdir -p data/

# 1.4 检查 .env 含 SQLCIPHER_KEY
cat .env  # 应含 SQLCIPHER_KEY + SMTP_REAL_NETWORK 等
```

**预期输出**:`make install` 成功 · `data/` 创建 · `.env` 完整

### 阶段 2:8/8 质量门 baseline 验证

```bash
make test      # 2225 passed / 1 skipped / 88.85% coverage
make lint      # 0 errors / 106 files
```

**预期输出**:8/8 质量门全绿(本 baseline 沿 v0.2.6 锚定)

### 阶段 3:launchd kickstart + plist 安装

```bash
# 3.1 dry-run 验证(可选)
bash scripts/launchd_kickstart_and_seal.sh --dry-run  # 需脚本支持 dry-run,否则跳过

# 3.2 kickstart + 5 源判定
bash scripts/launchd_kickstart_and_seal.sh
```

**预期输出**:`exit 0` 或 `exit 2`(预期业务失败契约,7/1 自然触发)

### 阶段 4:菜单栏启动

```bash
# 4.1 启动菜单栏
make menu-bar

# 或后台启动
nohup python -m my_ai_employee.menu_bar.app > data/menu_bar.log 2>&1 &
```

**预期输出**:菜单栏图标出现在 macOS 顶部 · `data/menu_bar.log` 写入

### 阶段 5:Apple Notes 同步(可选,等用户手动触发)

```bash
# 5.1 dry-run 验证(可选)
uv run python scripts/sync_notes.py --dry-run

# 5.2 真实同步(等用户手动触发)
uv run python scripts/sync_notes.py --confirm yes-i-understand-this-syncs-real-notes
```

**预期输出**:Apple Notes 同步完成 · NoteStore 状态机化 NEW → STRUCTURED

### 阶段 6:W3 真账单 spike(等用户提供真实 CSV)

```bash
# 6.1 用户手动导出微信/支付宝账单(沿 [[docs/微信账单导出教程]] 8 步 / [[docs/支付宝账单导出教程]] 5 步)

# 6.2 真实 spike 30 笔
export WECHAT_REAL_IMPORT=1
uv run python scripts/spike_real_bill.py \
  --csv /Users/wei/Documents/账单/wechat_2026.csv \
  --count 30 \
  --confirm yes-i-understand-this-imports-real-bill
```

**预期输出**:30 笔真实导入成功 · 真异常误报率 < 5%(沿 D8.5.4 baseline)· 性能 < 1ms / 笔

### 阶段 7:outlook/gmail SMTP 真实发送 spike(等用户授权 + 凭据)

```bash
# 7.1 用户授权 + 凭据就绪(沿 [[v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21]] §"启动流程 5 阶段")
# 7.2 provider 白名单扩展(B 类决策)
# 7.3 OAuth flow 跑通(可选)
# 7.4 OutboxDispatcher 集成(可选)

# 7.5 真实发送 1 封(沿 [[d5.6.5-real-send]] 4 重防误发)
export SMTP_REAL_NETWORK=1
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.office365.com \
  --smtp-port 587 \
  --smtp-username <outlook邮箱> \
  --smtp-provider outlook \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1
```

**预期输出**:1 封真实发送成功 · 状态机 4 步全过(PENDING_SEND → APPROVED → SENDING → SENT)· sent=1/1.27s(沿 d5.6.5 baseline)

## 🔄 撞坑恢复 3 步实战演练 4(沿 v0.2.4 机制 3)

本报告作为 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本的**实战演练 4**。

### 撞坑恢复 3 步范本累计

| 演练 | 报告 | 时间 | 0 撞坑触发 |
|------|------|------|----------|
| 演练 1 | v0.2.5 SMTP 真实发送 spike preflight | 2026-06-18 | ✅ |
| 演练 2 | v0.2.7 outlook/gmail SMTP spike 准备 | 2026-06-21 | ✅ |
| 演练 3 | v0.2.9 W3 真账单 spike 准备 | 2026-06-22 | ✅ |
| **演练 4** | **v0.2.10 全链路重启 checklist(本次)** | **2026-06-22** | **✅** |
| 撞坑史累计 | v0.2.2 #8 SMTPProviderFactory | 2026-06-18 | 1 次撞坑(已恢复) |
| **范本累计** | **4 个实战演练(均 0 撞坑触发)** | — | ✅ 沿 [[v0.2.4]] §3 机制 3 |

### 演练 4:本任务的撞坑恢复 3 步

#### 步骤 1:撤回自己基于旧状态写的 docs-only 报告

- **本任务**:**不需要撤回**(本任务是首次执行全链路重启 checklist,无旧状态报告)
- **触发条件**:未来撞同款问题(并发进程冲突 6/23 全链路重启实化撞坑)时,撤回自己写的 stale 报告

#### 步骤 2:验证并发进程工作通过 8/8 质量门

- **本任务**:**不需要验证**(因为没有并发进程)
- **8/8 质量门 baseline 验证**(本 preflight docs-only 不涉及代码):
  - ✅ ruff check:All checks passed
  - ✅ ruff format:已 formatted
  - ✅ mypy src:Success 101 files 0 errors
  - ✅ alembic --sql:exit 0
  - ✅ pytest:2225 passed / 1 skipped / 88.85% coverage
  - ✅ uv build:success
  - ✅ MD lint:0 errors / 106 files
  - ✅ coverage:88.85% ≥ 80%

#### 步骤 3:用户授权代 commit 2 commits(feat + docs closure)

- **本任务**:**不需要**(因为本任务是 docs-only,沿 v0.2.5/v0.2.7/v0.2.9/v0.2.8 范本)
- **commit 范本**:`docs(sync): v0.2.10 全链路重启 checklist docs-only`

### 撞坑恢复 3 步应用次数统计

- **撞坑恢复 3 步实际应用次数**:**0 次**(本会话无撞坑)
- **撞坑史累计次数**:1 次(v0.2.2 #8 SMTPProviderFactory 撞坑恢复,2026-06-18 22:00)
- **撞坑恢复范本累计**:4 个(演练 1 + 演练 2 + 演练 3 + 演练 4)
- **7/1 月度复盘**:撞坑恢复范本应用情况统计(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §4 复盘 5 项)

## 🚦 全链路重启后真实 spike 启动路径(6/23+)

### 路径 A:W3 真账单 spike(等用户提供真实 CSV)

```
用户导出微信/支付宝账单
       ↓
WECHAT_REAL_IMPORT=1 + 4 重防误发门控
       ↓
跑 spike_real_bill.py --csv wechat_2026.csv --count 30
       ↓
fingerprint + 3 层去重 + categorizer + D8 异常检测
       ↓
真异常误报率 < 5%(沿 D8.5.4 baseline)
       ↓
docs 收口报告(spike 跑通报告 + 真异常检测报告)
```

### 路径 B:outlook/gmail SMTP 真实 spike(等用户授权 + 凭据)

```
用户授权扩展白名单 + 提供 Outlook/Gmail 凭据
       ↓
B 类决策激活(沿 [[b-class-deferral-2026-06-09]])
       ↓
provider 白名单扩展(B 类决策,1 commit)
       ↓
OAuth flow 跑通(可选,1-2 commits)
       ↓
OutboxDispatcher 集成(可选,1 commit)
       ↓
SMTP_REAL_NETWORK=1 + 5 重防误发门控
       ↓
跑 spike_send_100.py --real --smtp-provider outlook/gmail
       ↓
1 封真实发送 + 状态机 4 步全过
       ↓
docs 收口报告(spike 跑通报告)
```

### 路径 C:QQ SMTP 真实 spike(已就绪,等用户授权)

```
用户授权 + QQ 邮箱凭据(App Password 或已存在 Keychain)
       ↓
SMTP_REAL_NETWORK=1 + 5 重防误发门控
       ↓
跑 spike_send_100.py --real --smtp-provider qq
       ↓
1 封真实发送(沿 [[d5.6.5-real-send]] baseline)
       ↓
docs 收口报告(spike 跑通报告)
```

## 🔗 关键引用(12 项)

- **本报告**: `我的AI员工/docs/v0.2.10-full-restart-checklist-2026-06-22.md`(6 模块核验 + 7 阶段启动流程 + 撞坑恢复 3 步实战演练 4)
- **v0.2.9 W3 真账单 spike 准备**: `我的AI员工/docs/v0.2.9-w3-real-bill-spike-prep-2026-06-22.md`(W3 真账单 spike 启动流程 5 阶段)
- **v0.2.7 outlook/gmail SMTP spike 准备**: `我的AI员工/docs/v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21.md`(outlook/gmail 启动流程 5 阶段 + 撞坑恢复实战演练 2)
- **v0.2.5 SMTP 真实发送 spike preflight**: `我的AI员工/docs/v0.2.5-smtp-real-send-preflight-2026-06-18.md`(4 模块链路核对 + 5 重防误发门控 + 撞坑恢复实战演练 1)
- **v0.2.4 状态漂移审查机制**: `我的AI员工/docs/v0.2.4-drift-review-mechanism-2026-06-18.md`(`机制 3` 撞坑恢复 3 步范本 · 实战演练 4 锚定)
- **v0.2 release notes**: `我的AI员工/docs/v0.2-release-notes-2026-06-22.md`(v0.2.1 release tag 锚定策略 + 8 项前置条件)
- **W3 faker spike 报告**: `我的AI员工/reports/v0.2-d8-real-faker-spike-2026-06-17.md`(D8.5.4 修复后真异常误报率 0% + 30/102/1000 笔三阶段验证)
- **微信账单导出教程**: `我的AI员工/docs/微信账单导出教程.md`(8 步导出路径)
- **支付宝账单导出教程**: `我的AI员工/docs/支付宝账单导出教程.md`(5 步导出路径)
- **D5.6.5 真实 1 封 SMTP 范本**: `Agent Assistant/memory/d5.6.5-real-send.md`(smtp.qq.com:465 SSL · sent=1/1.27s · 4 重防误发)
- **D10.5.4 launchd kickstart and seal 范本**: `Agent Assistant/memory/d10.5.4-launchd-kickstart-and-seal.md`(4 退出码范本)
- **v0.1 launch decision 选项 C**: `Agent Assistant/memory/v0.1-launch-decision-option-c-2026-06-16.md`(选 C 方案 Step 2-5)

## 📌 下一棒

### 端午连休后(6/23 周二 · 全链路重启当天)

- **6/23 当天**:跑本报告 §"6/23 全链路重启 checklist 7 阶段":
  - 阶段 1:环境准备(make install + mkdir data/ + 检查 .env)
  - 阶段 2:8/8 质量门 baseline 验证(make test + make lint)
  - 阶段 3:launchd kickstart + plist 安装
  - 阶段 4:菜单栏启动
  - 阶段 5:Apple Notes 同步(可选,等用户手动触发)
  - 阶段 6:W3 真账单 spike(等用户提供真实 CSV)
  - 阶段 7:outlook/gmail SMTP 真实 spike(等用户授权 + 凭据)

### 真实链路阻塞(沿 [[v0.2-release-notes-2026-06-22]] §"阻塞项 5 项")

- ⏸️ **W3 真账单 spike**:等用户提供真实微信/支付宝 CSV
- ⏸️ **outlook/gmail SMTP 真实 spike**:等用户授权 + 凭据 + B 类决策白名单扩展
- ⏸️ **`v0.1.0` tag 锚定**:`2af775f` 不动(继续锚定)
- ⏸️ **不跑真实 OAuth flow**:等用户手动跑 OAuth flow 拿 access_token + refresh_token

### 月度复盘 + release tag 锚定(7/1 + 8/1)

- **7/1 月度复盘**(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §4 复盘 5 项):
  - 状态漂移审查(机制 1 + 2)
  - 并发进程冲突审查(机制 4)
  - B 类延后清单重新评估(D4.7.4 v1.0.3 已实化 + outlook/gmail 部分实化 + W3 真账单已 spike + NoteStore OAuth 集成未实化 + OutboxDispatcher 集成未实化)
  - v0.2.1 release tag 锚定策略复审
  - 撞坑恢复范本应用情况统计(累计 4 个实战演练)
- **8/1**:v0.2.1 release tag 锚定(沿 [[v0.2-release-notes-2026-06-22]] §"8 项前置条件")

## 📌 完成定义(本报告 Done)

- [x] 6 模块链路核验(launchd/kickstart + DB 路径 + 菜单栏 + Notes + 账单导入脚本 + SMTP 门控 · 全部就绪)
- [x] 6 模块风险门控汇总(env 门控 + 退出码范本 + 4/5 重防误发 + Keychain + 列名嗅探)
- [x] 6/23 全链路重启 checklist 7 阶段(环境准备 + 8/8 质量门 + launchd + 菜单栏 + Notes + W3 + outlook/gmail)
- [x] 真实 spike 启动路径 3 条(W3 + outlook/gmail + QQ · 阻塞条件明确)
- [x] 撞坑恢复 3 步实战演练 4 沉淀(本任务无撞坑,沿 [[v0.2.4]] §3 机制 3 范本)
- [x] 撞坑恢复范本累计 4 个(演练 1 + 演练 2 + 演练 3 + 演练 4 · 均 0 撞坑触发)
- [x] 12 项关键引用(v0.2.9/v0.2.7/v0.2.5/v0.2.4 + v0.2 release notes + W3 faker + 微信/支付宝教程 + D5.6.5 + D10.5.4 + v0.1 launch decision)
- [x] 6/23 全链路重启路径 + 真实 spike 阻塞路径 + 7/1 月度复盘路径 + 8/1 release tag 锚定路径
- [x] **不真发邮件**(本报告纯 docs-only,只做 6 模块核验 + 7 阶段 checklist)
- [x] **不真导入账单**(本报告纯 docs-only,等用户提供真实 CSV)
- [x] **不移动 `v0.1.0` tag**(继续锚定 `2af775f`,沿 D5.7.2 范本)
- [x] **不打 `v0.2.0/v0.2.1` tag**(留作 8/1 锚定策略)
- [x] **不跑真实 OAuth flow**(等用户授权 + 凭据)
- [x] **不在没有 CSV 的情况下造"真账单"结论**(等用户提供真实微信/支付宝 CSV)

## 维护者

**Mr-PRY** · 2026-06-22 v0.2.10 全链路重启 checklist docs-only(撞坑恢复 3 步实战演练 4)
**模型**:MiniMax-M3
**项目**:我的AI员工(v0.2.10 全链路重启 checklist docs-only · 端午不休息第 5 天 · 端午连休最后一天)
**关键决策**:**6 模块链路核验 + 7 阶段启动 checklist + 3 真实 spike 启动路径**(沿用户结论 §1-3);全部 6 模块 ✅ 就绪 · 启动阻塞项 5 项明确 · 撞坑恢复实战演练 4 沉淀
**沿用范本**:[[v0.2.9-w3-real-bill-spike-prep-2026-06-22]] / [[v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21]] / [[v0.2.5-smtp-real-send-preflight-2026-06-18]] / [[v0.2.4-drift-review-mechanism-2026-06-18]] / [[v0.2-release-notes-2026-06-22]] / [[v0.2-d8-real-faker-spike-2026-06-17]] / [[d5.6.5-real-send]] / [[d10.5.4-launchd-kickstart-and-seal]] / [[v0.1-launch-decision-option-c-2026-06-16]]

**下一棒**:6/23 周二全链路重启(沿本报告 §"7 阶段 checklist"· 跑阶段 1-4 必做,阶段 5-7 等用户授权 + 凭据 + 真 CSV);7/1 月度复盘(沿 [[v0.2.4]] §4 复盘 5 项);8/1 v0.2.1 release tag 锚定(沿 [[v0.2-release-notes-2026-06-22]] §"8 项前置条件")