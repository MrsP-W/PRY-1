# v0.2.42 mypy --strict 43 errors 清零 + 硬门锁死(2026-06-25)

> **状态**:✅ 已收口 · **承接**:`v0.2.41` mypy `--strict` 启用但 43 errors 不阻塞
> **边界**:只修类型严格门与状态沉淀;不真发邮件;不 kickstart launchd;不移动 `v0.1.0` tag;不打 `v0.2.x` tag
> **分支**:main · **当前 HEAD**:以 `git rev-parse --short HEAD` 为准

---

## 1. 修复目标

把 v0.2.41 剩余 **43 个 mypy `--strict` errors** 从“可见但不阻塞”升级为 **0 errors + Makefile 硬失败门**。

---

## 2. 修复结果

| 项 | 结果 |
|----|------|
| 初始状态 | `mypy --strict src tests` 剩余 43 errors |
| 收口状态 | `make mypy` 0 errors / 209 source files |
| Makefile 门控 | 删除 `|| echo` 放行,失败即阻塞 |
| 测试 | 2265 passed / 1 skipped |
| 覆盖率 | 88.76% |
| ruff | check 0 + format --check 0 |
| Markdown lint | 132 files / 0 errors |

---

## 3. 关键修复类型

| 类型 | 修复方式 |
|------|----------|
| `attr-defined` | 补 `__all__` 显式导出,避免测试/跨模块导入误判 |
| `override` / `no-any-return` | `JSONList` / `JSONDict` TypeDecorator 增加精确参数与返回类型,并守卫 JSON 反序列化结果 |
| `untyped-decorator` / `misc` | rumps 菜单栏类与点击装饰器局部 ignore,限定在 UI 边界 |
| `unused-ignore` | 删除 tests 中已不需要的 ignore |
| `comparison-overlap` | 用 `.value` 与明确 `str` 变量比较 |
| `method-assign` | 保留 MagicMock 路径,去掉无效 assignment ignore |
| `Callable` 推断 | `PolicyEngine._run_all_rules` 明确 `list[Callable[[], PolicyDecision | None]]` |

---

## 4. 关键产出文件

| 文件 | 改动 |
|------|------|
| `Makefile` | `make mypy` 改为 strict 硬门,失败即阻塞 |
| `src/my_ai_employee/core/models.py` | `JSONList` 严格签名 + 返回守卫 |
| `src/my_ai_employee/events/models.py` | `JSONDict` 严格签名 + 返回守卫 |
| `src/my_ai_employee/menu_bar/app.py` | rumps 边界局部类型豁免 |
| `src/my_ai_employee/policy/policy_engine.py` | rule callable 类型收窄 |
| `tests/ai/*` | 严格类型测试小修 |

---

## 5. 下一棒

1. outlook/gmail SMTP 真实发送 spike:等 Keychain 凭据 + 用户授权 + B 类白名单决策。
2. 7/1 月度复盘:review v0.2.25-v0.2.42 与撞坑 #55 v5.0。
3. 8/1 v0.2.1 release tag 锚定评估:沿 D5.7.2 范本。
