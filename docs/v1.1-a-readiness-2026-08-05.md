# v1.1-A 启动门槛核对(2026-08-05,D6.15.1 docs-only 重核)

> **范围声明(优先于正文)**:本文是 v1.1-A 启动候选前的 4 门槛重核文档,**不启动 v1.1-A、不修改 `src/`、`scripts/`、LaunchAgent、`ENABLE_*`**。
> **承接**:`docs/v1.1-a-readiness-2026-08-03.md`(基于 `a46fe02`);本文基于 `2f698802`(ahead origin/main 2);期间自然集成 4 提交(`7b6c0c1` SLO chain 收口、`41538b6` Feature Flag 审计、`f38b12d` Feedback schema 审计、`c1157cc` 密钥生命周期评估、`ce975f5` SLO active 实施前置清单、`69cbb2f`/`aefafde` D6.15.2 再核验、`cdf1042` v1.1-A 4 门槛核对、`2f69880` MD 314 sync)。
> **不发起 push/merge;不修改 13 项用户未跟踪 WIP;不触碰运行时/LaunchAgent/SMTP/真实数据**。

---

## 1. 4 必须门槛对照(2026-08-05 重核)

| # | 门槛 | 当前状态 | 评估 |
|---|------|---------|------|
| 1 | P3 7d unattended eligibility | elapsed ≈ 6.4/7 d;Day0=`2026-07-30T07:04:45.527698Z`;最新日报仍含 `health_unhealthy_sample` / `health_alert_opened` attention | **FAIL**:时间未到(明天 `2026-08-06T07:04:45.527698Z` 后才可能进入窗口);attention 非空 |
| 2 | P3 30d P0/P1-free eligibility | elapsed ≈ 6.4/30 d | **FAIL**:时间未到;`2026-08-29T07:04:45.527698Z` 后才可能 PASS |
| 3 | 评测样本 ≥ 30 条跨 suite | 30/30(`e502e40` 在当前 main `2f698802` 集成后) | **PASS**:3 suite(13/9/8),`tests/eval/test_eval_fixtures_schema.py` 仍 `129 passed` |
| 4 | Feature Flag + SLO + Feedback 3 design docs | SLO chain(`7b6c0c1`)+ Feature Flag audit(`41538b6`)+ Feedback schema(`f38b12d`)+ 密钥生命周期(`c1157cc`)+ SLO active 实施前置(`ce975f5`) | **PASS**:5/5 docs-only 已集成;较 8/3 多 +3(Feedback schema、密钥生命周期、SLO active 实施前置) |

**总判定**:**2 PASS / 2 FAIL**(时间 + attention);v1.1-A 启动候选**当前不可解锁**。

---

## 2. 候选门槛(不阻塞 v1.1-A 解锁)

| # | 候选 | 当前状态 |
|---|------|---------|
| 5 | 30+ 样本 2 轮扩 | 30→40 计划 docs-only 候选 `0bb7fba` 已审 `ready_to_merge`(本自动化上一轮,独立 worktree `/private/tmp/my-ai-employee-ai-agent-d6181-fixture-30-40-plan-20260805`,分支 `codex/ai-agent-d6181-fixture-30-40-plan-20260805`);**未集成到 main**;轮 1 任务包 `TASK-20260805-002` 待起草 |
| 6 | 撞坑 #104+ 全沉淀 | `pitfall-107` 已落地;D6.11.3 / D6.14.1 / D6.15.3 / D6.17.3 均已确认区间内无可识别新坑(`e72e034`) |
| 7 | v1.0 tag 评估 docs | D6.18.3 `57480cb` 已落地 |
| 8 | P3 epoch rollover 脚本 docs | `scripts/p3_rollover_epoch.py` + `tests/scripts/test_p3_rollover_epoch.py` 仍 WIP(未集成);需补设计说明后才进入审计 |

---

## 3. 关键状态变化(相比 8/3 报告)

* 门槛 #3:维持 PASS(30 条 fixture 仍 `129 passed`)。
* 门槛 #4:从「3 docs-only 已审待集成」升级到「5 docs-only 已集成」;原候选 `11f92ef`/`eba1050`/`69cbb2f`/`aefafde`/`ce975f5` 均 cherry-pick 到 `2f698802`。
* 门槛 #1:时间窗接近解锁(`2026-08-06T07:04:45.527698Z` ≈ +17 小时);但 attention 未消,即使时间到也仍 FAIL。
* 门槛 #2:30d 时间窗未到;预计 `2026-08-29T07:04:45.527698Z` 后。
* 新增观察:`com.myaiemployee.agent` 非零退出 + 143/143 不健康样本根因仍 `needs_human`;attention 类型最近 7d 仍 `health_unhealthy_sample` / `health_alert_opened`;Day0 重置未触发。

---

## 4. 风险与未决项

* R1:`com.myaiemployee.agent` 非零退出 + 143/143 不健康样本的根因仍 `needs_human`;7d eligibility 重新核验前必须先授权处理。这是 v1.1-A 解锁的**硬卡点**,与时间窗无关。
* R2:30d 计时受 P3 epoch 起始时间绑定;若发生 rollover,30d 窗口随新 Day0 重置。
* R3:v1.1-A 解冻需 P3 4 必须 + Feature Flag 接口解冻条件(`docs/v1.1-feature-flag-interface.md` §8)同时满足;本文未触发任何 `ENABLE_*` 切换、未写 `flags` 表、未启动 SMTP / Notes 真同步。
* R4:30→40 计划 docs-only 候选 `0bb7fba` 已审 ready_to_merge;但 cherry-pick 集成 + push 仍需用户单独批准;**本文不擅自集成、不 push**。
* R5:本文不发起任何 `ENABLE_*` 切换、不写 `flags` 表、不启动 SMTP / Notes 真同步;不修改 13 项用户未跟踪 WIP。

---

## 5. 解锁 v1.1-A 的最小步骤(顺序,本轮均不实施)

1. **用户授权处理 `com.myaiemployee.agent` 根因**;新 epoch 启动后重新核验 7d attention。
2. 等待 `2026-08-29T07:04:45.527698Z` 后 30d 计时满足(若 Day0 未重置)。
3. 用户对 Feature Flag 接口解冻条件(持久化方案 / `ENABLE_*` 兼容层 / 运行时调用点)逐项授权。
4. 在独立 worktree 起草 v1.1-A 启动候选 task-contract;Codex 验收后再合并。
5. 用户单独批准 30→40 计划 `0bb7fba` cherry-pick;批准后起 `TASK-20260805-002`(轮 1,5 条 fixture)。

---

## 6. 验收(本文档)

* Markdown lint:0 issue(`/Users/wei/.n/bin/markdownlint docs/v1.1-a-readiness-2026-08-05.md`)。
* 任务包 `status: ready_to_merge`;`changed_files` 与 `git diff --name-status` 完全一致(3 文件白名单)。
* `git diff --check` 通过。
* 不触碰运行时、tag、远程、`ENABLE_*`、13 项用户未跟踪 WIP。
* 主工作树 `main = 2f698802`;ahead origin/main 2;本文在独立 worktree,未集成、未 push。

---

## 7. 下一棒

* 用户决定是否集成 `0bb7fba`(30→40 计划 docs-only)→ 批准后起 `TASK-20260805-002`(轮 1,5 条 fixture)。
* 用户单独批准 push → ahead 2 同步到 origin/main。
* 持续观察 P3 7d 时间窗(`2026-08-06T07:04:45.527698Z`)和解锁条件;本日 windows 已接近但**attention 根因未解,不解锁**。
* 等待 `com.myaiemployee.agent` 根因处理结果;**未处理前不解锁**。
