# v1.1-A 启动候选上线 plan(2026-08-05 起草,D6.15.5 docs-only)

> **范围声明(优先于正文)**:本文是 v1.1-A 启动候选的端到端上线 plan,**不启动 v1.1-A、不修改 `src/`、`scripts/`、LaunchAgent、`ENABLE_*`、不接入真实数据、不 push**;仅为后续 cherry-pick / SOL 审核 / Feature Flag 接口解冻条件 / 启动候选 task-contract 起草提供统一路线图。
> **承接**:`docs/v1.1-a-readiness-2026-08-03.md`(基于 `a46fe02`)+ `docs/v1.1-a-readiness-2026-08-05.md`(基于 `2f698802`,本自动化上一轮 `1a7dec1` ready_to_merge);候选链 `0bb7fba → 1a7dec1 → 6272eb8` 均 ready_to_merge,待用户单独批准 cherry-pick。
> **关键节点**:`2026-08-06T07:04:45.527698Z`(P3 7d 时间窗入口)/ `2026-08-08`(用户预期 GPT 额度恢复,启用 SOL 审核)/ `2026-08-29T07:04:45.527698Z`(P3 30d 时间窗入口,若 Day0 未重置)。
> **硬卡点**:`com.myaiemployee.agent` 非零退出 + 143/143 不健康样本根因仍 `needs_human`;不解锁 v1.1-A。

---

## 1. 阶段总览

| 阶段 | 时间窗 | 范围 | 主导 Agent | 触发条件 |
|------|--------|------|-----------|----------|
| A 前置准备 | 8/5 - 8/7 | docs-only / tests-only / 只读核验 | LUNA(max)/ M3(若可桥接) | 用户「重启」即持续 |
| B 解封条件 | 异步(用户处理根因后) | 根因处理 + 新 epoch + 7d/30d 重核 | Codex + 用户 | 用户处理 com.myaiemployee.agent 根因 |
| C SOL 审核 | 8/8(GPT 额度恢复后) | 跨模块 cross-review readiness + 候选链 + FF 解冻条件 | SOL(gpt-5.6-sol, ultra) | GPT 额度恢复证据(API `quota_exhausted` 反向证明) |
| D Feature Flag 接口解冻 | 8/8 - 8/29 | 持久化方案 + `ENABLE_*` 兼容层 + 运行时调用点 | 用户授权 | 用户对解冻条件逐项拍板 |
| E 启动候选 task-contract | 8/29 后 | task-contract 起草 + Codex + SOL 双验收 | Codex → SOL | 4 必须门槛 PASS + D 完成 |
| F push / tag | E 验收后 | cherry-pick 集成 + push + v1.1-A tag | 用户单独批准 | E 状态 `ready_to_merge` + 用户授权 push |

---

## 2. 阶段 A:前置准备(8/5 - 8/7,docs-only / tests-only / 只读)

### A1. 落地本上线 plan 文档(本轮完成,`1a7dec1` 已就绪)

* 产出:`docs/v1.1-a-launch-plan-2026-08-05.md`(本文件)
* 产出:`docs/agent-team/tasks/TASK-20260805-004-d6155-v11-a-launch-plan.yaml`
* 独立 worktree:`/private/tmp/my-ai-employee-ai-agent-v11-a-launch-plan-20260805`
* 分支:`codex/ai-agent-v11-a-launch-plan-20260805`

### A2. 候选门槛 #8:P3 epoch rollover 脚本 docs-only 只读审计(8/6 或 8/7)

* 对象:`scripts/p3_rollover_epoch.py` + `tests/scripts/test_p3_rollover_epoch.py`(均 WIP)
* 范围:只读审计脚本设计、与 v1.1-A 7d/30d 时间窗绑定关系、隐私边界、测试覆盖
* 产出:审计报告 + 任务包 + `MODIFICATION-LOG.md` 追加
* 不集成、不修改脚本;仅 docs-only

### A3. 候选链 cherry-pick 集成(待用户单独批准,优先级最高)

* 候选链:`0bb7fba`(30→40 计划)→ `1a7dec1`(readiness 重核)→ `6272eb8`(轮 1 任务包,depends_on `0bb7fba`)
* 顺序:**必须先集成 `0bb7fba`,再集成 `6272eb8`;`1a7dec1` 独立可单独集成**
* 验收:每次集成后 `tests/eval/test_eval_fixtures_schema.py` 仍 `129 passed`(不变);主工作树仍 `main = 2f698802`;13 项 WIP 不变
* push:每次集成后用户单独批准 `git push origin main`

### A4. P3 7d 时间窗进入观察(8/6 07:04 UTC)

* 时点:`2026-08-06T07:04:45.527698Z` ≈ 现在 + 14 小时
* 进入后单独核验:7d 时间窗口是否 PASS、attention 是否仍非空
* 注意:即使时间到,attention 未消,门槛 #1 仍 FAIL;不解锁 v1.1-A

### A5. 30→40 扩样轮 1 实施准备(用户明确批准后)

* 任务包:`TASK-20260805-003-d6181-fixture-batch1`(ready_to_merge,depends_on `0bb7fba`)
* 5 条 fixture JSON 文件 + `MODIFICATION-LOG.md` 更新
* 沿用 `b019043`/`056efaa` guards;ID 前缀续编
* pytest 预期:30→35 增加 ~25 assertions,基线 `129 passed` → `≥ 154 passed`

### A6. 30→40 扩样轮 2 任务包起草(8/6 - 8/7)

* 任务包:`TASK-20260805-005-d6181-fixture-batch2.yaml`(本 plan 起草后另起 worktree)
* 5 条 fixture 设计:对抗混淆(伪 CEO 付款)、PII 自报、催办+礼貌边界、FB60+FBL5N 跨模块、权限报头
* 与轮 1 同结构(depends_on `0bb7fba`)

---

## 3. 阶段 B:解封条件(异步,用户处理根因后)

### B1. com.myaiemployee.agent 根因处理

* 触发:用户在独立 worktree 单独处理
* 现象:非零退出 + 143/143 不健康样本 + `health_unhealthy_sample` / `health_alert_opened` attention
* 不在本 plan 范围内;仅记录触发条件

### B2. 新 epoch 启动

* 触发:用户授权
* 影响:7d/30d 时间窗随新 Day0 重置;Day0=`新 epoch 启动时间`
* 后续:重新核验 7d attention

### B3. P3 30d 时间窗入口

* 时点(若 Day0 未重置):`2026-08-29T07:04:45.527698Z`
* 若 Day0 已重置:30d 窗口从新 Day0 算起

---

## 4. 阶段 C:SOL 审核(8/8, GPT 额度恢复后)

### C1. SOL 启用条件

* 触发证据:用户确认 GPT 额度恢复(或 API 配额监控显示 `quota_exhausted` 反向解除)
* 启用路径:`SOL = gpt-5.6-sol, ultra`;主 Agent 仅在阶段 C 唤醒 SOL,其他阶段 SOL 槽位保持 `ROLE_UNAVAILABLE`
* 模型:`SOL` 唤醒后调用 1-2 次;首次 cross-review,若发现 critical issue 允许第 2 次复核

### C2. SOL 审核范围

* **跨模块 cross-review**:5 docs-only design docs 集成完整性
* **候选链集成 sanity**:`0bb7fba → 1a7dec1 → 6272eb8` 集成后的 diff 一致性
* **Feature Flag 接口解冻条件**:持久化方案(Q1)/ 健康度阈值(Q4)等决策是否充分
* **v1.1-A 启动候选 readiness**:4 必须门槛最终判定
* **30→40 扩样**:轮 1 实施后 guards 兼容性,或任务包就绪状态

### C3. SOL 审核产物

* 独立 review worktree(基于集成后的 main)
* 报告:`tests/eval/audit/sol-v11-a-launch-review-20260808.md`
* 状态:`PASS` / `PASS_WITH_FINDINGS` / `FAIL`
* 若 `FAIL`:回退到阶段 A 修复,不进入阶段 D

### C4. 8/8 之前不可用时的降级

* 若 8/8 之前 GPT 额度仍未恢复:保持 `SOL_BLOCKED`;不调用 SOL
* 阶段 A 继续 docs-only 准备,不等 SOL
* 解封条件(阶段 B)独立推进,不依赖 SOL

---

## 5. 阶段 D:Feature Flag 接口解冻(用户授权)

### D1. 持久化方案选定(Q1)

* 选项 A:JSON 文件 + 文件锁
* 选项 B:SQLCipher(独立加密 SQLite)
* 风险:JSON 易读但弱加密;SQLCipher 强但需新增依赖
* 决策:用户单独拍板;非 docs-only

### D2. ENABLE_* 兼容层

* 现有:`ENABLE_*` 命名约定
* 解冻:从「命名占位」升格为「运行时开关」
* 边界:不在 docs-only 范围;需 P2 代码骨架候选

### D3. 运行时调用点注入

* 现有:零调用点(因 Feature Flag 仍 design-only)
* 解冻:选择 1-3 个最小调用点(如 `p3_health_check.py`、`p3_attention.py`)
* 边界:不动 `src/`、`scripts/`、`ops/` 的运行时,需独立 worktree + Cherry-pick 审批

### D4. 集成顺序

1. D1 选定 → D2 设计 docs
2. D2 验收 → D3 起草 P2 代码骨架候选
3. D3 集成到 main → 解冻完成

---

## 6. 阶段 E:v1.1-A 启动候选 task-contract(8/29 后)

### E1. 触发条件(全部满足)

1. 4 必须门槛 PASS(7d 时间到 + attention 消 + 30-fixture + 5 docs-only)
2. Feature Flag 接口解冻完成(阶段 D)
3. SOL 审核 PASS(阶段 C)
4. 30→40 扩样已完成(阶段 A5 + A6)或豁免

### E2. task-contract 内容

* 范围:启动候选合并路径、commit 链、运行时启用顺序、回滚步骤
* 验证:Codex 验收 + SOL 复核双门
* 状态:`ready_to_merge`

### E3. 验收后合并

* 用户单独批准 cherry-pick
* 用户单独批准 push
* 用户单独批准 v1.1-A tag(若适用)

---

## 7. 阶段 F:push / tag(E 完成后)

### F1. push 集成

* `git push origin main`(用户单独批准)
* 验证:`main...origin/main = 0/0`;13 项 WIP 保留

### F2. v1.1-A tag(若适用)

* `git tag -a v1.1-A -m "..."`(用户单独批准)
* `git push origin v1.1-A`(用户单独批准)
* 不自动 push;不自动创建 tag

---

## 8. 风险与红线

### 风险

* R1(硬卡点):`com.myaiemployee.agent` 根因未处理不解锁 v1.1-A;阶段 B 是必经路径
* R2:GPT 额度恢复时间不可控;若 8/8 未恢复,阶段 C 推迟,阶段 D/E 顺延
* R3:30d 时间窗(8/29)距今 ≈ 24 天;期间任何 P3 epoch rollover 会重置窗口
* R4:Feature Flag 接口解冻(阶段 D)需用户拍板 JSON vs SQLCipher、ENABLE_* 兼容层、运行时调用点;每个决策都需独立授权
* R5:30→40 扩样轮 1 实施(阶段 A5)需用户明确批准写 fixture JSON;不擅自落地
* R6:候选链集成(阶段 A3)需用户对每个提交单独批准 cherry-pick
* R7:SOL 审核依赖 GPT 额度;额度未恢复时保持 `SOL_BLOCKED`,不调用 SOL

### 红线(本 plan 不触发)

* 不修改 `src/`、`scripts/`、`ops/` 下的运行时/配置
* 不接入真实数据 / SMTP / Notes 真同步
* 不启用任何 `ENABLE_*`
* 不自动 push、不自动 tag、不自动合并
* 不修改 13 项用户未跟踪 WIP
* 不擅自集成候选;每次集成需用户单独批准
* 不调用未授权的外部三 Agent

---

## 9. 验收(本 plan 文档)

* `markdownlint docs/v1.1-a-launch-plan-2026-08-05.md`:0 issues
* `python -c "import yaml; yaml.safe_load(open('docs/agent-team/tasks/TASK-20260805-004-d6155-v11-a-launch-plan.yaml'))"`:解析通过
* `git diff --check`:通过
* 文件白名单:3 文件全部为 docs-only(plan 文档 + 任务包 + `MODIFICATION-LOG.md`)
* 不触碰运行时、tag、远程、`ENABLE_*`、13 项用户未跟踪 WIP
* 主工作树 `main = 2f698802`;ahead origin/main 2;本文在独立 worktree,未集成、未 push

---

## 10. 下一棒

* 用户决定是否集成候选链 `0bb7fba → 1a7dec1 → 6272eb8`(优先级最高)
* 用户决定是否集成本 plan 文档(本轮 `1a7dec1` ready_to_merge 的姊妹篇)
* 用户决定是否启动候选门槛 #8 审计(阶段 A2,docs-only / 只读)
* 持续观察 P3 7d 时间窗(8/6 07:04 UTC)与 attention 状态
* 等待 GPT 额度恢复(预计 8/8)以启用 SOL 阶段 C 审核
* 等待 `com.myaiemployee.agent` 根因处理(阶段 B,异步)
