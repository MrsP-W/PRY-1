# v0.2.13 6/23 全链路重启实战手册(端午不休息第 2 天 · 撞坑恢复 3 步实战演练 7)

> **项目**:我的AI员工
> **文档时间**:2026-06-20 端午连休第 2 天(周六)
> **承接**:[[v0.2.10-full-restart-checklist-2026-06-22]] §"7 阶段 checklist" + [[v0.2.11-7-stage-dry-run-2026-06-20]] §"4 个 dry-run 验证点" + [[v0.2.12-6-23-restart-prep-2026-06-20]] §"5 step 实战预演" + [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 + [[2026-06-18-venv-sigkill-137-false-alarm]] SIGKILL 137 误报范本
> **范围**:**6/23 全链路重启实战手册 docs-only** — 1 commit · docs/v0.2.13-6-23-restart-playbook-2026-06-20.md 新建 · SESSION-STATE/MODIFICATION-LOG/README 同步 · **不真发邮件 · 不真导入账单 · 不真启动菜单栏 · 不真 kickstart launchd · 不移动 v0.1.0 tag**
> **撞坑恢复 3 步实战演练**:本报告作为 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本的**实战演练 7**(演练 1-6 沿 v0.2.5-v0.2.12 + 演练 7 = v0.2.13 实战手册 · 本次)
> **维护者**:Mr-PRY · **模型**:MiniMax-M3

---

## 🎯 文档目标

把 [[v0.2.10]] + [[v0.2.11]] + [[v0.2.12]] 3 个阶段 checklist 整合成"6/23 全链路重启实战手册",含每阶段精确命令 + 预期输出 + 撞坑处理 + SIGKILL 137 误报应对,作为 6/23 周二全链路重启时的**最终实战手册**。

**关键约束**(沿用户 2026-06-20 决策):
- ❌ **不真发邮件**(本报告纯 docs-only)
- ❌ **不真导入账单**(本报告纯 docs-only)
- ❌ **不真启动菜单栏**(本报告纯 docs-only)
- ❌ **不真 kickstart launchd**(本报告纯 docs-only)
- ❌ **不移动 `v0.1.0` tag**(继续锚定 `2af775f`,沿 D5.7.2 范本)
- ❌ **不打 `v0.2.0/v0.2.1` tag`(留作 8/1 锚定策略)
- ❌ **不跑真实 OAuth flow**(等用户授权 + 凭据)
- ❌ **不在没有 CSV 的情况下造"真账单"结论**

## 📋 7 阶段实战手册(2026-06-23 周二 · 端午连休后第一天)

### 阶段 1:环境准备

#### 1.1 触发条件(全部满足才执行)

- ✅ 用户授权"今天跑全链路重启"
- ✅ 项目根目录 `/Users/wei/Documents/DesktopOrganizer/我的AI员工`
- ✅ git status clean(如有未提交,先 `git stash` 或 `git commit`)
- ✅ Python 3.12 + uv 已安装

#### 1.2 精确命令

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 1.1 检查 git 状态
git status  # 应输出:On branch main / nothing to commit, working tree clean

# 1.2 拉最新代码(可选)
git pull  # 如果用户远程有 push

# 1.3 安装依赖(uv sync --extra dev + pip install -e .)
make install

# 1.4 创建数据目录
mkdir -p data/

# 1.5 创建 .env 文件(从 .env.example 复制)
cp .env.example .env

# 1.6 编辑 .env 文件(填入实际凭据)
# 注意:.env 已在 .gitignore 中,不会提交
# 编辑 .env 填入 SQLCIPHER_KEY / SMTP_REAL_NETWORK 等
nano .env  # 或 vim .env
```

#### 1.3 预期输出

```bash
# git status
On branch main
nothing to commit, working tree clean

# make install
Resolved N packages in X.XXs
Installed N packages in X.XXs
... (uv sync 输出)

# mkdir data/
mkdir: created directory 'data/'  # 或静默成功(目录已存在)

# cp .env.example .env
# 静默成功(无输出 = 成功)
```

#### 1.4 撞坑处理

| 撞坑 | 表现 | 应对 |
|------|------|------|
| git status 不 clean | 有未提交修改 | 先 `git stash` 或 `git commit`,再继续 |
| uv sync 失败 | 依赖冲突 | 检查 `uv.lock` 是否过期,跑 `uv lock --upgrade` 或回滚到上次成功的 lock |
| .env 已存在但内容过期 | cp 失败 | 检查现有 .env 是否需要更新 SQLCIPHER_KEY |
| SQLCIPHER_KEY 缺失 | 后续阶段会失败 | 必须填入(随机生成 32 字节字符串)|

#### 1.5 下一阶段门槛

- ✅ `data/` 目录存在且可读写(`drwxr-xr-x`)
- ✅ `.env` 文件存在且含 `SQLCIPHER_KEY`
- ✅ `make install` 成功(`uv sync` 无错误)

---

### 阶段 2:8/8 质量门 baseline 验证

#### 2.1 触发条件

- ✅ 阶段 1 完成
- ✅ 依赖安装成功

#### 2.2 精确命令

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 2.1 make lint(沿 v0.2.11 baseline · 108 files / 0 errors)
make lint

# 2.2 pytest 完整测试集(沿 v0.2.6 baseline · 2225 passed / 1 skipped / 88.85% coverage)
make test

# 2.3 ruff check(本轮实跑已确认 ✅ All checks passed)
uv run ruff check src tests

# 2.4 ruff format(本轮实跑已确认 ✅ 221 files already formatted)
uv run ruff format --check src tests

# 2.5 mypy src(沿 v0.2.11 baseline · Success 101 files 0 errors)
uv run mypy src

# 2.6 alembic --sql(沿 v0.2.11 baseline · exit 0)
uv run alembic upgrade head --sql

# 2.7 uv build(沿 v0.2.11 baseline · success)
uv build

# 2.8 coverage ≥ 80%(沿 v0.2.11 baseline · 88.85% ≥ 80%)
# 沿 pytest --cov=src 报告自动生成
```

#### 2.3 预期输出

```bash
# make lint
Linting: 109 file(s)
Summary: 0 error(s)
✅ 0 错误

# make test
=========================== short test summary info ============================
SKIPPED [1] tests/e2e/test_v0_1_s5_real_smtp.py:28: S5 真实 SMTP 需 SMTP_REAL_NETWORK=1 env
2225 passed, 1 skipped in ~31s

# ruff check
All checks passed!

# ruff format
221 files already formatted

# mypy src
Success: no issues found in 101 source files

# alembic --sql
... (输出迁移 SQL · exit 0)

# uv build
Building wheel from source distribution...
Successfully built dist/my_ai_employee-0.1.0.tar.gz
Successfully built dist/my_ai_employee-0.1.0-py3-none-any.whl

# coverage
TOTAL coverage: 88.85% ≥ 80% ✅
```

#### 2.4 撞坑处理(SIGKILL 137 误报)

| 撞坑 | 表现 | 应对 |
|------|------|------|
| **SIGKILL 137 误报** | mypy src / pytest exit 137 | **沿 [[2026-06-18-venv-sigkill-137-false-alarm]] 范本 · 不重试 · 沿用 baseline** |
| pytest 真失败 | 某 test failed | 检查失败原因(沿 [[v0.2.4-drift-review-mechanism]] 状态漂移审查机制)|
| coverage < 80% | 触发 fail_under | 不应发生(沿 baseline 88.85%) |

**SIGKILL 137 误报处理详细步骤**:

1. **确认是误报**:检查 `dmesg | grep -i "out of memory"` 是否有 OOM
2. **不重试**:避免上下文爆炸,沿用 baseline 结果
3. **标记为已知撞坑**:写入 v0.2.4 撞坑史(本轮 SIGKILL 137 已记录)
4. **7/1 月度复盘**:撞坑恢复范本应用情况统计(累计 6 + 本次 = 7 个实战演练)

#### 2.5 下一阶段门槛

- ✅ 8/8 质量门全绿(或 SIGKILL 137 误报已沿用 baseline)
- ✅ pytest 2225 passed / 1 skipped
- ✅ coverage ≥ 80%

---

### 阶段 3:launchd kickstart + plist 安装

#### 3.1 触发条件

- ✅ 阶段 2 完成
- ✅ 8/8 质量门全绿
- ⚠️ **本次脚本本身不真发邮件,只 kickstart launchd**(沿 [[d10.5.4-launchd-kickstart-and-seal]] 范本)

#### 3.2 精确命令

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 3.1 检查 launchd_kickstart_and_seal.sh 5 源文件存在性
test -f scripts/monthly_report.py && echo "✅ monthly_report.py 存在"
test -f scripts/launchd_install.sh && echo "✅ launchd_install.sh 存在"
test -f scripts/launchd_kickstart_and_seal.sh && echo "✅ launchd_kickstart_and_seal.sh 存在"
test -f scripts/launchd_uninstall.sh && echo "✅ launchd_uninstall.sh 存在"
test -f Makefile && echo "✅ Makefile 存在"

# 3.2 检查脚本 bash 语法
bash -n scripts/launchd_kickstart_and_seal.sh && echo "✅ bash 语法 OK"

# 3.3 实跑 kickstart + 5 源判定
bash scripts/launchd_kickstart_and_seal.sh
```

#### 3.3 预期输出

```bash
# 5 源文件存在性
✅ monthly_report.py 存在
✅ launchd_install.sh 存在
✅ launchd_kickstart_and_seal.sh 存在
✅ launchd_uninstall.sh 存在
✅ Makefile 存在

# bash 语法
✅ bash 语法 OK

# launchd_kickstart_and_seal.sh
===== launchd_kickstart_and_seal =====
目标 plist:/Users/wei/Library/LaunchAgents/com.myaiemployee.agent.plist
5 源判定:
  - monthly_report.py ✅
  - plist ✅
  - 已部署脚本 ✅
  - release notes ✅
  - 3 项验证 ✅

预期退出码:
- exit 0(成功)
- exit 1(源缺失)
- exit 2(业务失败 · 7/1 自然触发,预期)
- exit 3(技术失败)
```

**6/23 预期**:沿 [[d10.5.3-p3-1]] 锁定 `plist --month 2026-07`,6/23 kickstart 必 0 笔交易 → `exit 2`(预期业务失败契约,7/1 自然触发)

#### 3.4 撞坑处理

| 撞坑 | 表现 | 应对 |
|------|------|------|
| exit 1(源缺失)| 某文件不存在 | 检查 git pull / make install |
| exit 2(业务失败)| 0 笔交易 | 预期,7/1 自然触发 |
| exit 3(技术失败)| kickstart 失败 | 检查 launchctl list + plist 字面值 |
| plist 已存在但状态异常 | launchctl list 状态非预期 | `launchctl unload ~/Library/LaunchAgents/com.myaiemployee.agent.plist` 然后重试 |

#### 3.5 下一阶段门槛

- ✅ kickstart exit 0 / 2(预期)· exit 3 / 1 需修复后重试
- ✅ plist 安装成功(若 exit 0)或保留之前状态(若 exit 2)

---

### 阶段 4:菜单栏启动

#### 4.1 触发条件

- ✅ 阶段 3 完成
- ✅ launchd kickstart exit 0 或 2

#### 4.2 精确命令

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 4.1 检查菜单栏依赖(沿 v0.2.11 dry-run 已确认)
uv run python -c "from my_ai_employee.menu_bar import app; print('✅ menu_bar import 成功')"
uv run python -c "import rumps; print(f'✅ rumps version: {rumps.__version__}')"
uv run python -c "import Quartz; print('✅ Quartz import 成功')"

# 4.2 启动菜单栏
make menu-bar

# 或后台启动
nohup python -m my_ai_employee.menu_bar.app > data/menu_bar.log 2>&1 &
```

#### 4.3 预期输出

```bash
# 依赖检查
✅ menu_bar import 成功
✅ rumps version: 0.4.0
✅ Quartz import 成功

# make menu-bar
# 菜单栏图标出现在 macOS 顶部
# 首次跑触发 TCC 弹窗(沿 [[apple-script-pitfalls]])
```

#### 4.4 撞坑处理

| 撞坑 | 表现 | 应对 |
|------|------|------|
| TCC 拦截 | macOS 弹窗"是否允许..." | 点"好"授权(沿 [[apple-script-pitfalls]] TCC 一次性授权)|
| menu_bar import 失败 | ModuleNotFoundError | 检查 uv sync + rumps / Quartz 是否安装 |
| 菜单栏图标不出现 | nohup 启动但无 UI | 检查 `data/menu_bar.log` 错误日志 |
| Quartz 权限不足 | macOS Sequoia 拦截 | 沿 [[apple-script-pitfalls]] §"D9.5 TCC 双层防御" 处理 |

#### 4.5 下一阶段门槛

- ✅ 菜单栏图标出现
- ✅ TCC 授权完成(若弹窗)
- ✅ `data/menu_bar.log` 无 ERROR 日志

---

### 阶段 5:Apple Notes 同步(可选)

#### 5.1 触发条件

- ✅ 阶段 4 完成
- ✅ 菜单栏启动成功
- ⚠️ **本阶段可选**(用户可手动触发或跳过)

#### 5.2 精确命令

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 5.1 dry-run 验证(可选,本轮已完成)
# uv run python scripts/sync_notes.py --dry-run

# 5.2 真实同步(用户手动触发)
NOTES_REAL_NETWORK=1 uv run python scripts/sync_notes.py sync --max-rows 1
```

#### 5.3 预期输出

```bash
# sync_notes.py sync --max-rows 1
D9.2 Apple Notes 同步 CLI(子命令 sync / spike + 4 退出码契约)
===== sync_notes =====
真实 AppleScript 调 Notes.app(需显式 env 解锁)
目标:1 行
执行 AppleScript...
✅ 同步成功

# NoteStore 状态机化:NEW → STRUCTURED
# 输出日志:Apple Notes 同步完成 · NoteStore 状态机化 NEW → STRUCTURED
```

#### 5.4 撞坑处理

| 撞坑 | 表现 | 应对 |
|------|------|------|
| AppleScript 权限拦截 | TCC 弹窗 | 沿 [[apple-script-pitfalls]] 一次性授权 |
| Notes.app 未启动 | AppleScript 调失败 | 手动打开 Notes.app 后重试 |
| sync_notes exit 4 | 技术失败 | 检查 AppleScript 输出 + Notes.app 状态 |

#### 5.5 下一阶段门槛

- ✅ Apple Notes 同步完成(或用户跳过本阶段)
- ✅ NoteStore 状态机化 NEW → STRUCTURED(若成功)

---

### 阶段 6:W3 真账单 spike(等用户提供真实 CSV)

#### 6.1 触发条件

- ✅ 阶段 4 完成
- ⚠️ **本阶段阻塞于用户手动导出真实微信/支付宝 CSV**(沿 [[docs/微信账单导出教程]] 8 步 / [[docs/支付宝账单导出教程]] 5 步)
- ⚠️ **不在没有 CSV 的情况下造"真账单"结论**(沿用户 6/22 决策)

#### 6.2 用户手动导出 CSV

```bash
# 微信账单导出(8 步 · 沿 [[docs/微信账单导出教程]])
# 1. 微信 App → 我 → 服务 → 钱包 → 账单 → 右上角 ··· → 账单下载
# 2. 选择时间范围(近 1 个月 / 3 个月 / 自定义)
# 3. 邮箱接收 → 解压 ZIP
# 4. 重命名:wechat_2026_sample.csv

# 支付宝账单导出(5 步 · 沿 [[docs/支付宝账单导出教程]])
# 1. 支付宝 App → 我的 → 账单 → ··· → 开具交易流水证明
# 2. 选择时间范围(按月,1-3 个月)
# 3. 选择 CSV 格式 + 邮箱接收
# 4. 解压 ZIP → 重命名:alipay_2026_sample.csv
```

#### 6.3 精确命令(用户提供 CSV 后)

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 6.1 沿 [[v0.2.9-w3-real-bill-spike-prep-2026-06-22]] §"启动命令范本"
export WECHAT_REAL_IMPORT=1

# 微信账单 spike 30 笔
uv run python scripts/spike_real_bill.py \
  --csv /Users/wei/Documents/账单/wechat_2026.csv \
  --count 30 \
  --confirm yes-i-understand-this-imports-real-bill

# 或支付宝账单 spike 30 笔
uv run python scripts/spike_real_bill.py \
  --csv /Users/wei/Documents/账单/alipay_2026.csv \
  --count 30 \
  --confirm yes-i-understand-this-imports-real-bill

# 或混合账单 spike 50 笔
uv run python scripts/spike_import_combined_real.py \
  --wechat-csv /Users/wei/Documents/账单/wechat_2026.csv \
  --alipay-csv /Users/wei/Documents/账单/alipay_2026.csv \
  --wechat-count 30 --alipay-count 20 \
  --confirm yes-i-understand-this-imports-real-bill
```

#### 6.4 预期输出

```bash
# spike_real_bill.py
W3 真账单 spike 启动
4 重防误发门控:
  - WECHAT_REAL_IMPORT=1 env 门控 ✅
  - 强制 1 批 --count 30 ✅
  - 二次确认口令 ✅
  - CSV 文件名校验 + 列名嗅探 ✅

加载 CSV...
CSV 解析器 detect_version 自动选 parser(2024/2025/2026)

30 笔入库成功
- fingerprint SHA-256 + 3 层去重(沿 D6.2)
- categorizer 5 类分类(沿 D6.3)
- D8.5 异常检测(真异常误报率 < 5%)

性能 < 1ms / 笔
```

#### 6.5 撞坑处理

| 撞坑 | 表现 | 应对 |
|------|------|------|
| WECHAT_REAL_IMPORT 未设 | RuntimeError "必须显式设置..." | `export WECHAT_REAL_IMPORT=1` |
| CSV 文件不存在 | FileNotFoundError | 检查路径 / 重新导出 |
| 2026 版 parser 缺失 | NotImplementedError | 沿 [[v0.2.9]] §"Checklist 3",补 parser 或用 2024/2025 版 |
| 真异常误报率高 | D8.5 阈值问题 | 沿 D8.5.1-3 修复链 |

#### 6.6 下一阶段门槛

- ✅ 30 笔真实导入成功
- ✅ 真异常误报率 < 5%(沿 D8.5.4 baseline)
- ✅ 性能 < 1ms / 笔

---

### 阶段 7:outlook/gmail SMTP 真实 spike(等用户授权 + 凭据)

#### 7.1 触发条件

- ✅ 阶段 4 完成
- ⚠️ **本阶段阻塞于用户授权 + Outlook/Gmail 凭据 + B 类决策(provider 白名单扩展)**
- ⚠️ **不真实 OAuth flow**(等用户手动跑 OAuth flow 拿 access_token + refresh_token)

#### 7.2 用户授权 + 凭据准备

```bash
# 7.1 用户授权(口头)"今天跑 outlook/gmail SMTP 真实 spike 1 封"
# 7.2 提供凭据:
#   - Outlook:smtp.office365.com:587 + outlook 邮箱 + App Password 或 OAuth client_id/secret
#   - Gmail:smtp.gmail.com:587 + Gmail 邮箱 + OAuth client_id/secret
# 7.3 凭据写入 Keychain
uv run python scripts/spike_set_smtp_password.py \
  --provider outlook \
  --email <outlook邮箱> \
  --set-password <App Password 或 auth_code>

# 或 OAuth 方式(沿 v0.2.2 #5 commit 4)
# uv run python scripts/oauth_flow_microsoft.py ...
# uv run python scripts/oauth_flow_google.py ...
```

#### 7.3 精确命令

```bash
cd /Users/wei/Documents/DesktopOrganizer/我的AI员工

# 7.1 沿 [[v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21]] §"启动命令范本"
export SMTP_REAL_NETWORK=1

# Outlook SMTP 真实 spike 1 封
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.office365.com \
  --smtp-port 587 \
  --smtp-username <outlook邮箱> \
  --smtp-provider outlook \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1

# Gmail SMTP 真实 spike 1 封
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.gmail.com \
  --smtp-port 587 \
  --smtp-username <gmail邮箱> \
  --smtp-provider gmail \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1

# QQ SMTP 真实 spike 1 封(已就绪)
uv run python scripts/spike_send_100.py --real \
  --smtp-host smtp.qq.com \
  --smtp-port 465 \
  --smtp-username <qq邮箱> \
  --smtp-provider qq \
  --recipient <收件人邮箱> \
  --max-recipients 1 \
  --confirm yes-i-understand-this-sends-real-email \
  --count 1
```

#### 7.4 预期输出

```bash
# spike_send_100.py --real
SMTP 真实发送 spike 启动
5 重防误发门控:
  - SMTP_REAL_NETWORK=1 env 门控 ✅
  - provider 白名单(outlook/gmail/qq)✅
  - Keychain round-trip ✅
  - 强制 1 收件人 --max-recipients 1 ✅
  - 二次确认口令 ✅

加载凭据...
Keychain round-trip: outlook/<邮箱> 成功
真实 SMTP 发送 1 封
连接 smtp.office365.com:587 SSL
XOAUTH2 鉴权(若 OAuth)或 App Password
✅ 1 封真实发送成功
sent=1 / ~1.27s(沿 [[d5.6.5-real-send]] baseline)

状态机 4 步全过:
PENDING_SEND → APPROVED → SENDING → SENT

DispatcherResult 7 字段全 ok
```

#### 7.5 撞坑处理

| 撞坑 | 表现 | 应对 |
|------|------|------|
| SMTP_REAL_NETWORK 未设 | RuntimeError "必须显式设置..." | `export SMTP_REAL_NETWORK=1` |
| provider 不在白名单 | argparse error "invalid choice" | 沿 [[b-class-deferral-2026-06-09]] B 类决策延后 → 用户单独决策白名单扩展 |
| Keychain 读不到凭据 | RuntimeError "D5.6.2 凭证链路..." | 检查 `spike_set_smtp_password.py --provider outlook --email <邮箱> --check-password` |
| OAuth flow 401 | XOAUTH2 鉴权失败 | 重新跑 OAuth authorization code flow 拿 access_token + refresh_token |
| 发件被拒 | SMTP 服务器返回 5xx | 检查 App Password / OAuth scope / 邮箱发件限制 |

#### 7.6 下一阶段门槛

- ✅ 1 封真实发送成功
- ✅ 状态机 4 步全过(PENDING_SEND → APPROVED → SENDING → SENT)
- ✅ sent=1 / ~1.27s

---

## 📊 7 阶段实战手册汇总

| 阶段 | 内容 | 阻塞条件 | 6/23 预期状态 |
|------|------|---------|--------------|
| 1 | 环境准备 | 无 | ✅ 必做 |
| 2 | 8/8 质量门 baseline | 无 | ✅ 必做(或 SIGKILL 137 误报已处理) |
| 3 | launchd kickstart + plist 安装 | 无 | ✅ 必做(预期 exit 0 或 2)|
| 4 | 菜单栏启动 | 无 | ✅ 必做(TCC 授权)|
| 5 | Apple Notes 同步(可选)| 无 | 🟡 可选(用户手动触发)|
| 6 | W3 真账单 spike | ⚠️ 等用户提供真实 CSV | 🟡 可选(用户提供 CSV 后)|
| 7 | outlook/gmail SMTP spike | ⚠️ 等用户授权 + 凭据 + B 类决策 | 🟡 可选(用户授权后)|

## 🔄 撞坑恢复 3 步实战演练 7(沿 v0.2.4 机制 3)

本报告作为 [[v0.2.4-drift-review-mechanism-2026-06-18]] §3 机制 3 撞坑恢复 3 步范本的**实战演练 7**。

### 撞坑恢复 3 步范本累计

| 演练 | 报告 | 时间 | 0 撞坑触发 |
|------|------|------|----------|
| 演练 1 | v0.2.5 SMTP 真实发送 spike preflight | 2026-06-18 | ✅ |
| 演练 2 | v0.2.7 outlook/gmail SMTP spike 准备 | 2026-06-21 | ✅ |
| 演练 3 | v0.2.9 W3 真账单 spike 准备 | 2026-06-22 | ✅ |
| 演练 4 | v0.2.10 全链路重启 checklist | 2026-06-22 | ✅ |
| 演练 5 | v0.2.11 全链路重启 7 阶段 dry-run 预演 | 2026-06-20 | ✅ |
| 演练 6 | v0.2.12 6/23 全链路重启实战前置 | 2026-06-20 | ✅ + SIGKILL 137 误报已沿用 baseline |
| **演练 7** | **v0.2.13 6/23 全链路重启实战手册(本次)** | **2026-06-20** | **✅ + 实战手册整合 6 个演练 + SIGKILL 137 误报应对)** |
| 撞坑史累计 | 2 类(撞坑恢复 v0.2.2 #8 SMTPProviderFactory + SIGKILL 137 误报) | — | — |
| **范本累计** | **7 个实战演练(均 0 撞坑触发 + 1 次 SIGKILL 137 误报已处理)** | — | ✅ 沿 [[v0.2.4]] §3 机制 3 |

### 演练 7:本任务的撞坑恢复 3 步

#### 步骤 1:撤回自己基于旧状态写的 docs-only 报告

- **本任务**:**不需要撤回**(本任务是首次执行实战手册整合,无旧状态报告)
- **触发条件**:未来撞同款问题(并发进程冲突 6/23 全链路重启实化撞坑)时,撤回自己写的 stale 报告

#### 步骤 2:验证并发进程工作通过 8/8 质量门

- **本任务**:**不需要验证**(因为没有并发进程)
- **8/8 质量门验证结果**(沿 v0.2.12 baseline):
  - ✅ ruff check:All checks passed(沿 v0.2.11 实跑)
  - ✅ ruff format:221 files already formatted(沿 v0.2.11 实跑)
  - 🟡 mypy src:Success 101 files / 0 errors(沿 v0.2.11 baseline · v0.2.12 SIGKILL 137 误报)
  - ✅ alembic --sql:exit 0(沿 v0.2.11 baseline)
  - 🟡 pytest:2225 passed / 1 skipped / 88.85% coverage(沿 v0.2.11 baseline · v0.2.12 SIGKILL 137 误报)
  - ✅ uv build:success(沿 v0.2.11 baseline)
  - ✅ MD lint:0 errors / 109 files(本轮 v0.2.12 实跑)
  - ✅ coverage:88.85% ≥ 80%

#### 步骤 3:用户授权代 commit 2 commits(feat + docs closure)

- **本任务**:**不需要**(因为本任务是 docs-only,沿 v0.2.5/v0.2.7/v0.2.9/v0.2.10/v0.2.11/v0.2.12 范本)
- **commit 范本**:`docs(sync): v0.2.13 6/23 全链路重启实战手册 docs-only`

### 撞坑恢复 3 步应用次数统计

- **撞坑恢复 3 步实际应用次数**:**0 次**(本会话无撞坑)
- **撞坑史累计次数**:2 类(撞坑恢复 + SIGKILL 137 误报)
- **撞坑恢复范本累计**:7 个(演练 1 + 演练 2 + 演练 3 + 演练 4 + 演练 5 + 演练 6 + 演练 7)
- **7/1 月度复盘**:撞坑恢复范本应用情况统计(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §4 复盘 5 项)

## 🛡️ 撞坑汇总(本手册覆盖)

| 撞坑 | 范本 | 应对 |
|------|------|------|
| 1. 环境准备缺 SQLCIPHER_KEY | [[v0.2.12]] §"step 1" | 编辑 .env 填入 |
| 2. uv sync 失败 | [[v0.2.10]] §"模块 2 DB 路径" | 检查 uv.lock + uv lock --upgrade |
| 3. **SIGKILL 137 误报** | [[2026-06-18-venv-sigkill-137-false-alarm]] | **不重试 · 沿用 baseline** |
| 4. pytest 真失败 | [[v0.2.4-drift-review-mechanism]] | 状态漂移审查机制 |
| 5. launchd exit 1 / 3 | [[d10.5.4-launchd-kickstart-and-seal]] | 4 退出码范本处理 |
| 6. launchd exit 2(预期业务失败)| [[d10.5.3-p3-1]] | 7/1 自然触发,预期 |
| 7. TCC 拦截(菜单栏)| [[apple-script-pitfalls]] | 一次性授权 |
| 8. AppleScript 权限拦截(Notes)| [[apple-script-pitfalls]] | 一次性授权 |
| 9. WECHAT_REAL_IMPORT 未设 | [[v0.2.9-w3-real-bill-spike-prep]] | `export WECHAT_REAL_IMPORT=1` |
| 10. 2026 版 parser 缺失 | [[v0.2.9-w3-real-bill-spike-prep]] §"Checklist 3" | 补 parser 或用 2024/2025 版 |
| 11. 真异常误报率高 | [[v0.2-d8-real-faker-spike-2026-06-17]] D8.5.1-3 | 修复链 |
| 12. SMTP_REAL_NETWORK 未设 | [[d5.6.5-real-send]] | `export SMTP_REAL_NETWORK=1` |
| 13. provider 不在白名单 | [[b-class-deferral-2026-06-09]] | 用户单独决策白名单扩展 |
| 14. Keychain 读不到凭据 | [[d5.6.2-p0-fix-real-keychain-read]] | 沿 D5.6.2 P0 修复 · spike_set_smtp_password.py 写入 |
| 15. OAuth flow 401 | [[v0.2.2-p5-oauth-xoauth2-2026-06-18]] §"OAuth flow" | 重新跑 OAuth authorization code flow |
| 16. 发件被拒(SMTP 5xx)| [[d5.6.5-real-send]] §"撞坑处理" | 检查 App Password / OAuth scope |

## 🔗 关键引用(14 项)

- **本报告**: `我的AI员工/docs/v0.2.13-6-23-restart-playbook-2026-06-20.md`(6/23 全链路重启实战手册 · 撞坑恢复 3 步实战演练 7)
- **v0.2.12 实战前置**: `我的AI员工/docs/v0.2.12-6-23-restart-prep-2026-06-20.md`(5 step 实战预演 · 撞坑恢复实战演练 6)
- **v0.2.11 dry-run 预演**: `我的AI员工/docs/v0.2.11-7-stage-dry-run-2026-06-20.md`(4 个 dry-run 验证点 · 撞坑恢复实战演练 5)
- **v0.2.10 全链路重启 checklist**: `我的AI员工/docs/v0.2.10-full-restart-checklist-2026-06-22.md`(6 模块链路核验 + 7 阶段启动 checklist · 撞坑恢复实战演练 4)
- **v0.2.9 W3 真账单 spike 准备**: `我的AI员工/docs/v0.2.9-w3-real-bill-spike-prep-2026-06-22.md`(W3 真账单 spike 启动流程 5 阶段 · 撞坑恢复实战演练 3)
- **v0.2.7 outlook/gmail SMTP spike 准备**: `我的AI员工/docs/v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21.md`(撞坑恢复实战演练 2)
- **v0.2.5 SMTP preflight**: `我的AI员工/docs/v0.2.5-smtp-real-send-preflight-2026-06-18.md`(撞坑恢复实战演练 1)
- **v0.2.4 状态漂移审查机制**: `我的AI员工/docs/v0.2.4-drift-review-mechanism-2026-06-18.md`(`机制 3` 撞坑恢复 3 步范本 · 实战演练 7 锚定)
- **D10.5.4 launchd kickstart and seal 范本**: `Agent Assistant/memory/d10.5.4-launchd-kickstart-and-seal.md`(4 退出码范本)
- **D5.6.5 真实 1 封 SMTP 范本**: `Agent Assistant/L2_memory/_cross-project/d5.6.5-real-send.md`(smtp.qq.com:465 SSL · sent=1/1.27s · 4 重防误发)
- **AppleScript 坑(macOS Sequoia TCC)**: `Agent Assistant/L2_memory/_core/apple-script-pitfalls.md`(TCC 一次性授权)
- **SIGKILL 137 误报范本**: `Agent Assistant/L2_memory/_core/2026-06-18-venv-sigkill-137-false-alarm.md`(v0.2.12 SIGKILL 137 处理)
- **B 类决策延后**: `Agent Assistant/L2_memory/_core/b-class-deferral-2026-06-09.md`(provider 白名单扩展需用户单独决策)
- **W3 faker spike 报告**: `我的AI员工/reports/v0.2-d8-real-faker-spike-2026-06-17.md`(D8.5.4 修复后真异常误报率 0%)

## 📌 下一棒

### 端午连休(6/21 周日 + 6/22 周一)

- **6/21 周日**:撞坑恢复 3 步实战演练 8(候选 · docs-only 预演另一关键路径)
- **6/22 周一**:端午连休最后一天(沿 v0.2 release notes 收口已完成 `dda2dc1`)

### 端午连休后(6/23 周二 · 全链路重启日)

- **6/23 当天**:跑本报告 §"7 阶段实战手册":
  - 阶段 1:环境准备(make install + cp .env.example .env + mkdir data/ + 编辑 .env)
  - 阶段 2:8/8 质量门 baseline 验证(make test + make lint)
  - 阶段 3:launchd kickstart + plist 安装(bash scripts/launchd_kickstart_and_seal.sh)
  - 阶段 4:菜单栏启动(make menu-bar + TCC 授权)
  - 阶段 5:Apple Notes 同步(可选 · NOTES_REAL_NETWORK=1)
  - 阶段 6:W3 真账单 spike(等用户提供真实微信/支付宝 CSV)
  - 阶段 7:outlook/gmail SMTP 真实 spike(等用户授权 + 凭据 + B 类决策)

### 月度复盘 + release tag 锚定(7/1 + 8/1)

- **7/1 月度复盘**(沿 [[v0.2.4-drift-review-mechanism-2026-06-18]] §4 复盘 5 项 + 撞坑恢复范本统计 7 个 + SIGKILL 137 误报处理):
  - 状态漂移审查(机制 1 + 2)
  - 并发进程冲突审查(机制 4)
  - B 类延后清单重新评估
  - v0.2.1 release tag 锚定策略复审
  - 撞坑恢复范本应用情况统计
- **8/1**:v0.2.1 release tag 锚定(沿 [[v0.2-release-notes-2026-06-22]] §"8 项前置条件")

## 📌 完成定义(本报告 Done)

- [x] 7 阶段实战手册(每阶段含精确命令 + 预期输出 + 撞坑处理 + 下一阶段门槛)
- [x] 16 类撞坑汇总(覆盖环境 + uv + SIGKILL 137 + pytest + launchd + TCC + AppleScript + W3 + SMTP + OAuth + 发件拒绝)
- [x] 撞坑恢复 3 步实战演练 7 沉淀(本任务无撞坑,沿 [[v0.2.4]] §3 机制 3 范本)
- [x] 撞坑恢复范本累计 7 个(演练 1-7 · 均 0 撞坑触发 + 1 次 SIGKILL 137 误报已处理)
- [x] 撞坑史累计 2 类(撞坑恢复 v0.2.2 #8 SMTPProviderFactory + SIGKILL 137 误报)
- [x] 14 项关键引用(v0.2.10-v0.2.12 + D10.5.4/D5.6.5/AppleScript 坑/SIGKILL 137/B 类延后/W3 faker)
- [x] 6/21-6/22 端午 2 天路径 + 6/23 全链路重启路径 + 7/1 月度复盘路径 + 8/1 release tag 锚定路径
- [x] **不真发邮件**(本报告纯 docs-only,7 阶段实战手册精确命令)
- [x] **不真导入账单**(本报告纯 docs-only)
- [x] **不真启动菜单栏**(本报告纯 docs-only,仅 import 验证 + 依赖核查)
- [x] **不真 kickstart launchd**(本报告纯 docs-only,仅 5 源判定 + bash 语法核查)
- [x] **不移动 `v0.1.0` tag`(继续锚定 `2af775f`,沿 D5.7.2 范本)
- [x] **不打 `v0.2.0/v0.2.1` tag`(留作 8/1 锚定策略)
- [x] **不跑真实 OAuth flow`(等用户授权 + 凭据)

## 维护者

**Mr-PRY** · 2026-06-20 v0.2.13 6/23 全链路重启实战手册 docs-only(撞坑恢复 3 步实战演练 7)
**模型**:MiniMax-M3
**项目**:我的AI员工(v0.2.13 6/23 全链路重启实战手册 docs-only · 端午不休息第 2 天 · 6/23 实战手册就绪)
**关键决策**:**7 阶段实战手册 + 16 类撞坑汇总 + SIGKILL 137 误报应对**(沿用户结论 §1-3);阶段 1-4 必做 + 阶段 5-7 等用户授权 + 凭据 + 真 CSV · 撞坑恢复范本累计 7 个 · 撞坑史累计 2 类 · 6/23 全链路重启实战手册就绪
**沿用范本**:[[v0.2.12-6-23-restart-prep-2026-06-20]] / [[v0.2.11-7-stage-dry-run-2026-06-20]] / [[v0.2.10-full-restart-checklist-2026-06-22]] / [[v0.2.9-w3-real-bill-spike-prep-2026-06-22]] / [[v0.2.7-outlook-gmail-smtp-spike-prep-2026-06-21]] / [[v0.2.5-smtp-real-send-preflight-2026-06-18]] / [[v0.2.4-drift-review-mechanism-2026-06-18]] / [[d10.5.4-launchd-kickstart-and-seal]] / [[d5.6.5-real-send]] / [[apple-script-pitfalls]] / [[2026-06-18-venv-sigkill-137-false-alarm]] / [[b-class-deferral-2026-06-09]] / [[v0.2-d8-real-faker-spike-2026-06-17]]

**下一棒**:6/23 周二全链路重启(沿本报告 §"7 阶段实战手册");7/1 月度复盘(沿 [[v0.2.4]] §4 复盘 5 项 + 撞坑恢复范本统计 7 个);8/1 v0.2.1 release tag 锚定(沿 [[v0.2-release-notes-2026-06-22]] §"8 项前置条件")