# v0.2.43 outlook/gmail SMTP provider 白名单解封(2026-06-25)

> **状态**:✅ 已收口
> **范围**:只解封 provider 白名单 + 测试严判 + 状态沉淀;不真发邮件;不写入真实凭据
> **承接**:v0.2.7 outlook/gmail SMTP spike 准备 + v0.2.42 mypy strict 硬门

---

## 1. 目标

把 `scripts/spike_send_100.py --smtp-provider` 从只允许 `qq` 解封为 `qq/outlook/gmail`,与 `scripts/spike_set_smtp_password.py` 已支持的 provider-aware Keychain 能力对齐。

---

## 2. 改动

| 文件 | 改动 |
|------|------|
| `scripts/spike_send_100.py` | `choices=["qq"]` → `choices=["qq", "outlook", "gmail"]`;help 文案更新 |
| `tests/scripts/test_spike_send_100_real_mode.py` | help 断言改为必须含 `{qq,outlook,gmail}` |
| `README.md` / `SESSION-STATE.md` / `MODIFICATION-LOG.md` | 状态同步到 v0.2.43 |

---

## 3. 验证

| 检查 | 结果 |
|------|------|
| `spike_send_100.py --help` | 显示 `--smtp-provider {qq,outlook,gmail}` |
| 相关无副作用测试 | 76 passed (`--no-cov`) |
| `make mypy` | 0 errors / 209 source files |
| `make test` | 2265 passed / 1 skipped / 88.76% coverage |
| `ruff check` | All checks passed |
| `ruff format --check` | 246 files already formatted |

---

## 4. 真实发送仍需的门控

本轮没有真实发送。真实 SMTP spike 仍必须同时满足:

1. Keychain 中存在对应 `provider/email` 凭据。
2. 显式设置 `SMTP_REAL_NETWORK=1`。
3. `--max-recipients 1`。
4. `--count 1`。
5. `--confirm yes-i-understand-this-sends-real-email`。

---

## 5. 下一棒

等用户提供/确认 Keychain 凭据后,跑 1 封真实 SMTP spike。优先建议从 Outlook App Password 或已存在授权码开始;Gmail 更可能需要 OAuth/App Password 策略确认。
