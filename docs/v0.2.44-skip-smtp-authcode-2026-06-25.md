# v0.2.44 跳过授权码 + 真实 SMTP spike 延后(2026-06-25)

> **状态**:✅ 已收口
> **用户决策**:“跳过授权码”
> **范围**:docs-only 决策沉淀;不真发邮件;不写入 Keychain 凭据

---

## 1. 决策

本轮不继续等待 Outlook/Gmail 授权码,真实 SMTP spike 延后到未来凭据可用时再恢复。

保留:

- v0.2.43 `--smtp-provider {qq,outlook,gmail}` 白名单解封。
- provider-aware Keychain 写入/检查/删除能力。
- `SMTP_REAL_NETWORK=1` + `--max-recipients 1` + `--count 1` + 固定确认口令 + Keychain round-trip 五重防误发门控。

---

## 2. 已验证事实

| 检查 | 结果 |
|------|------|
| Outlook Keychain | `com.myaiemployee.smtp.outlook missing` |
| Gmail Keychain | `com.myaiemployee.smtp.gmail missing` |
| Outlook InMemory 预演 | sent=1,状态机最终态 |
| 真实发送安全门 | 未设置 `SMTP_REAL_NETWORK=1` 时硬拦截,未触网 |

预演报告:

- `/tmp/my_ai_employee_smtp_preflight_outlook_next/spike_send_100_20260625_082132.md`

---

## 3. 影响

- 当前不再把项目推进卡在授权码。
- 真实 SMTP 送达验证未完成,后续 release tag 锚定前需重新评估是否恢复。
- 下一棒切到 7/1 月度复盘准备。

---

## 4. 恢复条件

未来如需恢复真实 SMTP spike,只需补齐:

1. Keychain 中存在 `outlook` 或 `gmail` provider 凭据。
2. 用户确认发件邮箱与收件邮箱。
3. 执行 1 封真实 spike,仍保留五重防误发。
