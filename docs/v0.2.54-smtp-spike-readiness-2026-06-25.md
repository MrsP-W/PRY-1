# v0.2.54 SMTP Spike 就绪检查(2026-06-25)

> **状态**:✅ 就绪检查完成 · **真实发送**:⏸️ 阻断( outlook/gmail Keychain missing )
> **边界**:只读 Keychain 存在性(不 `-w` 读明文) · InMemory spike 可跑 · **不 SMTP_REAL_NETWORK=1**

---

## 1. Keychain 存在性(2026-06-25 实测)

| Service | 状态 |
|---------|------|
| `my-ai-employee.smtp.qq` | **present** |
| `my-ai-employee.smtp.outlook` | **missing** |
| `my-ai-employee.smtp.gmail` | **missing** |

检查命令(不输出密码):

```bash
security find-generic-password -s my-ai-employee.smtp.outlook >/dev/null 2>&1 && echo present || echo missing
```

---

## 2. InMemory Spike 复跑(2026-06-25)

```bash
uv run python scripts/spike_send_100.py --smtp-provider qq --count 1 --batch-size 1
```

结果:

- `sent=1` / `total_picked=1` / 状态机全终态 ✅
- 报告:`output/spike/spike_send_100_20260625_163607.md`

---

## 3. 真实 SMTP 恢复 checklist(仍待用户)

- [ ] 写入 outlook/gmail Keychain(`set_smtp_password_for_provider` 或 OAuth token)
- [ ] 用户明确授权:`yes, do SMTP_REAL_NETWORK=1 spike`
- [ ] 5 重门控:`SMTP_REAL_NETWORK=1` + `--count 1` + `--max-recipients 1` + 白名单收件人 + 确认口令

---

## 4. 结论

**链路就绪,凭据未齐** — qq InMemory 全绿;8/1 tag 前置 #2 仍缺 outlook/gmail 真实送达证据。
