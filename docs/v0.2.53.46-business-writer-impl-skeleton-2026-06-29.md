# v0.2.53.46 BusinessWriterImpl 4 动作实写骨架(2026-06-29)

> **承接**:[v0.2.53.27 BusinessWriterImpl opt-in 注入](v0.2.53.27-business-writer-opt-in-2026-06-26.md) — 默认 Impl 已就位,但 4 动作方法都 `raise NotImplementedError("real_write_handler 未启用")` 占位
> **撞坑累计**:70 类沿用(本棒 0 新增)
> **沿用边界**:默认禁写 · 实际写入留 8/1 后 · 不接 SMTP · 不读 Keychain 明文 · 不打 v0.2.x tag

---

## 1. 背景与目标

v0.2.53.17 commit `cdd619e` 落地 BusinessWriterImpl 默认 raise 接入骨架,但 4 动作方法都是 1 行 raise,无完整方法体结构。**v0.2.53.46 实写骨架**目标:

- **完整方法体骨架**:依赖检查 + 参数校验 + 状态守卫 + WriteResult 包装
- **默认 raise**(沿撞坑 #18 风险门控):不真写 DB,等待 v0.2.53.19 handler 路径 4 启用
- **write_executed 恒 False**(沿 v0.2.53.11 不变式)
- **不动 ApprovalGate 决策矩阵**(沿 v0.2.53.22 8 路径)

---

## 2. 设计决策(2026-06-29 锁定)

### 2.1 4 动作方法统一骨架

```python
def action_name(self, target_id: str, *, audit: AuditContext) -> WriteResult:
    self._check_dep(self._dep_service, "dep_name")  # 依赖检查
    err = self._validate_target_id(target_id)       # 参数校验
    if err is not None:
        return WriteResult(success=False, affected_id=None, error="invalid_target_id", reason=err)
    # 路径 4 启用后真实调用(注释保留)
    #   self._dep_service.some_method(...)
    raise NotImplementedError("...路径 4 启用后将调 ...;实际写入留 8/1 后 + 用户明确授权")
```

### 2.2 4 动作路径 4 启用后真实调用

| 动作 | 依赖 service | 路径 4 启用后调 | 沿用约束 |
|------|------------|---------------|---------|
| `approve_outbox` | `outbox_store` | `update_status(id, APPROVED, from_status=PENDING_SEND, last_approved_at_ms=now_ms)` | D5.6.3 P1-1 审批凭据必传 |
| `cancel_outbox` | `outbox_store` | `update_status(id, CANCELLED, from_status=<前态>, last_approved_at_ms=None)` | D5.6.3 P1-1 非 APPROVED 必传 None |
| `confirm_note` | `note_confirm_service` | `confirm_note(apple_note_id=target_id)` | 异常透传不收容(note_confirm_service.py:113-115) |
| `dismiss_anomaly` | `anomaly_dismissal_service` | `dismiss(anomaly_id=target_id, reason=audit.reason)` | reason 限 240 字符(_MAX_REASON_LEN) |

### 2.3 2 个辅助方法

- **`_check_dep(dep, name)`**:依赖为 None → raise NotImplementedError(沿撞坑 #18)
- **`_validate_target_id(target_id)`**:返回 None = OK,返回 str = 错误 reason(沿 note_confirm_service.py 校验模式)

---

## 3. 边界(沿撞坑 #18 + 撞坑 #65 + v0.2.53.14 §6)

| 边界维度 | 边界规则 |
|---------|---------|
| **依赖未注入** | `raise NotImplementedError` 字符串含依赖名(测试可断言依赖名) |
| **无效 target_id**(非 str / 空字符串) | `WriteResult(success=False, error='invalid_target_id', reason=...)` |
| **默认 raise** | 字符串明确说明"路径 4 启用后调什么",沿撞坑 #18 风险门控 |
| **write_executed 不变式** | dry_run 恒 False;WriteResult 文档约定 success=False 时 write_executed=True(沿 v0.2.53.15) |
| **不动 ApprovalGate 8 路径决策矩阵** | 沿 v0.2.53.22 |
| **不接 SMTP / 不读 Keychain 明文** | 沿撞坑 #18 |
| **实际写入留 8/1 后 + 用户明确授权** | 沿撞坑 #60 preliminary 范本 |

---

## 4. 测试覆盖(28 个新测试 · 沿 v0.2.53.27 opt-in 4 阶段范本)

| 测试类 | 测试数 | 覆盖点 |
|--------|--------|--------|
| `TestBusinessWriterImplApproveOutboxSkeleton` | 4 | 无依赖 raise / 3 种 target_id 校验 |
| `TestBusinessWriterImplCancelOutboxSkeleton` | 3 | 无依赖 raise / None target_id / 有效 target_id raise |
| `TestBusinessWriterImplConfirmNoteSkeleton` | 4 | 无依赖 raise / int target_id / 空字符串 / 有效 raise |
| `TestBusinessWriterImplDismissAnomalySkeleton` | 3 | 无依赖 raise / int target_id / 有效 raise |
| `TestBusinessWriterImplCheckDepHelper` | 2 | `_check_dep` 2 路径(None/非 None) |
| `TestBusinessWriterImplValidateTargetIdHelper` | 8 | `_validate_target_id` 8 路径(str/空/纯空白/int/None/bool/list) |
| `TestBusinessWriterImplSkeletonBoundaries` | 4 | write_executed 文档约定 / dry_run 不受影响 / 不接 SMTP / 4 动作签名对齐 Protocol |

**测试关键不变量**:
- 4 旧测试 `match` 字符串同步更新(`match="approve_outbox"` → `match="outbox_store"`,沿实写骨架 `_check_dep` 先 raise)
- SimpleNamespace mock 用 `cast(Any, SimpleNamespace())` 解决 mypy --strict Protocol 类型严格
- `_check_dep` helper 测试用 `cast(Any, None)` 满足 type-checker,实际运行验证 raise 路径

---

## 5. 质量门(9 门全绿)

| 门 | 结果 |
|---|------|
| MD lint | 188 files 0 errors |
| mypy --strict | 0 errors / 235 files |
| ruff check | All checks passed |
| ruff format | 249 files already formatted |
| pytest | 2546 passed / 1 skipped(实写骨架 28 + 旧 4 match 同步 + 旧 16) |
| coverage | **88.81%**(88.78% → 88.81% 微涨 0.03pp · 撞坑 #50 第二层修复) |
| alembic upgrade head --sql | exit 0 |
| uv build | OK(sdist + wheel) |
| FINAL_EXIT | 0 |

---

## 6. 撞坑 #50 第二层修复(quality_snapshot 微涨)

v0.2.53.27 commit 落地后,quality_snapshot.py 报 `pytest: 2518 passed` / `coverage: 88.78%`,但实际跑出来是 2546 passed / 88.81% — 撞坑 #50 衍生第三版(self-claim vs 实际漂移)。

v0.2.53.46 实写骨架新增 28 个测试,质量基线从 2518 → 2546(+28),coverage 88.78% → 88.81%(+0.03pp)。

**修复**:同步 quality_snapshot.py 字段 `2518 passed → 2546 passed` / `88.78% → 88.81%`(沿 v0.2.53.27 沿用规范)。

---

## 7. 路径 4 启用后切换点(留 8/1 后)

实写骨架的最后一行 `raise NotImplementedError(...)` 是切换点。路径 4 启用后:

```python
# Before (v0.2.53.46 默认 raise):
raise NotImplementedError(
    "BusinessWriterImpl.approve_outbox 路径 4 启用后将调 "
    "OutboxStore.update_status(...);实际写入留 8/1 后 + 用户明确授权"
)

# After (v0.2.53.19 启用后):
return self._outbox_store.update_status(
    outbox_id=int(target_id),
    new_status='APPROVED',
    from_status='PENDING_SEND',
    last_approved_at_ms=_now_ms(),
)
```

**4 动作切换点对应表**:

| 动作 | 切换前 raise 字符串 | 切换后真实调用 |
|------|------------------|---------------|
| approve_outbox | "路径 4 启用后将调 OutboxStore.update_status(PENDING_SEND → APPROVED, last_approved_at_ms=now_ms)" | update_status(int, APPROVED, PENDING_SEND, now_ms) |
| cancel_outbox | "路径 4 启用后将调 OutboxStore.update_status(PENDING_SEND/APPROVED → CANCELLED, last_approved_at_ms=None)" | update_status(int, CANCELLED, from, None) |
| confirm_note | "路径 4 启用后将调 NoteConfirmServiceImpl.confirm_note(apple_note_id=target_id)" | note_confirm_service.confirm_note(target_id) |
| dismiss_anomaly | "路径 4 启用后将调 AnomalyDismissalService.dismiss(anomaly_id=target_id, reason=audit.reason)" | anomaly_dismissal_service.dismiss(target_id, reason=audit.reason) |

---

## 8. 沿用边界 + 撞坑累计

**沿用边界**(本棒 0 新增,全部沿用):
- ❌ 不接真实业务 writer(实际写入路径留 8/1 后)
- ❌ 不写 DB 实际数据(默认 raise / dry-run 模式)
- ❌ 不发真实 SMTP(路径 4 启用后也仅 DB 状态更新)
- ❌ 不读 Keychain 明文(沿 #59 撞坑规范)
- ❌ 不打 `v0.2.x` tag(沿 D5.7.2 + 8/1 锚定)
- ❌ 不移动 `v0.1.0` tag(`2af775f` 锚定不动)
- ❌ 不接 outlook/gmail SMTP(用户决策豁免)
- ❌ 不 kickstart launchd(端午不休息边界)
- ✅ 默认 raise + 沿 v0.2.53.22 8 路径决策矩阵
- ✅ 撞坑 #18 风险门控(实际写入留 8/1 后)
- ✅ 撞坑 #65 opt-in 范本(env 门控 + 单项失败降级)
- ✅ 撞坑 #50 docstring/code 漂移防御(quality_snapshot 同步)
- ✅ 撞坑 #55 mypy --strict 双 0
- ✅ 9 质量门全绿
- ✅ write_executed 恒 False(沿 v0.2.53.11 不变式)

**撞坑累计 70 类沿用**(本棒 0 新增)。

---

## 9. 关联 commit

- **build commit**: `e76d716` — `feat(dashboard): v0.2.53.46 BusinessWriterImpl 4 动作实写骨架(默认 raise)`(3 files +464/-27)
- **docs commit**: 后续 — `docs(state): v0.2.53.46 8 段沉淀 + 状态同步`
- **跨项目沉淀 commit**: 后续 — `docs(cross-project): v0.2.53.46 4 动作实写骨架跨项目 memory 沉淀`

---

## 10. 下一棒(沿 v0.2.53.27 链 · 8/1 后)

- **8/1 后独立 launch**:路径 4 实写 4 动作切换 `raise → 真实调 service`(需用户明确授权,撞坑 #18 风险门控)
- **路径 3.5 handler 接入**(沿 v0.2.53.22 §3.5):仅 dry_run,write_executed 仍 False
- **audit log 落档**(沿 v0.2.53.20 §6):WriteResult.audit_id 填充(v0.2.53.20 落档 audit log 时填充)
- **撞坑 #18 风险门控**:不擅自启用,需用户授权 + 8/1 后独立 launch
