# D3.1 — SQLCipher 数据层基础完成报告（v3.1.3 — 文档一致性最终收口，D3.1 正式锁定）

> **日期**：2026-06-07（D3 阶段起步 / D3.1.1 修正落地 / D3.1.2 文档清理 + connection property / **D3.1.3 文档内部自相矛盾收口 → 正式锁定**）
> **范围**：D3.1 = 基础 + schema（D3 拆 3 phase：D3.1 基础 / D3.2 ORM+migrations / D3.3 同步+spike）
> **D3 整体** = D3.1（基础） + D3.2（ORM+migrations） + D3.3（同步+spike）
> **当前模型**：MiniMax-M3 · **维护者**：Mr-PRY
> **v1 → v2 → v3 → v3.1.3 变更**：D3.1 收尾 7 个修正点全落地（§8）→ D3.1.2 文档清理 + 受控 `connection` property（§9）→ **D3.1.3 报告 §3 重写 + week1-mvp.md line 230/239/241 同步（§10）**

---

## 0. 一句话总结

**D3.1 完美收官 + D3.1.1 修正全绿 + D3.1.2 文档清理 + 受控 connection 入口**：SQLCipher 加密数据库封装 + 6 张表 DDL + **23 个测试**（D3.1.1 +5 / D3.1.2 +3），pytest/ruff/mypy/md lint 全绿，db.py 覆盖率 97.5%。**D3.2 ORM+migrations / D3.3 同步+spike 待启动**。

| 指标 | v1 | v2（D3.1.1 修正后）| v3（D3.1.2 文档清理 + connection）| **v3.1.3（文档最终收口）** |
|------|----|----|----|----|
| 新增代码 | 743 行（4 文件） | 743 行（4 文件） | 743 行（4 文件） | 743 行（4 文件，无代码变更） |
| 新增测试 | 15 个 | 20 个（+5）| **23 个**（+3：D3.1.2 connection property）| 23 个（无变更）|
| 全项目测试 | 52 passed | 57 passed | **60 passed**（37 D2 + 23 D3.1）| 60 passed（无变更）|
| 覆盖率 | 76.8%（db.py 97.4%）| 76.8%（db.py 97.4%）| 77.1%（db.py **97.5%**）| 77.1%（db.py 97.5%，无变更）|
| ruff / mypy | 0 errors | 0 errors | 0 errors | 0 errors |
| `make lint` | ✅ 0 errors | ✅ 0 errors | ✅ 0 errors | ✅ 0 errors |

---

## 1. 交付清单

### 1.1 新增文件

| 文件 | 行数 | 职责 |
|------|------|------|
| [src/my_ai_employee/core/schema.sql](../src/my_ai_employee/core/schema.sql) | ~120 | 6 张表 DDL + **9 个索引**（D3.1.1 增 idx_emails_message_id）+ UNIQUE 约束 |
| [src/my_ai_employee/core/db.py](../src/my_ai_employee/core/db.py) | ~285 | Database 封装（sqlcipher3 + Keychain + dict_factory + quick_check + 5 PRAGMA + **受控 `connection` property**）|
| [tests/core/test_db.py](../tests/core/test_db.py) | ~466 | **23 测试**（15 v1 + D3.1.1 +5：WAL/busy_timeout/synchronous/字段可空 + D3.1.2 +3：connection property）|
| [scripts/spike_sqlcipher.py](../scripts/spike_sqlcipher.py) | ~50 | 5 分钟加密往返 spike（验证装包） |

### 1.2 6 张表 DDL

| 表 | 主键 | 关键字段 | 索引 |
|----|------|----------|------|
| **emails** | id (autoinc) | source / uid / **message_id（可空, D3.1.1）** / subject / sender / **received_at(ms, 可空, D3.1.1）** / raw_size / body_text / body_html / fetched_at / labels(JSON) | **UNIQUE(source, uid)（D3.1.1 改）** / (received_at) / (source, received_at) / (sender) / **idx_emails_message_id（D3.1.1 增）** |
| **attachments** | id | email_id(FK) / filename / content_type / size / local_path / sha256 | (email_id) |
| **labels** | id | name(COLLATE NOCASE) / source / color | UNIQUE(name, source) / (source) |
| **email_labels** | 双键 PK | email_id(FK) / label_id(FK) | (label_id) |
| **sync_state** | id | source(UNIQUE) / last_sync_at / last_uid / last_status / last_error / consecutive_failures | (source 唯一约束) |
| **audit_log** | id | event / source / detail(JSON) / created_at(ms) | (created_at) / (event) |

### 1.3 Database 封装 API

```python
# 短期脚本
db = Database.open()
db.init_schema()
db.execute("INSERT INTO ...", (...))
db.commit()
db.close()

# 上下文管理器（推荐 — 自动 commit/rollback）
with Database.open() as db:
    db.init_schema()
    db.execute(...)
    # 退出自动 commit；异常自动 rollback

# 受控底层连接（供 D3.2 alembic env.py / 高级用法）
with Database.open() as db:
    raw_conn = db.connection  # sqlcipher3.Connection 受控入口
    # 警告：直接操作 connection 绕过 Database 封装的事务/上下文管理逻辑
```

**方法**：`open` / `init_schema` / `execute` / `executemany` / `fetch_all` / `fetch_one` / `commit` / `rollback` / `close` / `__enter__` / `__exit__` / `connection` 属性 / `db_path` 属性

---

## 2. 关键设计决策

### 2.1 DB 密码自动生成 + Keychain 隔离

| 维度 | 决策 | 理由 |
|------|------|------|
| 密码生成 | `secrets.token_hex(32)`（64 chars）| 密码学安全随机，零记忆负担 |
| 密码存储 | macOS Keychain service=`my-ai-employee.db` account=`data.db` | 与 IMAP 授权码**完全隔离**（不复用 D2.3 IMAP 凭证）|
| DB 路径 | `~/Library/Application Support/my-ai-employee/data.db` | macOS 标准 Application Support 目录 |
| 首次启动 | Keychain 缺密码 → 自动生成 + 写入 | 零配置开箱即用，密码丢失 = 数据无法解密 |

### 2.2 PRAGMA key 密码设置

```python
# 错误方案 1: conn.execute("PRAGMA key = ?", (password,))
# → "OperationalError: near '?': syntax error"（PRAGMA 不支持参数化占位符）

# 错误方案 2: conn.execute(f"PRAGMA key = '{password}'")
# → 密码含 `'` 时引爆 SQL

# ✅ 正确方案: executescript + 单引号转义
safe_password = password.replace("'", "''")
conn.executescript(f"PRAGMA key = '{safe_password}'; PRAGMA foreign_keys = ON;")
```

**为什么用 executescript**：sqlite `PRAGMA` 不支持 `?` 参数化，必须走 SQL 解析器。`executescript` 内部用 sqlite3 解析器，能吃字符串拼接 + 多语句（顺便把 `PRAGMA foreign_keys = ON` 也合进去）。

### 2.3 行工厂：dict_factory 而非 sqlite3.Row

```python
def _dict_factory(cursor, row):
    return {col[0]: row[idx] for idx, col in enumerate(cursor.description)}

conn.row_factory = _dict_factory
```

**坑**：sqlcipher3 的 `Cursor` **不是** `sqlite3.Cursor` 子类。直接用 `sqlite3.Row` 工厂会抛：

```
TypeError: Row() argument 1 must be sqlite3.Cursor, not sqlcipher3.dbapi2.Cursor
```

**方案**：自定义 `dict_factory`，行为等价（`row["col"]` 可用），且不依赖 cursor 类型。

### 2.4 主动密码校验：PRAGMA quick_check

```python
# SQLCipher 是懒校验的 — PRAGMA key 本身不读加密页
# 错误密码要到第一次 SELECT/INSERT 时才报 "file is not a database"
# 错误位置远离 root cause，用户 debug 痛苦

# 试错的触发方式（不靠谱）:
# - SELECT count(*) FROM sqlite_master   → page 1 header 不加密，错密码也能跑
# - PRAGMA cipher_version                 → 编译时常量，不读加密页
# - PRAGMA integrity_check                → 可靠但读全库，MemoryError 风险

# ✅ PRAGMA quick_check → 轻量 + 100% 触发
try:
    conn.execute("PRAGMA quick_check").fetchone()
except sqlcipher3.DatabaseError as e:
    conn.close()
    raise sqlcipher3.DatabaseError(f"DB 打开失败：密码错误或文件损坏 ({e})") from e
```

**实战经过**：原本想用 `SELECT * FROM emails LIMIT 1` 触发，但**全新空 DB 没有 emails 表** → 抛 "no such table" → 被误判为密码错。这个鸡生蛋问题用 `PRAGMA quick_check` 完美解决（不依赖任何表存在，只读 schema root page）。

### 2.5 复用 D2.3 keychain

不重新发明 Keychain 包装，直接 import：

```python
from my_ai_employee.core import keychain
# keychain.get_db_password() / set_db_password() 已在 D2.3 预埋
# SERVICE_DB 常量也已定义
```

Keychain service 命名严格遵守 D2.3 规范（`my-ai-employee.<用途>`）。

---

## 3. 测试覆盖（23 个 — v1 15 / D3.1.1 +5 / D3.1.2 +3）

### 3.1 Keychain 密码管理（2）

- `test_first_open_generates_password` — 首次启动自动生成 32 字节随机串
- `test_keychain_password_persists_across_opens` — 二次开复用同一密码

### 3.2 加密往返（2）

- `test_db_round_trip_with_correct_password` — 写 → 关 → 重开 → 读出
- `test_db_rejects_wrong_password` — 错密码 `Database.open()` 主动抛 DatabaseError

### 3.3 Schema 应用（3）

- `test_init_schema_creates_all_tables` — 6 张表都存在
- `test_init_schema_is_idempotent` — 跑三遍不爆（IF NOT EXISTS）
- `test_init_schema_creates_indexes` — **9 个索引**都创建（D3.1.1 增 `idx_emails_message_id`）

### 3.4 CRUD（3）

- `test_execute_and_fetch_all` — executemany + fetch_all + fetch_one
- `test_unique_constraint_on_emails_source_uid`（D3.1.1 改名）— `UNIQUE(source, uid)` 约束生效
- `test_foreign_keys_enabled` — PRAGMA foreign_keys = ON

### 3.5 PRAGMA 配置（D3.1.1 增 — 3）

- `test_journal_mode_is_wal` — `PRAGMA journal_mode` 返回 "wal"
- `test_busy_timeout_is_5000` — `PRAGMA busy_timeout` 返回 timeout=5000（**列名是 `timeout` 而非 `busy_timeout`**，SQLite 文档规定）
- `test_synchronous_is_normal` — `PRAGMA synchronous` 返回 1（NORMAL=1 / OFF=0 / FULL=2）

### 3.6 字段可空（D3.1.1 增 — 2）

- `test_message_id_is_nullable` — INSERT 不传 message_id → 成功；读出为 None
- `test_received_at_is_nullable` — INSERT 不传 received_at → 成功；读出为 None（fallback 锚点：fetched_at 必须有）

### 3.7 受控 connection 入口（D3.1.2 增 — 3）

- `test_connection_property_returns_raw_connection` — `db.connection` 是 `sqlcipher3.Connection` 实例 + 能跑 `PRAGMA journal_mode` 返回 "wal"
- `test_connection_property_raises_after_close` — `db.close()` 后访问抛 `RuntimeError`（消息含 "DB 已关闭"）
- `test_connection_property_raises_after_context_exit` — with 块正常退出后再访问抛 `RuntimeError`

### 3.8 上下文管理器（2）

- `test_context_manager_commits_on_success` — 正常退出 commit
- `test_context_manager_rolls_back_on_exception` — 异常 rollback

### 3.9 路径/模块（3）

- `test_default_db_path_is_macos_standard` — 默认路径 = Application Support
- `test_schema_path_exists` — schema.sql 文件存在
- `test_module_exports` — **all** 导出正确

**Fixture 设计**：
- `tmp_db_path` — pytest `tmp_path`（每个测试独立，零污染）
- `fake_keychain` — monkeypatch `keychain.get_db_password/set_db_password` 用 in-memory dict（不污染真实 macOS Keychain）

---

## 4. 已知雷区 + 解决方案

| # | 雷区 | 触发 | 解决 |
|---|------|------|------|
| 1 | PRAGMA 不支持 `?` 占位符 | `conn.execute("PRAGMA key = ?", ...)` | `executescript` + 字符串拼接 + 单引号转义 |
| 2 | sqlcipher3 Cursor 不是 sqlite3.Cursor | `conn.row_factory = sqlite3.Row` | 自定义 `_dict_factory(cursor, row) → dict[列名, 值]` |
| 3 | SQLCipher 懒校验 — PRAGMA key 不报密码错 | 错密码 → 用 SELECT 等操作时才报 | `Database.open()` 主动跑 `PRAGMA quick_check` 触发 |
| 4 | SELECT count(*) FROM sqlite_master 不触发校验 | 空库/小 schema | 改用 `PRAGMA quick_check`（不依赖表存在）|

---

## 5. D3.2 / D3.3 启动包

### 5.1 D3.2 — ORM + Migrations（预计 2-3 小时）

**范围**：
- `src/my_ai_employee/core/models.py`（SQLAlchemy 2.0 DeclarativeBase，6 个 Model 类）
- `src/my_ai_employee/core/migrations/env.py`（alembic 初始化，集成 SQLCipher 密码）
- `src/my_ai_employee/core/migrations/versions/0001_initial.py`（从 schema.sql 翻译成 alembic op）
- `src/my_ai_employee/core/migrations/script.py.mako`（alembic 模板）
- `alembic.ini`（项目根目录）
- `tests/core/test_models.py`（CRUD + relationship + cascade）

**关键设计点**：
- `models.py` 必须独立 import `db.py`（复用 `Database` + Keychain）— 不能反向依赖
- alembic env.py 调 `Database.open().connection` 拿受控 raw connection（**D3.1.2 加 connection property** — 避免直接依赖私有 `_conn`）
- 5+ 个表 → 5+ 个 Model 类（Email / Attachment / Label / EmailLabel / SyncState / AuditLog）

### 5.2 D3.3 — 同步脚本 + Spike（预计 3-4 小时）

**范围**：
- `scripts/sync_imap.py`（调 `IMAPConnector.safe_fetch` + `Database` 批量入库）
  - 分批策略：100 封/批 commit（避免 SQLite 锁）
  - 增量同步：用 `sync_state.last_sync_at` 调 `fetch(since=...)`
  - 失败隔离：单封失败不阻塞（应急版范本）
- `tests/core/test_sync.py`（mock IMAPConnector + 真实 DB）
- **Spike**：1 万封 mock 邮件 < 30s 入库

**关键设计点**：
- `Email` ORM 的 `__init__` 接受 `IMAPConnector.fetch()` 返回的 dict
- `sync_state` upsert 逻辑：每 source 一行，新批次更新
- Spike 用 `faker` 生成 1 万 envelope dict

---

## 6. 应急版范本应用

D3.1 全程未触发应急版（环境一次过）。但 D3.3 sync 脚本要小心：

| 失败模式 | 应急路径 |
|----------|----------|
| 真实 QQ IMAP 抖动 | `IMAPConnector.safe_fetch` 隔离（已有）|
| DB 锁 | 分批 commit 100/批（避免长事务）|
| 1 万封 spike 超时 | 降级到 1000 封 < 5s，标注"spike 降级版"|

---

## 7. 链路状态

```
D1 ✅ → D2 ✅（真实 QQ 验收通过） → D3.1 ✅ → D3.2 待启动 → D3.3 待启动 → D4 (CalDAV)
```

> ⚠️ **重要**：当前只能说 **D3.1 完成且质量门全绿**。
> D3 整体完成还需 D3.2 (ORM+migrations) + D3.3 (IMAP 入库+1万封 spike)。
> events / transactions / notes / health_log / FTS5 / sqlite-vss 已从 D3 段移除，推到 D4-5 智能层。

---

## 8. D3.1.1 — 7 个修正落地（用户验收后补充）

D3.1 v1 提交后用户给出 7 个修正点，全部已落地。

### 8.1 修正清单

| # | 修正点 | 落地位置 | 关键变更 |
|---|--------|----------|----------|
| 1 | D3 范围拆分 | [docs/week1-mvp.md §D3](../docs/week1-mvp.md#d3--数据层--imap-同步) | D3 → D3.1/D3.2/D3.3 三段；FTS5/vss 推到 D4 |
| 2 | **WAL 开启** | [db.py](../src/my_ai_employee/core/db.py) §open() | `journal_mode=WAL` + `busy_timeout=5000` + `synchronous=NORMAL` |
| 3 | **去重改 UNIQUE(source, uid)** | [schema.sql](../src/my_ai_employee/core/schema.sql) emails | 删 `UNIQUE(source, message_id)`；`uid` 进唯一键；`message_id` 改可空 + 普通索引 |
| 4 | **received_at 可空** | [schema.sql](../src/my_ai_employee/core/schema.sql) emails | D3.3 入库映射层 fallback 到 `fetched_at` |
| 5 | Keychain 文档对齐 | [schema.sql line 3-5](../src/my_ai_employee/core/schema.sql) + [week1-mvp.md §D3.1](../docs/week1-mvp.md) | 统一为"首次启动自动生成，account=data.db"；删 "D3 启动前用户需手动"段 |
| 6 | test line 88 关连接 | [test_db.py line 84-95](../tests/core/test_db.py) | `with Database.open(): pass` 包裹（避免半开连接） |
| 7 | D3 状态表述 | 本报告 §7 | "D3 整体完成" → "D3.1 完成且质量门全绿" |

### 8.2 新增 5 个测试（D3.1.1）

| 测试名 | 断言 |
|--------|------|
| `test_journal_mode_is_wal` | `PRAGMA journal_mode` 返回 "wal" |
| `test_busy_timeout_is_5000` | `PRAGMA busy_timeout` 返回 timeout=5000（列名是 `timeout` 而非 `busy_timeout` — SQLite 文档规定）|
| `test_synchronous_is_normal` | `PRAGMA synchronous` 返回 1（NORMAL=1 / OFF=0 / FULL=2）|
| `test_message_id_is_nullable` | INSERT 不传 message_id → 成功；读出为 None |
| `test_received_at_is_nullable` | INSERT 不传 received_at → 成功；读出为 None（fallback 锚点：fetched_at 必须有）|

### 8.3 新增 1 个测试改写

`test_unique_constraint_on_emails_source_message_id` → `test_unique_constraint_on_emails_source_uid`

- 改：去重键由 `(source, message_id)` → `(source, uid)`
- 改：重复插入改为 `(qq, 1, "<msg-different@x.com>", ...)`（UID 相同 + message_id 不同 → 仍应抛 IntegrityError）
- 改：测试 docstring 解释改键原因（Message-ID 缺失风险 vs UID 协议保证）

### 8.4 db.py 关键 bug 修复（D3.1.1 顺带修）

**bug**：D3.1 v1 时 `try/except DatabaseError` 只包了 `PRAGMA quick_check`。
D3.1.1 加 `PRAGMA journal_mode = WAL` 后，**WAL 模式切换会写 WAL 文件到磁盘**，
触发 SQLCipher 第一次读 DB header 加密页（懒校验），**错密码时从 `executescript` 内就抛 DatabaseError**，
绕过 try/except 直奔调用方（错误消息 "file is not a database" 无 "密码错误" 前缀）。

**修**：try/except 扩到覆盖整个 executescript + quick_check 块。

```python
# v1 错误:
conn.executescript(...)  # WAL 触发 SQLCipher 错密码抛 DatabaseError → 直奔调用方
conn.row_factory = _dict_factory
try:
    conn.execute("PRAGMA quick_check").fetchone()  # 永远不会到这一步
except sqlcipher3.DatabaseError as e:
    raise sqlcipher3.DatabaseError("DB 打开失败：密码错误或文件损坏 (...)") from e

# ✅ v2 正确:
try:
    conn.executescript(...)  # 含 WAL
    conn.row_factory = _dict_factory
    conn.execute("PRAGMA quick_check").fetchone()  # 二次确认
except sqlcipher3.DatabaseError as e:
    conn.close()
    raise sqlcipher3.DatabaseError("DB 打开失败：密码错误或文件损坏 (...)") from e
```

### 8.5 PRAGMA 矩阵（D3.1.1 终态）

| PRAGMA | 值 | 目的 | 测试 |
|--------|----|----|------|
| `key` | `<32 字节随机串，存 Keychain>` | SQLCipher 加密 | `test_db_rejects_wrong_password` |
| `foreign_keys` | ON | 联表查询 + cascade 删除 | `test_foreign_keys_enabled` |
| `journal_mode` | **WAL**（D3.1.1 增）| 多读单写不阻塞 | `test_journal_mode_is_wal` |
| `busy_timeout` | **5000**（D3.1.1 增）| DB 锁等 5s | `test_busy_timeout_is_5000` |
| `synchronous` | **NORMAL**（D3.1.1 增）| WAL 模式推荐（性能/安全平衡）| `test_synchronous_is_normal` |

### 8.6 收尾定位

> **当前状态**：D3.1 完成且质量门全绿。
> D3 整体完成还需 D3.2 (ORM+migrations) + D3.3 (IMAP 入库+1万封 spike)。
> D3.2 / D3.3 启动包详见 [docs/week1-mvp.md §D3.2/D3.3](../docs/week1-mvp.md)。

---

## 9. D3.1.2 — 文档清理 + 受控 connection 入口

D3.1.1 收尾后用户验收发现 5 个文档一致性修正点（详见 `d3.1.2-doc-cleanup.md` 外部 memory），
全部已落地。

### 9.1 修正清单

| # | 修正点 | 落地位置 | 关键变更 |
|---|--------|----------|----------|
| 1 | [core/\_\_init\_\_.py](../src/my_ai_employee/core/__init__.py) 旧 D3 设计 | 删 `5 张表（emails/events/transactions/notes/health_log）` 段 | 改为 `6 张表（emails/attachments/labels/email_labels/sync_state/audit_log）`；ORM/migrations 标 D3.2；indexer 标 D4 |
| 2 | D3.1 报告 §1.1 / §1.2 旧口径 | 报告 §1.1/§1.2 | 8 索引 → 9 索引；15 测试 → 23 测试；UNIQUE(source, message_id) → (source, uid)；标 D3.1.1 + D3.1.2 修正 |
| 3 | D3.1 报告外部 `file://` 链接 | 报告 §2.1 + 末尾 | 全部删除，改为"用户私人 memory 索引，不在本仓库内" |
| 4 | [week1-mvp.md §D3.1 验收](../docs/week1-mvp.md) 算式口误 | 报告 + week1-mvp.md | `52 passed（37 D2 + 20 D3.1.1 = 实际 52+5=57）` → `60 passed（37 D2 + 23 D3.1）` |
| 5 | `_conn` 私有属性泄漏给 alembic | [db.py](../src/my_ai_employee/core/db.py) + week1-mvp.md + 报告 §5.1 | 新增受控 `Database.connection` property（带 closed 保护 + docstring 警示）|

### 9.2 受控 connection property（D3.1.2 核心代码变更）

**问题**：alembic 迁移需要拿原始 connection 才能 `connection.run_sync(...)` 跑 SQLAlchemy DDL，
但直接用私有 `_conn` 是封装泄漏（破坏封装、绕过事务/上下文管理逻辑、命名易冲突）。

**方案**：在 `Database` 类加 `connection` property：

```python
@property
def connection(self) -> sqlcipher3.Connection:
    """受控访问底层 sqlcipher3 connection（供 D3.2 alembic env.py / 高级用法）。

    警告：
        - 直接 db.connection.execute(...) 不会自动 commit（绕过 Database 封装）
        - 关闭后访问抛 RuntimeError（避免使用半关连接）
        - 普通 CRUD 请用 execute / fetch_all / fetch_one
    """
    if self._closed:
        raise RuntimeError(
            "DB 已关闭，无法访问 connection。"
            "请确保 Database 实例在 with 块内使用（context manager）。"
        )
    return self._conn
```

**设计权衡**：
- ✅ 仍是 public API（不是 `_conn` 私有），但**带 closed 保护**
- ✅ docstring 警示直接用 `db.connection` 会绕过事务管理
- ✅ 调用方仍应走 `with Database.open() as db:` 拿 db
- ❌ **不**返回 `Engine`（D3.1 不依赖 SQLAlchemy；alembic env.py 自己 `engine_from_config` 包装）

### 9.3 新增 3 个测试（D3.1.2）

| 测试名 | 断言 |
|--------|------|
| `test_connection_property_returns_raw_connection` | `db.connection` 是 `sqlcipher3.Connection` 实例 + 能跑 `PRAGMA journal_mode` 返回 "wal" |
| `test_connection_property_raises_after_close` | `db.close()` 后访问 `db.connection` 抛 `RuntimeError`（消息含 "DB 已关闭"）|
| `test_connection_property_raises_after_context_exit` | with 块正常退出后再访问 `db.connection` 抛 `RuntimeError` |

**踩坑经验**（v3 编写时实测）：
- `db.connection.execute("PRAGMA journal_mode").fetchone()` 返回 **dict**（不是 tuple）— 因为 `Database.open()` 在 open() 里设了 `conn.row_factory = _dict_factory`，raw connection 继承这个 factory
- 修复：测试断言用 `row["journal_mode"]` 而非 `row[0]`

### 9.4 文档一致性自检清单（D3.1.2 通过项）

- [x] `core/__init__.py` 模块 docstring 与 D3.1 实际状态一致（6 表 + ORM/migrations D3.2 + indexer D4）
- [x] D3.1 报告 §1.1/§1.2 数字与实测一致（9 索引 / 23 测试 / UNIQUE(source, uid)）
- [x] D3.1 报告不含 `file://` 外部 memory 链接（避免提交后别人打不开）
- [x] week1-mvp.md §D3.1 验收数字正确（57 → 60 passed）
- [x] D3.2 / D3.3 启动包统一用 `db.connection` 而非 `db._conn`（避免封装泄漏）

### 9.5 收尾定位（v3 终态）

> **D3.1 功能和测试达标**（v3 报告）— 60 passed / db.py 97.5% / ruff+mypy+md lint 0 errors
>
> **D3 整体还不能算完成**：D3.2 ORM/migrations 和 D3.3 IMAP 入库 spike 仍未启动。
> 严格表述：当前为 **D3.1 完成且质量门全绿**，下一步启动 D3.2 ORM+migrations。

---

## 10. D3.1.3 — 文档内部一致性最终收口（正式锁定 D3.1）

D3.1.2 提交后用户再次验收，发现 v3 报告内部仍有 3 处旧 v1 口径数字未更新（自相矛盾），
本次一次性收口，**正式锁定 D3.1**，进入 D3.2。

### 10.1 修正清单（3 项 — v3 报告内部自相矛盾点）

| # | 旧口径位置 | 旧 | 新 |
|---|------------|----|----|
| 1 | 本报告 §3 标题 | `测试覆盖（15 个）` | `测试覆盖（23 个 — v1 15 / D3.1.1 +5 / D3.1.2 +3）` |
| 2 | 本报告 §3.3 第 3 条 | `8 个索引都创建` | `**9 个索引**都创建（D3.1.1 增 idx_emails_message_id）` |
| 3 | 本报告 §3.4 第 2 条 | `test_unique_constraint_on_emails_source_message_id` | `test_unique_constraint_on_emails_source_uid（D3.1.1 改名）` |
| 4 | [docs/week1-mvp.md §D3.1 验收](../docs/week1-mvp.md) line 230 | `20 个测试（15 + D3.1.1 增 5）` | `**23 个测试**（15 v1 + D3.1.1 +5 + D3.1.2 +3）` |
| 5 | [docs/week1-mvp.md §D3.1 验收](../docs/week1-mvp.md) line 239 | `pytest 57 passed（37 D2 + 20 D3.1）` | `pytest **60 passed**（37 D2 + 23 D3.1）` |
| 6 | [docs/week1-mvp.md §D3.1 验收](../docs/week1-mvp.md) line 241 | `db.py 覆盖率 97.4%` | `db.py 覆盖率 **97.5%**` |

> 编号 1-3 是**报告内部自相矛盾**（§1.1/§1.2/§9 已写 23 测试/9 索引/UID，但 §3 还写 15/8/source_message_id）。
> 编号 4-6 是 **week1-mvp.md 同步**（D3.1.2 那一轮只改了 week1-mvp.md 的算式口误，line 230 和 241 没改）。

### 10.2 §3 重写后的 9 段测试覆盖（合计 23 测试）

| § | 段名 | 测试数 | 累计 |
|---|------|--------|------|
| 3.1 | Keychain 密码管理 | 2 | 2 |
| 3.2 | 加密往返 | 2 | 4 |
| 3.3 | Schema 应用 | 3 | 7 |
| 3.4 | CRUD | 3 | 10 |
| 3.5 | **PRAGMA 配置（D3.1.1 增）** | 3 | 13 |
| 3.6 | **字段可空（D3.1.1 增）** | 2 | 15 |
| 3.7 | **受控 connection 入口（D3.1.2 增）** | 3 | 18 |
| 3.8 | 上下文管理器 | 2 | 20 |
| 3.9 | 路径/模块 | 3 | 23 ✓ |

### 10.3 正式锁定声明

> **D3.1 已正式锁定**（v3.1.3 收口后）。
> - 功能：6 表 DDL + Database 封装 + 受控 connection 入口 — 全部就绪
> - 测试：23 个 / 60 全项目 / db.py 97.5% / ruff+mypy+md lint 0 errors
> - 文档：3 份（week1-mvp.md / D3.1 报告 / 2 份 memory）— 全部一致
>
> **D3 整体仍未完成**（D3.2 ORM+migrations + D3.3 IMAP 入库+spike 待启动）。
> **下一步**：启动 D3.2（SQLAlchemy 2.0 DeclarativeBase 6 Model + alembic 迁移框架）。

---

**最后更新**：2026-06-07（D3.1 v1 + D3.1.1 7 修正 + D3.1.2 文档清理 + connection property + **D3.1.3 文档一致性收口**）
**当前模型**：MiniMax-M3
**状态**：D3.1 **正式锁定**（v3.1.3），可启动 D3.2 ORM+migrations
**相关 memory**（用户私人 memory 索引，不在本仓库内）：
- `d2-imap-real-qq-verified.md`（D2 真实 QQ 验收 + DB 密码独立警告）
- `d3.1.1-fixes.md`（D3.1 收尾 7 修正 + 关键 bug 经验）
- `d3.1.2-doc-cleanup.md`（D3.1.2 文档清理 5 修正 + connection property 踩坑）
- `d3.1.3-final-doc-consistency.md`（D3.1.3 文档内部自相矛盾收口 3 修正 + 正式锁定声明）
