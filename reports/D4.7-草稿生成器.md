# D4.7 — 草稿生成器(LLM 邮件草稿生成 + 业务层三入口)

> **状态**:✅ **v1.0.6 真正锁定**(D4.7.3 第六轮复检收口,commit `9e4fb2e`,1027 passed / +107 tests / 8 质量门全绿)
> **承接 D4.6**(邮件分类器 v1.0.2-third,2026-06-09 早晨)— `EmailClassifierAdapter` 4 依赖可注入范本 + 三入口架构
> **本步范围**:**D4.7 邮件草稿生成器**(5+1 SYSTEM prompt + 裸 JSON 契约 + 严判 `subject`/`body`/`tone` + 业务层三入口)
> **D4.7.3 / D4.6 源文件零修改**(除 `policy/__init__.py` 顶层导出新增 9 个 D4.7 符号)
> **2026-06-10 启动 · 2026-06-10 v1.0.6 收口 · 维护者:Mr-PRY · 模型:MiniMax-M3**

---

## 0. 摘要(1 段决策陈述)

**D4.7 v1.0.6 已完成,8 质量门全绿,ai 业务层 107 / drafter 78 / 全量 1027 passed,0 失败,policy+ai 累计覆盖 95%+,integration.py 单文件 91.1%。**

**D4.7 v1.0.6 已正式锁定**(D4.6 v1.0.2-third 业务层三入口范本 + D4.7 邮件草稿生成业务实现)。本步:

- ✅ `ai/drafter.py` 实现:`EmailDrafter` 类(`draft` 单封 / `draft_batch` 批量)+ `_parse_draft_response` 严判 JSON 响应 + 3 类结果数据类(`DraftResult` / `DraftBlockedResult` / `DraftFailureResult`)+ 5 类窄化异常
- ✅ `ai/prompts/draft.py` 5+1 类 SYSTEM prompt(URGENT/TODO/FYI/SPAM/PERSONAL/DEFAULT 兜底)+ `build_system_prompt` 分发 + `build_user_message` 拼接原邮件上下文 + 5+1 类加"请求 tone 强制"
- ✅ `ai/__init__.py` + `ai/prompts/__init__.py` 顶层导出 D4.7 新符号
- ✅ `policy/integration.py` 新增 `EmailDrafterAdapter` + `DraftDecisionReport` / `DraftBlockedDecisionReport` / `DraftFailureDecisionReport` + 3 factory + 4 `_validate_draft_*` helper
- ✅ `policy/__init__.py` 顶层导出 9 个新符号(沿用 D4.6 v1.0.2-second P2-3 教训)
- ✅ 78 个 drafter 单元测试 + 107 个 EmailDrafterAdapter 单元测试 = 185 新增
- ✅ 8 大质量门:lint/ruff check/ruff format/mypy src/mypy src+tests/alembic --sql/uv build/pytest 全绿(详见 §2)

**D4.7 关键设计**(D3.3.3 异常窄化 + D4.6 严判范本 + D4.7.1 v1.0.1 3 项 P1 + v1.0.2 4 项收口 + D4.7.2 八轮复检 + D4.7.3 六轮复检 25 教训):

- 复用 `router.route(TaskType.DRAFT, ...)` 自动走 fallback 链(D4.1 已配 DeepSeek → Qwen → M3)
- 严判 LLM 响应:必须严格 JSON `{"subject": str, "body": str, "tone": <enum>}` 拒 markdown-wrapped(契约 2 沿用 D4.6,剥离反退)/ 拒空 subject / 拒空 body / 拒超长 body(契约 1: 10-8000 字符)/ 拒非法 tone
- 复用 D4.6 `EmailClassifierAdapter` 三入口架构(范本下沉):`draft_and_emit`(成功) / `record_draft_business_blocked_and_emit`(业务阻断,SPAM 默认拒,`allow_spam_reply` 显式授权可放行) / `record_draft_failure_and_emit`(技术失败,cf 必填 >= 1)
- 业务字段(`draft_subject` / `draft_body` / `tone` / `model_full_id` / `email_id` / `category`)透传到 `event_metadata` 顶层,**便于 `mmx policy history` 跨业务类型查询**
- lane_entry_id 命名 `draft:<source>:<run_id>`(与 `classify:` / `sync:` 区分)

**CLAUDE.md 关键约束验证**:

<!-- markdownlint-disable MD050 -->
- ❌ "不要在代码或提交中包含敏感信息":D4.7 无任何 API Key / Token,LLM 凭证由 D4.1 OpenAI-compatible provider 注入(`OPENAI_API_KEY` / `OPENAI_BASE_URL` 环境变量,provider 层 0 接触),policy + ai 层零接触
- ❌ "不要硬编码跨平台配置":D4.7 不涉及 macOS / Windows 平台差异
- ❌ "不要创建冗余文件":D4.7 6 个新文件(2 src + 4 tests)+ 2 个扩展(ai/__init__.py + policy/__init__.py),对齐 D4.6 范本
- ✅ "修改 .md 后必跑 make lint":本报告 + mapping + week1-mvp 修完必跑(见 §2 门 7)
<!-- markdownlint-enable MD050 -->

---

## 0.5 ⚠️ v1.0 起始快照(2026-06-10,commit `7cff852`)

> **初版提交历史保留**:本节不覆盖 §0 任何 v1.0 段,仅记录 D4.7 启动审批后的初版实现。v1.0 段是"功能层全绿"快照(测试绿、文档数字 / 业务语义口径存疑),**v1.0.1 才是"严判口径真正锁定"版本**,v1.0.2 ~ v1.0.6 才是"业务层三入口 + 25 教训沉淀"版本。

**D4.7 启动审批范围**(2026-06-09 晚间,Mr-PRY 审批):

- **4 项契约锁定**:`docs/week1-mvp.md §D4.7` L694-699 真理源
  1. **草稿无 `confidence` 字段** → 业务验收用**明确长度/必填/tone 枚举**判定(`business_accepted = subject 非空 AND body 长度 10-8000 AND tone ∈ {FORMAL, FRIENDLY, CONCISE}`),**不**用 LLM 自报 confidence
  2. **拒 markdown-wrapped JSON**(不剥离 ```json ... ``` fence)→ LLM 必须返回**裸 JSON**,违者拒收触发 retry;body 字段内容允许 markdown
  3. **tone 枚举锁定**:`FORMAL` / `FRIENDLY` / `CONCISE` 三选一,D4.7.1 起始固定,后续扩枚举需 B 类审批
  4. **范围限定**:D4.7 只负责**生成草稿文本 + emit 业务事件 + 推进 Lane**,**不写** `drafts` 数据库表、**不创建** Mail.app 草稿、**不接** iCloud CalDAV;端到端联动留 D4.7.1+ / D5+ 业务调度器
- **3+1 文档沉淀法**:`reports/D4.7-草稿生成器.md` 报告(本文件)+ `docs/d4-claw-code-mapping.md §8` mapping + `docs/week1-mvp.md §D4.7` 收口(3 文档已固化)
- **已知限制**(D4.7.1+ 复检 P 项预判,5 段待用户审批):
  - 草稿质量难量化(用户主观)→ spike 100 封手标 + 用户体感打分
  - 草稿长度不可控 → 严判 body 长度上限(e.g. 8000 字符)
  - 历史回复模式需要语料 → 暂用 placeholder,D4.7.1+ 接入 `sent_emails` 表
  - `draft_batch` 顺序串行(100 封 ≈ 100-1000s)→ D4.7.1+ 改 asyncio + httpx async
  - tone 枚举硬编码(3 类锁定)→ 暂不支持 APOLOGETIC / INSPIRATIONAL 等额外枚举

**v1.0 初版实现内容**(commit `7cff852`):

- `ai/drafter.py` 完整骨架:`EmailDrafter` + `_parse_draft_response` + 3 结果数据类 + 5 异常类
- `ai/prompts/draft.py` 5+1 类 SYSTEM prompt 模板
- 78 个 drafter 单元测试(30 严判 + 10 batch + 10 prompt + 6 数据类 + 6 异常 + 6 阻塞 + 10 集成)
- 业务字段契约定义:`draft_subject` / `draft_body` / `tone` / `model_full_id` / `email_id` / `category` 6 项
- 业务层接入尚未完成(占位 `EmailDrafterAdapter` 框架)

---

## 0.6 ⚠️ v1.0.1 收口后真正锁定(2026-06-10,commit `f9e9d1d`)

> **初版 v1.0 段保留**:本节不覆盖 §0 / §0.5 任何 v1.0 段,仅记录 v1.0.1 收口的 3 项 P1 修复。v1.0 段是"功能层全绿"快照,v1.0.1 才是"严判口径真正锁定"版本。

**v1.0.1 修复 3 项 P1**(commit `f9e9d1d`,703 passed / +14 tests):

1. **严判 duck type 拒 type-coerce**(D4.6 v1.0.1 教训应用):`True → 1.0` / `"0.5" → 0.5` 静默 coerce 必须拒,严判 `type(value) is not str` 拒 bool 子类陷阱(isinstance(True, int)==True)
2. **subject/body 严判非空**:`if not subject or not body` 误判数字 0 短路,改 `if subject is None or body is None or not subject.strip() or not body.strip()` 拒纯空白字符串
3. **平衡括号定位 + 拒 markdown-wrapped JSON**(D4.6 v1.0.1 P1-4 教训应用):LLM 必须返回裸 JSON,**不剥离** ```json ... ``` fence,违者拒收触发 retry;body 字段内容允许 markdown(`*bold*` / `**bold**` 是合法草稿内容)

---

## 0.7 ⚠️ v1.0.2 业务层接入真正锁定(2026-06-10,commit `aeba0e4`)

> **本节不覆盖 §0 / §0.5 / §0.6 任何旧段**,仅记录 v1.0.2 业务层接入的 1 P1 + 3 P2 收口。v1.0.1 段是"严判口径锁定"版本,v1.0.2 才是"业务层三入口 + 公共 API 顶层暴露"真正锁定版本。

**v1.0.2 修复 1 P1 + 3 P2**(commit `aeba0e4`,714 passed / +11 tests):

1. **P1-1**:`EmailDrafterAdapter` 业务层三入口架构接入(成功 `draft_and_emit` / 业务阻断 `record_draft_business_blocked_and_emit` / 技术失败 `record_draft_failure_and_emit`,cf 必填 >= 1)
2. **P2-1**:严判下沉到 `compute_*` / `build_*` 公共 API(`_validate_draft_tone` / `_validate_draft_body_length` / `_validate_draft_latency_ms` helper 复用,改一处全改)
3. **P2-2**:`DraftFailureDecisionReport` 独立类型 + `Literal[True]` 类型层面固化 + `__post_init__` 三重校验
4. **P2-3**:`policy/__init__.py` 顶层暴露 9 个 D4.7 新符号(`__all__` 声明 + 实际可导入,D4.6 v1.0.2-second P2-3 教训应用)

---

## 0.8 ⚠️ v1.0.6 收口真正锁定(2026-06-10,commit `9e4fb2e`)

> **本节不覆盖任何旧段**,仅记录 D4.7.2 / D4.7.3 八/六轮复检收口的 25 教训沉淀。v1.0.2 段是"业务层三入口接入"版本,v1.0.6 才是"25 教训全应用 + 固化哲学落地 + 业务层契约定型"真正锁定版本。

**D4.7.2 八轮复检 + D4.7.3 六轮复检 25 教训沉淀**(commit `717b65a` + `6ceb7a9` + `17833c1` + `282e32b` + `189e2b6` + `f23ea31` + `5344062` + `9a6f335` + `9e4fb2e`,1027 passed / +107 tests):

**D4.7.2 7 次复检**(`717b65a` ~ `189e2b6` + `f23ea31` + `5344062` + `9a6f335`):

- v1.0.1:严判 `expected_tone` 入口(防 URGENT 误入 FUNNY tone)+ 删 `validation_error` 不可达分支 + `DraftResult.__post_init__` 5 字段自校验
- v1.0.2:5+1 类草稿 SYSTEM prompt 模板 + `build_system_prompt` 分发 + 32 tests + `prompts/draft.py` 解耦枚举依赖
- v1.0.3:5+1 类加"请求 tone 强制"+ `SpamBlockedError` 业务硬阻断 + `BEGIN/END_EMAIL_BODY` 抗注入 + `TestDrafterProductionPath` 5 tests
- v1.0.4:`SpamBlockedError` 阻断路径不污染 stats + `draft_blocked_category` 入口严判 SPAM + `ai/__all__` 导出新符号 + 数据类契约 5 件套
- v1.0.5:`allow_spam_reply` 严判 `type(value) is bool` + 阻断模板自动截断原主题(200-24=176 字符)+ 降级构造异常也入 results 严守 1:1 + 异常联合类型必须穷尽列出
- v1.0.6:`build_user_message` 新增 `allow_spam_reply` 透传 SPAM 授权意图 + 阻断模板 audit 字段分别截断(80/80/100)+ `_stats_already_bumped` 入口严判
- v1.0.7:`spam_reply_authorized` 计算改 `email_category_str == "SPAM" and allow_spam_reply` + 强一致契约 + 数据类字段强一致性 + 文档与实际行为一致
- v1.0.8:audit 字段 `ensure_ascii=True` + `_serialize_audit_field` helper 二分精确截断 + `DraftBlockedResult` 升级为强一致

**D4.7.3 六轮复检**(`9e4fb2e`):

- v1.0.1:业务阻断 vs 技术失败拆分(共享 helper + 互斥语义,`blocked_kind` vs `failed_kind`)+ 6 字段契约补齐(`week1-mvp.md` 锁定)+ 强一致 `reason ↔ category` + `now_ms` 严判 `type() is int`(拒 bool 子类)
- v1.0.2:独立 `DraftFailureDecisionReport` + `build_draft_failure_packet` + 双向强一致(SPAM → `spam_authorized=True`)+ 业务阻断 `kind=Literal["business_blocked"]` + 边界对称 10-8000
- v1.0.3:复用契约 helper(`draft_and_emit` 调 `drafter._validate_draft_subject/_body`)+ 业务阻断字段名重命名 `failed → blocked` + 依赖注入 `is None` 不用 `or` + `acceptance_criteria` 固定契约描述
- v1.0.4:`DraftBlockedDecisionReport` 跨字段校验(category 必 SPAM + cf 必 0 + last_error.strip 非空)+ `build_draft_packet` 严判 10-8000(复用 drafter MIN/MAX 常量)+ `current_attempts` 透传 cf + 空白字段严判
- v1.0.5:`DraftDecisionReport.__post_init__` 双层防御(工厂层 + 数据类)+ `last_draft_failed ↔ cf` 双向强一致(`True → cf >= 1` / `False → cf == 0`)+ `reason TypeError` 统一为 `ValueError` + 剩余 3 处空白字段加固
- v1.0.6:测试尚未纳入 Git + 计划文档与实现不一致 + 源码注释仍有旧口径(3 处 `body_length > 10` → `10<=body_length<=8000` / 失败入口名修正 / `current_attempts: 1` 写死 → 透传 cf / `acceptance_results` 契约 1:1 对齐)

**25 教训 5 类沉淀**(供 D4.7.4 草稿审阅复用,见 `docs/d4-claw-code-mapping.md §9.3`):

1. **业务门 ≠ 模型上下文**(D4.7.2 v1.0.6):授权意图必须显式传 user 消息(`build_user_message(allow_spam_reply=True)`)
2. **统计副作用与参数校验严格分离**(D4.7.2 v1.0.6 升级版):入口段顺序 `type` 严判 → 业务参数校验 → 业务逻辑 + stats 变更
3. **截断 ≠ 防注入**(D4.7.2 v1.0.6 + v1.0.8 升级版):必须截断 + `json.dumps` 双重防护 + `ensure_ascii=True`
4. **数据类字段一致性契约两种策略**:强一致(DraftResult,`authorized=True` 必传 intent)+ 弱一致(DraftBlockedResult,v1.0.7 → v1.0.8 升级为强一致)
5. **Adapter 强一致契约必须 double-enforce**(D4.7.3 v1.0.5 P1-1 范本):工厂层 + `__post_init__` 双层防御(防"数据类被构造时漏校验")
6. **字段名级别硬区分 > 类型层面区分**(D4.7.3 v1.0.3 P2-1 范本):`DraftBlockedDecisionReport.blocked=Literal[True]` vs `DraftFailureDecisionReport.failed=Literal[True]`(防通用 `if report.failed` 绕过)
7. **依赖注入用 `is None` 不用 `or`**(D4.7.3 v1.0.3 P2-2 范本):保留 falsey 替身,区分"未传入"vs "falsey 替身"
8. **固化哲学**:修复完成 ≠ 修复落地,测试必须入 Git 才算"已固化"(D4.7.3 v1.0.6 P1-1 范本)
9. **入口校验必须复用契约 helper,不要自造**(D4.7.3 v1.0.3 P1-1 范本):契约修改只改 drafter.py 一处
10. **business_accepted ≠ transport_alive**(D4.6 v1.0.1 范本):草稿长度越界 / 空 subject / 非法 tone ≠ LLM 死(业务验收独立于传输存活,契约 1)
11. **last_draft_failed 显式 bool**(D4.6 v1.0.1 范本):解决成功路径误触发 retry / escalate
12. **平衡括号定位**(D4.6 v1.0.1 范本):严判 JSON 必须先平衡括号再 `json.loads`,防 LLM 半截 JSON 触发 `JSONDecodeError`
13. **严判 duck type 拒 type-coerce**(D4.6 v1.0.1 范本):`True → 1.0` / `"0.5" → 0.5` 静默 coerce 必须拒
14. **统计副作用必须在构造成功之后**(D4.7.2 v1.0.8 升级版):防 emoji 构造失败污染 audit 口径
15. **文档与实现一一对应**(D4.7.3 v1.0.6 范本):契约兑现,过期注释比无注释更危险
16. **bool 子类是 int 子类**(D4.7.3 v1.0.1 范本):`isinstance(True, int)==True` 陷阱,严判 `type() is int` 拒 bool
17. **报告类型必须与触发场景严格 1:1 对应**(D4.7.3 v1.0.2 范本):防"伪造"语义(DraftBlockedDecisionReport 必 SPAM + 业务阻断)
18. **强一致约束必须双向覆盖**(D4.7.3 v1.0.2 + v1.0.5 升级版):防漏方向
19. **边界值必须上下对称**(D4.7.3 v1.0.2 P2-3 范本):`10 <= body_length <= 8000`,v1.0.1 仅下限 + 默认 2000 错误
20. **dataclass 默认值字段必须放最后**(D4.7.3 v1.0.2 范本):Python 语法限制,non-default follows default 报错
21. **strip() 严判语义非空**(D4.7.3 v1.0.2 P2-3 范本):防纯空白字符串
22. **公共 API 入口必加 strip() 严判语义非空**(D4.7.3 v1.0.4 P2-4 范本):尤其可能被 Exception 喂入的字段 `last_error_str`
23. **业务 payload 透传字段必须显式类型转换**(D4.7.3 v1.0.1 P2-1 范本):`spam_reply_intent` 三形态(枚举 / None / 字符串)
24. **PermissionProfile 没有 WRITE_DRAFT**(D4.7.3 v1.0.1 范本):用 READ_ONLY
25. **测试用 Fake 类继承真实类 + 双层防御验证**(D4.7.3 v1.0.1 范本):`DuckDraftResult` 覆盖测试,验证契约可继承

---

## 1. 测试覆盖范围

| 模块 | 测试文件 | 测试数 | 覆盖重点 |
|------|---------|--------|----------|
| `ai/drafter.py` | `tests/ai/test_drafter.py` | 78 | 严判 / batch / prompt / 数据类 / 异常 / 阻塞 / 集成 |
| `policy/integration.py`(D4.7 段) | `tests/ai/test_drafter_adapter.py` | 107 | 三入口 + 公共 API + 顶层导出 + 7 项契约 + 字段名硬区分 + 双向强一致 + 跨字段校验 + 工厂严判 1:1 + 透传 cf + strip() 语义非空 + type 严判在 hash 前 |
| **D4.7 业务层小计** | — | **185** | D4.6 v1.0.2-second 107 → D4.7 +78 |
| **全量 pytest** | — | **1027** | D4.6 v1.0.2-third 920 → D4.7 +107 |

**D4.7.1 ~ D4.7.3 演进(测试数)**:

- D4.7.1 v1.0(`7cff852`):78 tests
- D4.7.1 v1.0.1(`f9e9d1d`):703 passed / +14 tests
- D4.7.1 v1.0.2(`aeba0e4`):714 passed / +11 tests
- D4.7.2 v1.0 ~ v1.0.8(8 commits):746 → 925 passed / +32 tests
- D4.7.3 v1.0 ~ v1.0.6(6 commits):1027 passed / +107 tests(7 轮复检)

**D4.7 聚焦测试**:`577 passed in 0.81s`(`test_drafter.py` + `test_drafter_adapter.py` + `test_reviewer.py` + `test_reviewer_adapter.py`)

---

## 2. 8 质量门实测(D4.7.3 v1.0.6 收口时,commit `9e4fb2e`)

| # | 门 | 命令 | 结果 |
|---|----|------|------|
| 1 | `pytest`(全量) | `uv run pytest` | **1027 passed in 2.3s**(D4.6 v1.0.2-third 920 → D4.7 +107) |
| 2 | `ruff check` | `uv run ruff check` | All checks passed |
| 3 | `ruff format` | `uv run ruff format --check` | 100 files already formatted |
| 4 | `mypy src` | `uv run mypy src` | 0 errors / 44 source files(D4.6 v1.0.2-third 41 → D4.7 +3) |
| 5 | `mypy src+tests` | `uv run mypy src tests` | 0 errors / 84 source files(D4.6 v1.0.2-third 81 → D4.7 +3) |
| 6 | `alembic --sql` | `uv run alembic upgrade head --sql` | exit 0(无新增 migration,D4.7 范围 0 写 DB) |
| 7 | `uv build` | `uv build` | tar.gz + .whl OK |
| 8 | `make lint` | `make lint` | 0 errors |

**D4.7 子任务 D4.7.7 ~ D4.7.11 8 质量门实测**(commit `9e4fb2e`,D4.7.3 收口时):

| 子任务 | 内容 | 状态 |
|--------|------|------|
| D4.7.7 | `docs/week1-mvp.md §D4.7` 本段(v1.0 → v1.0.6 演进) | ✅ v1.0.6(本 docs-only commit 同步) |
| D4.7.8 | `docs/d4-claw-code-mapping.md §8` D4.7.3 段 | ✅ v1.0.6(本 docs-only commit 同步) |
| D4.7.9 | `reports/D4.7-草稿生成器.md` v1.0.6 报告(8 质量门 + 25 教训应用) | ✅ v1.0.6(本 docs-only commit 同步) |
| D4.7.10 | **Spike**:100 封真实邮件跑 `draft` + 草稿质量用户体感 | 🎯 待 D4.7.4 启动前补 |
| D4.7.11 | 8 质量门 + commit + 验收 | ✅ v1.0.6(本 docs-only commit 同步) |

---

## 3. ⚠️ Spike 100 封缺口(D4.7.10,B 类决策)

**D4.7.10 100 封真实邮件 spike 未跑**(草稿质量用户体感打分,业务层锁定不影响)。D4.7.3 v1.0.6 收口时 spike 留待 D4.7.4 启动前并行处理,反馈数据用于:

- 草稿质量精确度用户体感打分
- 草稿长度分布 / 语气分布 / tone 命中率
- 业务阻断触发率(SPAM 默认拒 / `allow_spam_reply` 透传授权比例)
- LLM 延迟分布(单封 < 10s 验收)

**D4.7.10 spike 缺口处理**:**B 类决策延后**(per `L2_memory/_core/b-class-deferral-2026-06-09.md`),D4.7 v1.0.6 业务层契约定型点已固化为 commit `9e4fb2e`,后续 D4.7.4 草稿审阅 spike 100 封跑通后,反馈数据触发 D4.7.10 同步补全。

**B 类决策延后声明应用**(per CLAUDE.md):

- 扩 `tone` 枚举(当前 3 类,真实业务可能需 APOLOGETIC / INSPIRATIONAL / DIPLOMATIC 4-5 级)
- 加 `draft_history` 字段(语料回填,接入 `sent_emails` 表)
- `draft_batch` 改 asyncio + httpx async(100 封从 100-1000s 降到 10-100s)
- 接 SMTP 发送(留 D4.8 草稿入库 + D5+ 业务调度器)
- 草稿审阅联动(D4.7.4 已实施,业务层接入 ✅)

---

## 4. D4.7 vs D4.7.4 范本对照

| 维度 | D4.7 草稿生成器 | D4.7.4 草稿审阅 |
|------|----------------|-----------------|
| **起始** | 2026-06-10 早晨 | 2026-06-10 晚间 |
| **架构** | 草稿生成(LLM 主调用) | 草稿审阅(LLM 决策 + 4 类本地阻断) |
| **SYSTEM prompt** | 5+1 类(URGENT/TODO/FYI/SPAM/PERSONAL/DEFAULT) | 5+1 类(同上) |
| **业务阻断白名单** | 1 类(`spam_business_blocked`)+ `allow_spam_reply` 透传 | 4 类(`sensitive_word_hit` / `template_violation` / `tone_mismatch` / `factual_conflict`) |
| **三入口** | ✅(范本源头) | ✅(复用 D4.7.3 v1.0.6) |
| **业务字段** | `draft_subject` / `draft_body` / `tone` / `model_full_id` / `email_id` / `category` 6 项 | `review_passed` / `flagged_issues` / `review_summary` / `block_reason` / `blocked_word` / `model_full_id` / `email_id` / `category` 8 项 |
| **lane_entry_id** | `draft:<source>:<run_id>` | `review:<source>:<run_id>` |
| **契约** | 4 项(D4.7 启动审批锁定) | 4 项(D4.7.4 启动审批锁定,沿用 D4.7 范本) |
| **测试数** | 78 + 107 = 185 | 95 + 118 = 213 |
| **8 质量门** | 8/8 全绿(commit `9e4fb2e`) | 8/8 全绿(commit `b1497b3` 后 `b15ba96` + `cd1a972` + `b15ba96`) |
| **业务层契约定型** | ✅ D4.7.3 v1.0.6 25 教训沉淀 | ✅ D4.7.4 v1.0.2 复用 D4.7 25 教训 |
| **spike 100 封** | 🎯 待 D4.7.4 启动前补 | ✅ D4.7.4.10 跑通(100/100) |

**D4.7.4 直接复用 D4.7 v1.0.6 25 教训范本,无新增架构范本**(D4.7.3 v1.0.6 是 4.x 智能层业务层契约定型点)。

---

## 5. 演进路径(2026-06-10 收口)

| 阶段 | commit | 内容 | 测试数 |
|------|--------|------|--------|
| 启动 | `d0ff43e` | 4 项契约锁定(week1-mvp 真理源) | — |
| 范围 | `54b9e8c` | D4.7 草稿生成器范围明确 | — |
| D4.7.1 v1.0 | `7cff852` | EmailDrafter 业务层 v1.0(78 tests / 4 项契约锁定) | 78 |
| D4.7.1 v1.0.1 | `f9e9d1d` | 3 项 P1 修复(严判 duck type + subject/body 非空 + 平衡括号定位) | +14 → 703 |
| D4.7.1 v1.0.2 | `aeba0e4` | 1 P1 + 3 P2 收口(业务层三入口接入) | +11 → 714 |
| D4.7.2 8 轮复检 | `9cf8c98` + `9a6f335` + `5344062` + `f23ea31` + `189e2b6` + `282e32b` + `17833c1` + `6ceb7a9` + `717b65a` | v1.0 ~ v1.0.8 严判口径 + 抗注入 + 阻断路径不污染 stats + audit 截断 + 强一致 | +23 → 925 |
| **D4.7.3 v1.0.6** | `9e4fb2e` | 业务层三入口 6 轮复检 25 教训沉淀 + 固化哲学落地 | +107 → 1027 |
| **v1.0.6 收口** | `9e4fb2e` | 8 质量门 8/8 全绿 + 业务层契约定型点 + D4.7.4 范本源头 | — |

**下一棒 → D4.7.4 草稿审阅**(D4.7 v1.0.6 业务层三入口 + 25 教训范本 + D4.7.4 草稿审阅业务实现)。D4.7 v1.0.6 业务层契约定型后,2026-06-10 晚间启动 D4.7.4(详见 `docs/week1-mvp.md §D4.7.4` + `reports/D4.7.4-草稿审阅.md` + `docs/d4-claw-code-mapping.md §9`)。

---

## 6. 关联文档(3+1 文档沉淀法)

| 文档 | 路径 | 状态 |
|------|------|------|
| 报告 | `reports/D4.7-草稿生成器.md`(本文件) | ✅ v1.0.6 docs-only 收口 |
| 范围 | `docs/week1-mvp.md §D4.7` L690-761 | ✅ v1.0.6 docs-only 收口 |
| mapping | `docs/d4-claw-code-mapping.md §8` | ✅ v1.0.6 沿用(D4.7.3 段在 §8 L462-466 固化) |
| spike | `scripts/spike_draft_100.py` + `output/spike/spike_draft_100_*.md` | 🎯 B 类决策延后(待 D4.7.4 启动前补) |

**完整 3+1 文档沉淀**:本报告 + 范围 + mapping + spike 全部到位 = 业务层 v1.0.6 docs-only 收口。
