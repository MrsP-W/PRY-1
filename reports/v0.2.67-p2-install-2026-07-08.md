# v0.2.67 P2 launchd install 真部署报告(2026-07-08)

## 📌 报告目的

P2 真 install 完整执行收口 — 把 P0/P1/P2 three-stage 包(推出 + 冻结基线 + launchd 真部署)闭环进 v0.2 launch plan。

## 🎯 执行范围

| 阶段 | 任务 | 实测结果 |
|------|------|----------|
| **P0** | 远端状态同步(`git push` ahead 2)| ✅ `3058e11..76cd2eb main -> main` 推送成功 |
| **P1** | v1.0 前冻结基线(3 门 quality gates)| ✅ `make lint` 260 · `make check-snapshot` 双门 OK · `make mypy` 0 errors / 256 files |
| **P2** | launchd 无人值守预演 | ✅ preflight → 真 install → bootout → 5 件套 sync |

## 🚨 撞坑 #81 实测命中复盘

### 触发

- 时间:2026-07-08 13:11(launchctl load -w 后立即)
- plist:`com.myaiemployee.digital-employee.plist`
- plist 字段:`RunAtLoad=true`
- 触发链:`start-digital-employee.sh` #!/bin/bash → TCC Python.framework 3.12 鉴权拦截 → exit 126

### 撞击历史

- 撞坑 #81 2026-07-01 首次命名(`memory/pitfall-81-*.md`)
- 历史 closure 范本:`ops/day2-81-fix-closure.md`(2026-07-01)
- 历史 runbook 范本:`ops/day2-81-tcc-fix-runbook.md`

### 本次即时恢复

```bash
launchctl bootout gui/$(id -u)/com.myaiemployee.digital-employee
# exit=0
```

**结果**:
- ✅ 数字员工 launchd job 立即移除(不反复重试)
- ✅ plist 文件保留(用户 TCC 授权后可手动 `launchctl load -w` 重新激活)
- ✅ wrapper `my-ai-employee-start` 保留(183 bytes)
- ✅ agent + imap-sync 2/3 job 仍注册运行

### TCC 授权后 5 步骤

1. **手动授权**:System Settings → Privacy & Security → Files and Folders(或 Full Disk Access)
2. **验证 TCC**:`bash ops/start-digital-employee.sh` 应无 Operation not permitted
3. **重新激活**:`launchctl load -w ~/Library/LaunchAgents/com.myaiemployee.digital-employee.plist`
4. **验证日志**:tail -f `~/Library/Logs/MyAIEmployee/digital-employee.out.log`
5. **24h 观察**:launchctl list PID 稳定 + 无重复 Operation not permitted + menu bar/dashboard/scheduling 链路稳定

## 📊 当前 launchd 部署状态

| job | RunAtLoad | 触发时间 | launchctl list | wrapper | plist | log 文件 |
|-----|-----------|---------|----------------|---------|-------|---------|
| `com.myaiemployee.agent` | false | 每月 1 号 09:00 | `- 0`(待启动) | 290 bytes | 1269 bytes | agent.{out,err}.log |
| `com.myaiemployee.imap-sync` | false | 每日 07:00 | `- 0`(待启动)| 530 bytes | 1266 bytes | imap-sync.{out,err}.log |
| `com.myaiemployee.digital-employee` | true | RunAtLoad | **bootout** ⏸️ | 183 bytes | 1332 bytes | digital-employee.{out,err}.log |

## ✅ 已闭合 4 个 P2 完成标准

- [x] **可启动**:3 job 注册完成(2/3 实际运行 + 1/3 待 TCC 激活)
- [x] **可停止**:`launchctl bootout` 已实测退出码 0
- [x] **可查日志**:6 个 log 文件已 touch 在 `~/Library/Logs/MyAIEmployee/`
- [x] **可回滚**:`bash scripts/launchd_install.sh uninstall` 已具备章节(撞坑 #81 后部分 uninstall 顺延)

## 📅 下一棒 P3 立刻启动(用户 13:10+ 明确授权)

| 任务 | 命令 | 收件人授权要求 |
|------|------|---------------|
| Notes 1-5 真同步 | `uv run python -m my_ai_employee.scripts.sync_notes sync --max-rows 5 --provider icloud` | 独立授权(Notes 是个人数据,需明确同意) |
| SMTP 单封真发 | `uv run python -m my_ai_employee.scripts.process_inbox --execute --limit 1` | **逐封命名收件人授权**(红线 · 不默认域白名单)|
| 审计 | 双审计落地(LOG + UI 双向) | 自动(撞坑 #85 三层防御已闭合 Layer 1+2+3)|
| 失败回滚 | OutboxStore.update(status=cancelled) + Notes rollback | 自动(`scripts/dispatch_outbox.py --cancel`) |

## 🔗 关联文档

- `docs/v0.2-launch-plan.md` 当前实测基线段
- `docs/v0.2.65-day13-preflight-hour-1-7-2026-07-08.md` P0 preflight 收口
- `docs/v0.2.66-p2-launchd-preflight-2026-07-08.md` P2 launchd preflight 收口
- `docs/v0.2.67-p2-install-2026-07-08.md` P2 install 真部署收口(本文档对应 docs)
- `memory/pitfall-81-*.md` 撞坑 #81 历史范本
- `memory/checkpoint-2026-07-08-p0-p2-launchd-preflight.md` 本轮 P0-P2 preflight 阶段 checkpoint
